# ATLAS PROOF — Реестр решений

> **Это первый файл, который читают в любом новом чате.**
> Он существует, чтобы уже принятые решения не обсуждались заново.
>
> Статусы:
> `LOCKED` — не пересматривать. Если работа требует изменить решение —
> вывести `STRATEGY REVIEW REQUIRED` и остановиться.
> `OPEN` — ждёт решения владельца. Реализацию, зависящую от него, не начинать.
> `SUPERSEDED` — заменено; указано, чем именно.
>
> Реестр **append-only**: строки не удаляются, статус меняется.
> Новое решение владельца записывается сюда в том же коммите, что и работа.

---

## Продукт и доступ

| ID | Решение | Статус | Где обосновано |
|---|---|---|---|
| D-001 | Приоритет источников: `01_LOCKED_DECISIONS.md` выше canonical. Legacy-фичи (Links, Deep Check, TON/GRAM, тарифы FREE/START/PLUS/PRO, 4 стадии прогресса) в код не попадают | LOCKED | `atlas-audit.md`, решение A2 |
| D-002 | Стек: Next.js 16 App Router + отдельный Node-worker, PostgreSQL 16 + Drizzle, pg-boss, SSE + polling fallback, Tailwind 4, vitest + реальный Postgres, Playwright | LOCKED | `atlas-audit.md`, решение A1 |
| D-003 | DEMO: 3 Proof пожизненно, потолок способности `TARGETED_REFRESH`, ограниченный список проектов. DEMO — настоящий ATLAS в миниатюре, ограничен охватом и бюджетом, но не качеством | LOCKED | `01_LOCKED_DECISIONS.md` §2 |
| D-004 | ARI • CORE: 2999 Stars в месяц, `FRESH_RESEARCH`, динамический список `ACTIVE_CORE`-проектов, а не фиксированный каталог | LOCKED | `01_LOCKED_DECISIONS.md` §3 |
| D-005 | Proof приватен по умолчанию. Публичных Proof-ссылок в v1 нет; значения `PUBLIC` не существует в схеме | LOCKED | `01_LOCKED_DECISIONS.md` §7 |
| D-006 | Scope и Entitlement — разные проверки. Проект может быть внутри области ATLAS и при этом недоступен DEMO | LOCKED | `atlas-intent`, `gates.ts` |
| D-007 | Подписка `CANCEL_AT_PERIOD_END` даёт доступ до `valid_until` (вариант A) | LOCKED | `phase-1-plan.md` §5 |
| D-008 | Квота DEMO — reservation model: допуск считает `RESERVED + CONSUMED`, пользователю показывается только `CONSUMED`. Неудачный гейт квоту не тратит | LOCKED | `phase-1-plan.md` |
| D-009 | Максимум 2 уточнения, затем flow закрывается фиксированным сообщением. Лимит держит CHECK в БД | LOCKED | `01_LOCKED_DECISIONS.md` §5 |

## Архитектура и инварианты

| ID | Решение | Статус | Где обосновано |
|---|---|---|---|
| D-010 | Инварианты живут в БД, а не в дисциплине кода: партиальные уникальные индексы (один активный job, dedupe активных задач, одна entitling-подписка, одна ACTIVE-версия Pattern), CHECK-констрейнты, триггеры графа переходов, append-only журнал | LOCKED | `phase-1-plan.md` §4 |
| D-011 | PostgreSQL — источник истины. SSE — только транспорт уведомлений. Три слоя сходимости: снимок при подключении, NOTIFY, периодическая сверка | LOCKED | `phase-3-plan.md` |
| D-012 | ResearchJob — персистентная сущность, а не блокирующий вызов. Если UI обещает «вернитесь позже», сервер обязан это выполнить | LOCKED | `02_ARCHITECTURE_AND_PHASES` |
| D-013 | Пользовательский ввод — всегда данные, никогда инструкции. Границы блока данных экранируются | LOCKED | `phase-4-plan.md` §5 |
| D-014 | Модель предполагает — каталог решает. Резолюция проекта детерминирована сервером; LLM не может «заявить» проект в scope | LOCKED | `phase-4-plan.md` §2.6 |
| D-015 | Entitlement Gate проверяет **все** названные сущности задачи. Половина сравнения не выполняется | LOCKED | `atlas-intent`, adversarial review Фазы 4, HIGH-1 |
| D-016 | Два слоя понимания: `understood_summary` (язык пользователя, показывается) и `research_task` (английский, вход движка, наружу не идёт) | LOCKED | `phase-4-freeze.md` §1 |
| D-017 | Исследование стартует только при `status=READY` **и** `route=DEEP_RESEARCH` | LOCKED | `phase-4-plan.md` §3.4 |
| D-018 | Аутентификация: HMAC-проверка initData на сервере, сессия в HttpOnly-cookie, CSRF-токен, allowlist только своих origin, bootstrap-исключение для `POST /api/auth/telegram` | LOCKED | `phase-2-plan.md` |
| D-019 | Rate limit хранится в таблице `auth_rate_limits`; бакет по Telegram-id заводится только после успешной проверки подписи (иначе — вектор блокировки чужого аккаунта) | LOCKED | `phase-2-plan.md` §2.1 |
| D-046 | `quick_answer` на маршруте `OUTSIDE_CURRENT_DOMAIN` нормализуется в `null`, а не роняет контракт — поле декоративное там (продукт показывает свой фиксированный текст, `toView()` его всё равно отбрасывает). На `DEEP_RESEARCH` строгость сохранена намеренно (LOCKED D-027: никакого AI-текста рядом с Proof). **Переподтверждено живой ручной приёмкой Фазы 4, 2026-08-21** | LOCKED | регрессия «Стоит ли покупать SUI сейчас?» → 502; `src/server/interpreter/schema.ts` (`normalizeModelOutput`); `phase-4-freeze.md` §6 |
| D-047 | `route=CLARIFICATION_REQUIRED` механически форсирует `status=NEEDS_CLARIFICATION`, тем же способом, что уже применён к маршрутам-объяснениям (route → status — наша проекция, не суждение модели). До этого модель, вернувшая верный маршрут при `status=READY`, получала жёсткий отказ схемы ("READY with CLARIFICATION_REQUIRED") вместо честного уточняющего вопроса. **Переподтверждено живой ручной приёмкой Фазы 4, 2026-08-21** | LOCKED | регрессия (лог: `[interpreter] unavailable READY with CLARIFICATION_REQUIRED`, 502) на «доход протокола реально доходит до держателей токена?»; `src/server/interpreter/schema.ts` (`normalizeModelOutput`); `phase-4-freeze.md` §6 |

## ARI, память и обучение

| ID | Решение | Статус | Где обосновано |
|---|---|---|---|
| D-020 | Граница обучения §11 сохраняется. Автоматическое извлечение уроков даёт только **кандидатов** | LOCKED | `ARI_LEARNING_LOOP.md` |
| D-021 | Переход `CANDIDATE → ACTIVE` выполняет человек. Автоматического перевода нет | LOCKED | `ARI_LEARNING_LOOP.md` §11 |
| D-022 | CORE управляется человеком. Новая версия CORE — только после слепой/adversarial регрессии | LOCKED | `ARI_LEARNING_LOOP.md` |
| D-023 | Извлечение кандидатов **не** ограничено `VERIFIED`; ограничен только промоушен. Пороги извлечения и промоушена — разные | LOCKED | решение владельца по Learning Loop |
| D-024 | Пользовательская активность даёт сигналы, а не истину. Спрос не активирует тему | LOCKED | `atlas-memory` |
| D-025 | Lifecycle памяти не обходится никогда, даже в тестах — единственный барьер против отравления | LOCKED | `02_ARCHITECTURE_AND_PHASES`, security-мандат Фазы 5 |
| D-026 | Модель Interpreter — `claude-haiku-4-5`; меняется ключом `interpreter_model` без деплоя | LOCKED | `phase-4-freeze.md` §2 |
| D-027 | `QUICK_EXPLANATION` / `NO_RESEARCH_NEEDED` показывают короткий ответ ARI (вариант A) — без Proof, без Research Memory, с лимитом длины | LOCKED | `phase-4-freeze.md` §2 |
| D-028 | `interpreter_enabled = true` с Фазы 4; `research_enabled = false` до Фазы 6 | LOCKED | `phase-4-freeze.md` §2 |

## Процесс разработки

| ID | Решение | Статус | Где обосновано |
|---|---|---|---|
| D-029 | Цикл фазы: план → решение владельца → реализация → тесты → adversarial review со свежим контекстом → разбор находок → отчёт | LOCKED | `DEVELOPMENT_WORKFLOW.md` |
| D-030 | Роли — закрытый список: Main Claude, Adversarial Reviewer, Blind Evaluator (с Фаз 5–6). Новая роль только после измеренной проблемы | LOCKED | `DEVELOPMENT_WORKFLOW.md` |
| D-031 | Параллельная реализация несколькими агентами не используется | LOCKED | `DEVELOPMENT_WORKFLOW.md` |
| D-032 | Распределение моделей: Opus — архитектура, security, планы фаз, adversarial review; Sonnet — реализация, тесты, миграции, UI; Haiku — механическая генерация и runtime Interpreter | LOCKED | `DEVELOPMENT_WORKFLOW_OPTIMIZATION.md` §2–3 |
| D-033 | Чаты разделены по стадиям фазы. Право записи в код — только у чата реализации | LOCKED | `DEVELOPMENT_WORKFLOW_OPTIMIZATION.md` §5 |
| D-034 | Иерархия контекста: `DECISIONS` → LOCKED → план фазы → нужный skill. canonical поднимается точечно | LOCKED | `DEVELOPMENT_WORKFLOW_OPTIMIZATION.md` §4 |
| D-035 | Владелец продукта — отдельный обязательный слой решений между планированием и реализацией. Opus предъявляет варианты, владелец выбирает, Sonnet исполняет | LOCKED | `DEVELOPMENT_WORKFLOW_OPTIMIZATION.md` §7.1 |
| D-036 | Никогда не имитируется то, чего не произошло: ни прогресс исследования, ни пройденный тест, ни выполненная проверка | LOCKED | `PRODUCT_QUALITY_DIRECTIVE.md` |

---

## Фаза 5 — Research Memory (Strategy Lock)

| ID | Решение | Статус | Где обосновано |
|---|---|---|---|
| D-040 | Retrieval v1 — **structured-first** по онтологии (проект, тема, интент, шаг Pattern, claim-ключи) + FTS/`pg_trgm`. pgvector и эмбеддинги не вводятся. `MemoryRetrievalGateway` сохраняется как шов для будущего гибрида, если оценка покажет пропуски | LOCKED | `phase-5-plan.md` §3.4 |
| D-041 | `proofs.verification_status` (`DRAFT`/`REVIEWED`/`VERIFIED`) добавляется. `VERIFIED` гейтит **промоушен в ACTIVE-память**, а не появление кандидата. Промоушен выполняет человек | LOCKED | `phase-5-plan.md` §5.2 |
| D-042 | Кросс-проектное обобщение в v1 **запрещено**: память проекта A не закрывает шаг проекта B. Путь к переиспользуемым паттернам механизмов сохранён на будущее, но открывается отдельным решением | LOCKED | `phase-5-plan.md` §8 |
| D-043 | Ключ конфига `memory_enabled` — оценка Memory OFF vs ON штатный режим, а не хак в тестах | LOCKED | `phase-5-plan.md` §7 |
| D-044 | Словарь свежести нормализуется: `LOW_CHANGE` / `MEDIUM_CHANGE` / `HIGH_CHANGE`; `evidence` приводится одной миграцией, пока таблица пуста | LOCKED | `phase-5-plan.md` §5.3 |
| D-056 | Память обязана **менять Research Boundary Contract** (`already_satisfied_steps`, `reusable_evidence`, `required_fresh_evidence`, `missing_steps`, `known_information`, бюджет, `novelty_state`), а не подмешивать прошлый текст в промпт. Проверяемый вопрос: «исследовал бы Atlas иначе, потому что вспомнил?» | LOCKED | `phase-5-plan.md` §4.2 |
| D-057 | Режимы `MEMORY` / `TARGETED_REFRESH` / `FRESH_RESEARCH`. Переиспользование **никогда** не отменяет требование свежести | LOCKED | `phase-5-plan.md` §4.4 |
| D-058 | **Вывод режима исследования.** `missing > 0` → `FRESH_RESEARCH`; `missing = 0` и `requiredFresh > 0` → `TARGETED_REFRESH`; все шаги закрыты → `MEMORY`. `FRESH_RESEARCH` **не** означает «выбросить память и переделать все 8 шагов»: `already_satisfied_steps` продолжают ограничивать Фазу 6. Режим описывает **тип** новой работы, контракт — её **объём**. Зажатие потолком `capability_at_start` сохраняется, и при зажатии `excludedScope`/`stopConditions` заполняются ВСЕГДА, независимо от числа закрытых шагов | LOCKED | `phase-5-plan.md` §4.4; ревью HIGH-1 |
| D-059 | **Поведение health.** `OK` — может закрывать шаг. `QUESTIONABLE`/`REVERIFY`/`STALE` — извлекаются и направляют перепроверку, но шаг не закрывают (→ `REQUIRED_FRESH`). `DEPRECATED` — не извлекается вовсе: знание признано неверным, а не устаревшим (→ шаг становится `MISSING`). Принцип: закрывать шаг может только здоровая переиспользуемая память | LOCKED | `phase-5-plan.md` §2.3a; ревью HIGH-2 |
| D-060 | **Контракт покрытия шага Pattern v1.** Вводится «компонент шага»; список фиксирован внутри `research_patterns.content` (то есть внутри CORE, D-022): 1 `SOURCE_OF_VALUE`; 2 `FLOW_PATH`; 3 `MECHANISM_SPEC`+`GOVERNANCE_BASIS`; 4 `EXECUTION_EVIDENCE`; 5 `CURRENT_STATE`; 6 `DESTINATION`+`RECIPIENT`; 7 `NET_EFFECT`; 8 `DURABILITY_BASIS`. `research_memory.component` валидируется против активного Pattern — произвольная пара (claim_key, pattern_step) больше не может закрыть чужой шаг. Шаг `ALREADY_SATISFIED` только когда **каждый** его компонент покрыт пригодной записью; частичное покрытие → `REQUIRED_FRESH` с перечислением непокрытых компонентов. Универсальная онтология claim-ключей не вводится | LOCKED | `phase-5-plan.md` §4.1a; ревью MEDIUM-4 |
| D-061 | **Определение false reuse.** Шаг помечен `ALREADY_SATISFIED`, и хотя бы один элемент памяти, обеспечивший это решение, контрольный набор объявил непригодным. Причины непригодности: `STALE`, `LOW_CONFIDENCE`, `WRONG_PROJECT`, `UNHEALTHY`, `UNPROMOTED`, `WRONG_STEP_MAPPING`, `CONTRADICTED`. Считается по шагам: числитель — шаги, закрытые с участием непригодного элемента; знаменатель — все шаги `ALREADY_SATISFIED` набора. `false_reuse_rate = 0` остаётся условием приёмки. Заменяет прежнее определение (только «непромоутнутое»), которое было структурно недостижимо и потому всегда давало ноль | LOCKED | `phase-5-plan.md` §7.3; ревью MEDIUM-2; уточняет D-049 |
| D-062 | **Правило противоречия.** Две записи `ACTIVE`, прошедшие фильтры health и свежести, относящиеся к одному `(pattern_step, component)` и несущие **различный `mechanism_state`**, образуют конфликт: шаг не может быть `ALREADY_SATISFIED`, он получает `REQUIRED_FRESH` с причиной `CONTRADICTED` и перечислением конфликтующих `memoryId`. Обнаруживается только конфликт состояний; расхождение свободного текста `statement` конфликтом не считается (парафраз), семантическое обнаружение противоречий — задача Critic-слоя, не Фазы 5 | LOCKED | `phase-5-plan.md` §4.3b; ревью LOW-2 |
| D-063 | **Критерий Blind Evaluator.** Оценщик судит качество плана без утечки происхождения: из вида удаляются `reason`, `memoryIds`, `memory_used` и `knownInformation` (последнее содержит тексты памяти). Он **не обязан** не знать, была ли включена память — при `memory_enabled=false` все 8 шагов всегда `MISSING`, поэтому OFF-контракт опознаётся структурно, и прежняя формулировка обещала недостижимое. Слепая оценка применяется к сравнению ON-планов между собой; пара OFF/ON остаётся количественным сравнением метрик | LOCKED | `phase-5-plan.md` §7.5; ревью MEDIUM-3; уточняет D-049 |
| D-064 | **Предусловие пригодности DEMO.** По D-058 первый вопрос о проекте без памяти даёт 8 `MISSING` → `FRESH_RESEARCH` → потолок DEMO (`TARGETED_REFRESH`) → честная граница способности вместо запуска. Это ровно поведение LOCKED §2 («требуется полноценное исследование с нуля → Guard блокирует»), впервые ставшее исполнимым. Следствие: DEMO станет пригоден только после того, как владелец исследует три `demo_project_slugs` и промоутнет память до `ACTIVE` с полным покрытием восьми шагов. Сид создавать verified-знание не может и не должен. Не задача кода Фазы 5; блокирует полезность DEMO в Фазе 6 | LOCKED | `phase-5-plan.md` §4.4a; `01_LOCKED_DECISIONS.md` §2 |
| D-065 | **Барьер промоушена — в БД, а не только в приложении.** Триггер lifecycle обязан проверять, что `promoted_by` принадлежит пользователю с ролью `ADMIN`, а не только что поле непустое. Ревью доказало прямым SQL: обычный пользователь записывается как `promoted_by`, и переход в `ACTIVE` проходит. D-010 требует инвариантов в БД, D-025 называет lifecycle единственным барьером против отравления — проверка только в коде приложения этим требованиям не удовлетворяет | LOCKED | ревью MEDIUM-5 |
| D-066 | **Метрики считаются там, где они определены.** `recall`/`precision` усредняются только по сценариям с непустым ожидаемым множеством; сценарии с пустым ожиданием (нет памяти, просрочено, отравление, чужой проект, граница доступа) уходят в отдельную метрику отсутствия ложных срабатываний. Прежняя схема присваивала им `1`/`1` по определению и завышала агрегат | LOCKED | ревью MEDIUM-6 |
| D-048 | FACT / RESEARCH / PROOF memory разделены. Системная Research Memory **не зависит от хранения пользовательских данных**: provenance копируется при промоушене, ссылок с `RESTRICT` на пользовательский `evidence` нет — иначе удаление аккаунта блокируется | LOCKED | `phase-5-plan.md` §5.1 |
| D-049 | Метрики Фазы 5: recall, precision, noise rate, **false reuse rate**, снятые шаги, обновлённые шаги, дельта запланированных поисков, качество плана. `false_reuse_rate = 0` — условие приёмки. Blind Evaluator судит контракты, не зная, была ли память включена | LOCKED | `phase-5-plan.md` §7 |
| D-050 | Research Boundary Contract строится на 8 шагах Pattern v1 и явно фиксирует: что известно, что переиспользуется, что обновляется, чего не хватает, что вне области | LOCKED | `phase-5-plan.md` §4.1–4.2 |
| D-051 | **Стандарт приёмки слоя интеллекта**: концепция → архитектура → реализация → автотесты → живая ручная приёмка → починка и регрессия → freeze. ~5 сценариев обычно, ~7–10 для критических слоёв, углы: норма, неполный ввод, живая формулировка, граница, отказ. После починки — упавший сценарий плюс соседние | LOCKED | `DEVELOPMENT_WORKFLOW.md` |
| D-052 | Планировщик Фазы 5 **детерминирован**: раскладку памяти по шагам делает код, а не модель. Даёт воспроизводимую оценку, аудируемость и независимость от провайдера | LOCKED | `phase-5-plan.md` §4.3a |
| D-053 | Token Value Capture — первая обученная область, **не окончательная граница ATLAS**. Долгосрочная цепочка слоёв зафиксирована как направление и **не входит в объём Фазы 5**. Расширение области — отдельное решение владельца | LOCKED | `phase-5-plan.md` §10a |
| D-055 | `VERIFIED` в Фазе 5 выставляется контролируемым скриптом владельца/админа — **не ждём UI Фазы 9**. Требования: только человек, никакого автоматического промоушена моделью, полноценный админ-UI не строится, действие остаётся аудируемым. Это операционный механизм, чтобы прогнать и проверить жизненный цикл Фазы 5. Создание кандидата и промоушен в `VERIFIED`/`ACTIVE` остаются разными понятиями; эволюция CORE — вне Фазы 5 | LOCKED | решение владельца 2026-08-21 |
| D-054 | Ввод пользователя — не знание. Вывод модели — не знание. Извлечённая память — не автоматически текущая истина. Переиспользование остаётся provenance-aware, freshness-aware и auditable | LOCKED | `phase-5-plan.md` §8.0 |
| D-067 | **Фаза 5 заморожена** владельцем 2026-08-21 на каноническом HEAD `3ff98347ae468122a3ac90abcc612c378fe2af99` (ветка `claude/phase-5-research-memory`), вердикт `PHASE 5 FINAL REVIEW PASSED — READY TO FREEZE`. Архитектура Фазы 5 не переоткрывается. Четыре LOW-находки повторного ревью (`promoted_by` переписывается после промоушена; `loadActivePattern()` без явного `status='ACTIVE'`; вакуумное `SATISFIED` при пустом `requiredComponents`; `WRONG_STEP_MAPPING` без golden-сценария) остаются **явно отложенным долгом** и freeze не блокируют. Фаза 6 — **НЕ НАЧАТА**, `research_enabled = false` (D-028) | LOCKED | `phase-5-freeze.md` |
| D-068 | **`false_reuse_rate` сам по себе структурно недостаточен** для обнаружения класса регрессий D-062 («противоречие ниже порога переиспользования»). Приёмка Фазы 5 обязана и дальше оценивать вместе: `false_reuse_rate`, `negative safety` и сравнение с **объявленной** контрольной истиной сценария (объявленной вручную, не выведенной из вывода планировщика, D-061). Агрегат, зависящий от суждения проверяемого компонента, измеряет согласованность, а не правильность | LOCKED | `phase-5-freeze.md` §4; финальная перепроверка блокеров D-062 |

---

## Фаза 6 — Research Engine v1 (Strategy Lock, 2026-08-21)

Владелец утвердил направление архитектуры и оба блокирующих решения
(D-071, D-083). После утверждения проведён прореживающий проход по
предложениям D-069…D-087: детали реализации и приёмки постоянными
решениями не становятся. Классификация и судьба каждого предложения —
`phase-6-plan.md` §17.2. Номера D-069, D-073, D-082, D-085, D-086, D-087
не заведены и не переиспользуются.

| ID | Решение | Статус | Где обосновано |
|---|---|---|---|
| D-070 | **Границы интеллекта в исполнении.** Область, бюджет, допустимость действия и момент остановки определяет код; модель предлагает запросы, извлекает и нормализует содержимое внутри этих границ и никогда не выбирает следующее действие, не расширяет область и не объявляет остановку. Продолжение D-052 из планирования в исполнение. Проверка: подмена всех модельных ролей fake-реализацией не меняет ни область, ни бюджет, ни допустимость исполнения, ни причины остановки | LOCKED | `phase-6-plan.md` §4.1, §17.3 |
| D-071 | **Граница способности — не исследование.** План выше `capability_at_start` завершает job как `FAILED` с `errorCode='CAPABILITY_BOUNDARY'`: Proof не создаётся, DEMO-квота возвращается. Новое терминальное состояние в v1 не вводится. Решение владельца: граница доступа не является выполненным исследованием и не должна ни изготавливать Proof, ни списывать квоту | LOCKED | решение владельца 2026-08-21; `phase-6-plan.md` §13, §15 сценарий 11; уточняет D-064 |
| D-072 | **Единица работы — компонент шага Pattern.** Рабочий список берётся только из `stepDecisions[].components[]`; `alreadySatisfiedSteps`/`requiredFreshEvidence`/`missingSteps` — проекции, авторитетными не являются. Компонент `SATISFIED` внутри шага `REQUIRED_FRESH` не переисследуется вне явного пути инвалидации (§5.3 плана). Движок не переинтерпретирует и не «чинит» контракт: расхождение выведенной области с `excludedScope` — жёсткий отказ job'а | LOCKED | `phase-6-plan.md` §3.2–3.3, §5.1–5.3; поглощает предложение D-073 |
| D-074 | **Сила источника не считается повторами.** Класс источника (детерминированный) и официальность домена (подтверждённая человеком) — независимые оси. `SOCIAL` не поддерживает вывод никогда — только порождает исследование. Число повторов утверждения не входит в правило достаточности. Официальность `CLAIMED` ограничивает компонент до `PARTIALLY_SUPPORTED` | LOCKED | `phase-6-plan.md` §7.2–7.3; канон §20 |
| D-075 | **Движок не повышает память никогда.** Единственное разрешённое автоматическое изменение — понижение здоровья `OK → REVERIFY` при доказанном свежем противоречии: понижение не создаёт знание и потому не является обходом D-025. Создание записей — только `OBSERVED`; повышение и переход в `ACTIVE` остаются человеческими (D-021) | LOCKED | `phase-6-plan.md` §10.3, §12.4 |
| D-076 | **Доказательство без provenance не существует.** Evidence возможен только с `retrieved_url` + `content_hash` + `fetched_at`; сниппет поисковой выдачи доказательством не является — движок обязан открыть документ сам | LOCKED | `phase-6-plan.md` §6.4, §7.1 |
| D-077 | **Пять понятий раздельны:** SOURCE / EVIDENCE / FACT / RESEARCH MEMORY / PROOF CLAIM. Вывод Proof не становится Evidence или памятью без человека; память не доказывает свежесть. Схлопывание понятий ради удобства реализации запрещено | LOCKED | указание владельца 2026-08-21; `phase-6-plan.md` §6.1 |
| D-078 | **Отсутствие доказательства не доказывает отсутствие.** Недоказанное текущее состояние — `UNKNOWN`/`UNRESOLVED`, а не «механизм выключен». Вытеснение старого утверждения требует одновременно: несёт текущее состояние, класс источника не ниже, строго новее; иначе это противоречие, а не обновление | LOCKED | `phase-6-plan.md` §8.4; CHECK 2 `atlas-core` |
| D-079 | **Вердикт выводится из формы экономической цепочки**, а не из качества прозы: отсутствующее звено делает сильный вывод невозможным структурно (`buyback ≠ burn`, `выручка протокола ≠ выручка токена`, `полезность системы ≠ захват ценности`) | LOCKED | `phase-6-plan.md` §9 |
| D-080 | **Достаточность считается по компоненту**, количество источников в правило не входит: требуется прямое доказательство пригодного для этого компонента класса, внутри окна свежести, без неразрешённого противоречия | LOCKED | `phase-6-plan.md` §11.2 |
| D-081 | **Confidence детерминирован** — функция кода от состояний компонентов, классов источников и ограничений; модель число не называет | LOCKED | `phase-6-plan.md` §11.4 |
| D-083 | **Proof v1 = Proof Core.** Фаза 6 создаёт вердикт, confidence, слои 1–4, 6, 7 (LOCKED §7), Evidence/Sources/Gaps; слой 5 намеренно пуст. Отложено: Proof Map, layered UI, Telegram/контентное оформление, финальная редакторская система copy, визуальная подача сверх минимальной продуктовой поверхности. Назначение Proof Core — сделать Фазу 6 вручную проверяемой владельцем как настоящим пользователем | LOCKED | решение владельца 2026-08-21; `phase-6-plan.md` §12.3 |
| D-084 | **`INSUFFICIENT_EVIDENCE` — успешный исход исследования** с потреблением квоты; любой отказ до появления Proof квоту возвращает. Исчерпание бюджета не порождает уверенность | LOCKED | `phase-6-plan.md` §11.3, §13; `atlas-proof` |
| D-088 | **Владение Evidence — две координаты, обе в БД.** Канонический цикл Фазы 6 — `Research Job → Source → Evidence → сопоставление → Proof`, поэтому `evidence.proof_id` становится nullable, а `evidence.research_job_id` добавляется как `NOT NULL`: Evidence существует до Proof. Proof-заглушки ради удовлетворения FK запрещены. Валидных состояний ровно два — `JOB_ONLY` (job есть, Proof ещё/уже не будет) и `PROOF_BOUND` (Proof того же job'а); `ORPHAN` структурно невозможен. Привязка к Proof чужого job'а запрещена составным внешним ключом `(proof_id, research_job_id) → proofs(id, research_job_id)`, а не дисциплиной кода (D-010). Миграция аддитивна и обратно-безопасна: backfill из `proofs.research_job_id` полон, существующие связи Evidence↔Proof сохраняются, семантика удаления не меняется | LOCKED | решение владельца P1, 2026-08-21; `phase-6-plan.md` §6.3–6.3a |
| D-089 | **Проектные классы источника ставит человек, а не вывод.** Классы `OFFICIAL_DOCS`, `GOVERNANCE`, `OFFICIAL_REPORT` для конкретного проекта выводятся исключительно из явного поля `routeClass` в содержимом `ACTIVE` записи `project_memory_items.kind='SOURCE_ROUTE'` того же проекта и того же домена. Поисковый провайдер, содержимое загруженной страницы, извлечение моделью, формулировка URL и имя домена этих классов не дают никогда. `routeClass` применяется только там, где публичный детерминированный классификатор не опознал домен положительно — иначе `routeClass` на общей площадке или соцсети обходил бы D-074. `CONFIRMED` сам по себе сильного класса не даёт: официальный аккаунт проекта остаётся `SOCIAL` + `CONFIRMED`. `ACTIVE` SOURCE_ROUTE без `routeClass` валиден и даёт только официальность; исторические записи вида `{ domain }` совместимы без миграции. Создание и повышение SOURCE_ROUTE, как и изменение `routeClass`, остаются человеческими (D-021, D-025, D-075); неопознанный источник не повышается ради достаточности S5. **Уточняет D-074** | LOCKED | решение владельца 2026-08-21; `phase-6-plan.md` §7.2–7.2a |
| D-090 | **`maxModelCostMicro` — потолок авторизованного расхода на модельные вызовы в микро-USD**, а не абстрактная внутренняя единица (подтверждает семантику, уже заявленную в `config/product.ts`). Покрывает модельные роли движка (`QueryProposer`, `EvidenceExtractor` и будущие); денежный расход `SearchGateway` в него не входит и ограничен отдельно `maxSearchQueries`. Для каждого вызова консервативная верхняя граница выводится из утверждённого профиля модели (идентификатор, цены входа/выхода, максимумы входа/выхода, версия прайса) и резервируется атомарно **до** обращения к провайдеру; при нехватке остатка вызов не выполняется. Граница действительна только если вызов ограничен теми же числами: ограничить → оценить → зарезервировать → вызвать. Нет утверждённого профиля у production-модели — **fail closed**, типизированная ошибка конфигурации/бюджета, живого вызова нет. Резерв — авторизованный максимум, а не факт расхода (D-036): сверка с фактическим потреблением возможна только вниз и по отдельно утверждённому правилу, никогда вверх и никогда выше замороженного потолка job'а. Константы `5_000`/`15_000` не являются доказанными денежными границами и до `research_enabled = true` подлежат замене либо формальному выводу из профиля | LOCKED | решение владельца 2026-08-21; `phase-6-plan.md` §5.6 |
| D-091 | **Срез S4 Фазы 6 принят и заморожен.** Ограниченный слой поиска и извлечения (`QueryProposer`, `SearchGateway`, `ContentFetcher`, `EvidenceExtractor` и конвейер исполнения) принят на HEAD `d7e5b8a21ef150388e9cafeda2a4117ed7bb0e97` по вердикту независимого ревью `PHASE 6 S4 FINAL RE-REVIEW PASSED — READY FOR S5` (BLOCKER 0, HIGH 0, MEDIUM 0, LOW 3, NOTE 3). Заморожены: `SOURCE ≠ EVIDENCE`, provenance/traceability по фактически показанному модели тексту, лексическое содержание проекта, детерминированный авторитет источника с человеческим шлюзом `SOURCE_ROUTE` (D-074, D-089), идемпотентность персистенции Evidence и resume, атомарные потолки `maxSearchQueries`/`maxSourceOpens`/`maxModelCostMicro`, разделение потолка поиска и числа попыток, preflight резолверов до первой резервации. D-088 и дискриминатор контракта Evidence (R-2) не тронуты; замороженная семантика Фазы 5 не тронута. **Это freeze среза, а не freeze Фазы 6.** Живое модельное исполнение остаётся **fail-closed**: каталог production-профилей стоимости намеренно пуст до S10, тестовые профили — структурно отдельный путь, недостижимый из production-конфигурации. Предусловия S10 (утверждённые владельцем значения профиля, доказуемо безопасный учёт полного входа в токенах, живые smoke Anthropic и Brave, реальные ошибки/rate-limit/латентность, сверка фактического расхода только при отдельном утверждении и только вниз, `research_enabled = true`) сохраняются полностью. Три LOW и пять NOTE записаны как отложенный долг и S5 не блокируют. S5 может начинаться и S4 не переоткрывает без реального воспроизведённого регресса | LOCKED | решение владельца 2026-08-22; `phase-6-s4-freeze.md`; `phase-6-plan.md` §19 |
| D-092 | **Результат реконсиляции компонента — ровно четыре состояния:** `SUPPORTED`, `PARTIALLY_SUPPORTED`, `CONTRADICTED`, `INSUFFICIENT_EVIDENCE`. `NOT_APPLICABLE` в Pattern v1 не вводится: все десять компонентов объявлены обязательными, и «неприменимо» стало бы местом, куда прячут отсутствие доказательств; ввод возможен только когда Pattern научится объявлять компонент опциональным (Pattern v2, D-022). Различия `UNRESOLVED` / `NOT_FOUND` сохраняются как **коды причин внутри** `INSUFFICIENT_EVIDENCE`, а не как состояния: состояние отвечает на «что установлено», код причины — на «чего не хватило». `EXCLUDED` и `REUSED_FROM_MEMORY` остаются понятиями контракта и контроллера (`ContractView`) и терминальными состояниями реконсиляции не являются — до реконсиляции такие компоненты не доходят. «Доказательств нет» и «доказательства противоречат» остаются разными состояниями: они требуют разных действий контроллера и разных слов пользователю. `PARTIALLY_SUPPORTED` невозможен без одного из четырёх утверждённых оснований (`INSUFFICIENT_AUTHORITY`, `INDIRECT_ONLY`, `STATE_NOT_FULLY_LIVE`, `TOKEN_STATE_UNQUALIFIED`) и не является общим ведром для неуверенности; расширение списка — решение владельца, а не реализации. Следует из D-080 и D-084 | LOCKED | решение владельца 2026-08-22; `phase-6-s5-plan.md` §3, §8.2 |
| D-093 | **Основа текущести и вытеснения; авторитет источника относителен компоненту.** Текущее состояние доказывается `published_at` внутри окна свежести компонента либо — единственное исключение — `fetched_at` для `ONCHAIN_VERIFIABLE`, где ончейн-наблюдение фиксирует состояние в момент загрузки. Окно свежести — свойство **компонента**, а не записи: `evidence.freshness_class` S4 не заполняет, и требовать этого значило бы переоткрыть замороженный срез; значения берутся из существующего `memory_stale_after_days` (D-044). Вытеснение требует одновременно: оба элемента несут состояние ≠ `UNKNOWN` по одному компоненту, у обоих сопоставимая основа даты, новый строго новее, и новый **не менее пригоден устанавливать именно этот компонент** по требованиям активного Pattern. **Глобального рейтинга `sourceClass` не существует и вводить его запрещено:** `OFFICIAL_DOCS` превосходен для `MECHANISM_SPEC` и недостаточен для `EXECUTION_EVIDENCE`, `GOVERNANCE` устанавливает `GOVERNANCE_BASIS` и не устанавливает исполнение, `ONCHAIN_VERIFIABLE` устанавливает исполнение и текущее состояние там, где Pattern это разрешает, — и ни один из них не становится от этого универсально старшим. Ранг класса в сортировке вывода — детерминированный tie-break, а не правило авторитета. Нет сопоставимой основы даты — «новейшее побеждает» молча не применяется; отсутствие доказательства не становится доказательством отсутствия. **Уточняет D-078** | LOCKED | решение владельца 2026-08-22; `phase-6-s5-plan.md` §6.2, §8.1, §8.1a |
| D-094 | **Противоречие среди свежих доказательств: порог противоречия равен порогу установления.** Evidence может создать терминальное противоречие по компоненту, только если само пригодно установить **этот** компонент по требованиям Pattern. `SOCIAL` не побеждает сильное доказательство простым несогласием; элемент недопустимого для компонента класса или `INFERRED` не отключает документально установленный механизм — иначе враждебная страница обесценивала бы выводы. Официальность порог не двигает: `CLAIMED` не лишает элемент пригодности устанавливать компонент, она ограничивает результат до `PARTIALLY_SUPPORTED`. Условия противоречия: один и тот же компонент, оба элемента пригодны, машиночитаемые состояния несовместимы (словарь CORE CHECK 2 плюс `PAUSED`; неопознанная строка модели нормализуется в `UNKNOWN`, а не в новое состояние), ни один не вытесняет другой по D-093. Законный исторический переход (старое состояние → новое вытесняющее) противоречием не является; парафраз при одинаковом состоянии — тоже. Исход — отдельное состояние `CONTRADICTED`, а не `INSUFFICIENT_EVIDENCE` с флагом. **Победитель по уверенности не выбирается никогда**: единственный законный выход — новое доказательство. Переносит D-062 из памяти в свежие источники | LOCKED | решение владельца 2026-08-22; `phase-6-s5-plan.md` §6.1, §7 |
| D-095 | **Требования компонента — данные Pattern/CORE, а не код реконсилятора.** Матрица классов, способных установить компонент, признак «требует текущего состояния», класс свежести и признак чувствительности к состоянию токена живут в `research_patterns.content` рядом с `requiredComponents`; `ComponentReconciler` их **потребляет и никогда не изобретает**, оставаясь переиспользуемым для Pattern v2 (D-022). Расширение обязано быть аддитивным и обратно-совместимым: поведение планировщика Фазы 5 не меняется, семантика `steps` и `requiredComponents` не меняется, семантика памяти Фазы 5 не меняется, `npm run eval:memory` даёт идентичный результат, автоматической эволюции CORE не появляется — требования меняет человек (D-021, D-025, D-075). Отсутствующий ключ означает отсутствующее требование, а не значение по умолчанию из кода | LOCKED | решение владельца 2026-08-22; `phase-6-s5-plan.md` §5, §11.1 |
| D-096 | **Состояние токена: обнаружение и перенос вперёд, понижение — только по трём условиям.** `CRV ≠ veCRV`, `AAVE ≠ stkAAVE`, ликвидный токен ≠ locked/staked/wrapped/производное состояние. S5 детерминированно обнаруживает квалификаторы состояния по закрытому кодом владеемому списку с проверкой границ токенов, **только по персистированному содержимому Evidence, которое действительно поддерживает компонент**, и записывает находки в `tokenStateMentions` всегда. Запрещено: считать квалифицированное и неквалифицированное состояния эквивалентными, молча сливать состояния, молча исключать элемент за квалификацию, повышать поддержку по факту обнаружения. **Точная квалификация сама по себе слабостью не является:** «veCRV holders receive protocol fees» — сильное точное доказательство, и понижать его только за точность формулировки запрещено. Понижение до `PARTIALLY_SUPPORTED` + `TOKEN_STATE_UNQUALIFIED` ставится, только если выполнены все три условия: компонент объявлен чувствительным к состоянию токена (`RECIPIENT`, `DESTINATION`, `NET_EFFECT` либо иной, явно помеченный в Pattern), **и** устанавливающее Evidence ссылается на квалифицированное состояние, **и** требование компонента не устанавливает, что это состояние совпадает с требуемым. Требуемое состояние явно то же — понижения нет. Совпадение требует семантического вывода — S5 не выводит его ни в какую сторону, а переносит квалификацию в S6. Идентичность экономического пути и заполнение `value_source` остаются задачей S6; S4 не переоткрывается | LOCKED | решение владельца 2026-08-22; `phase-6-s5-plan.md` §9, §9.1 |
| D-097 | **Срез S5 Фазы 6 принят и заморожен.** Детерминированная реконсиляция компонента принята на HEAD `a657db39eee22353d5243ff3ea55a0a3d308be60` (ветка `claude/phase-5-research-memory`) по вердикту независимого ревью `PHASE 6 S5 FINAL ACCEPTANCE RE-REVIEW PASSED — READY FOR FREEZE` (BLOCKER 0, HIGH 0, MEDIUM 0, LOW 5, NOTE 1). Замороженная граница: provenance-несущий набор Evidence по одному компоненту → детерминированный машиночитаемый `ComponentReconciliationResult` с ровно четырьмя терминальными состояниями (`NOT_APPLICABLE` в Pattern v1 отсутствует). Заморожены: относительная компоненту допустимость Evidence, детерминированное установление, детерминированная частичная поддержка, машиночитаемое противоречие, обработка времени и текущего состояния, вытеснение, выбор `ACTIVE` привязанного к контракту Pattern, детерминированная квалификация состояния токена, верификация известного состояния, консервативное обнаружение неизвестного, наборы результата со ссылками на Evidence, детерминированная производная персистенция, безопасность повторов и конкурентности, каноническая production-проводка, восстановление проекции после падения. S5 остаётся детерминированным, model-free, network-free, ограниченным компонентом, связанным с provenance. **Дисциплина D-096 в реализации — две разные операции:** VERIFICATION проверяет орфографические варианты уже известной идентичности из `requiredTokenState` (`veCRV`/`VeCRV`/`VECRV`/`vecrv`/`ve-CRV`/`ve CRV`) без семантического вывода; DISCOVERY при отсутствии `requiredTokenState` использует консервативное структурно-лексическое обнаружение, и неоднозначные полностью строчные или полностью ПРОПИСНЫЕ слитные неизвестные идентичности остаются необнаруженными, а не угаданными. Словаря тикеров, онтологии и семантического вывода алиасов в S5 нет — это принятое поведение v1. Пять LOW и одно NOTE записаны как отложенный долг и S6 не блокируют. **После freeze S5 не переоткрывается** ради косметики, умозрительных улучшений, отложенных LOW, более широкой онтологии токенов, дополнительных алиасов или требований S6; только при воспроизведённом регрессе против замороженного инварианта, дефекте безопасности/корректности, материально влияющем на выводы S5, либо явном решении владельца, изменяющем D-092…D-096. Фаза 5 и `eval:memory` не тронуты, ядро S4 побайтово идентично замороженному runtime, `research_enabled = false`, утечки S6 нет. **Это freeze среза, а не freeze Фазы 6**; стратегия S6 разрешена | LOCKED | решение владельца 2026-08-22; `phase-6-s5-freeze.md`; `phase-6-plan.md` §19 |
| D-098 | **`value_source` — атрибут сборки S6, а не дозапись в Evidence.** S6 не изменяет замороженное Evidence Фазы S4 ради заполнения `evidence.value_source` и вообще не пишет в `evidence` ни одной колонки: `value_source` в S6 — вывод классификатора, а не наблюдение; его запись в таблицу наблюдений схлопнула бы `EVIDENCE` и `FACT` (D-077) и сделала бы неизменяемый provenance изменяемым (D-076). Значение живёт в производной проекции сборки. Существующая колонка `evidence.value_source` остаётся `NULL` и **зарезервированной** под возможный будущий extraction-time контракт — только по отдельному решению владельца, никогда как побочный эффект S6. `SOURCE ≠ EVIDENCE ≠ FACT ≠ MEMORY ≠ PROOF` сохраняется: классификация сборки не становится задним числом сырым Evidence | LOCKED | решение владельца 2026-08-22; `phase-6-s6-plan.md` §7 |
| D-099 | **Ограниченные Pattern-упорядоченные потоки с общим префиксом, не граф.** S6 представляет один или несколько потоков, упорядоченных шагами Pattern. Расхождение допускается **на любой структурно установленной позиции**, включая стадию распределения, а не только на терминальном узле: реальные механизмы расходятся раньше (`выручка → распределение → buyback / казначейство / операции`). Несколько потоков **могут разделять установленный префикс**, и общая часть выражается общим `sharedPrefixId` и общей родословной, а не разделяемым узлом произвольного графа. Расхождение только вперёд по шагам Pattern; слияний ветвей, циклов и рёбер назад нет. Ветвь существует только при собственном установленном основании; доли распределения S6 не восстанавливает и не суммирует — числовых утверждений срез не делает вовсе. Числовой лимит «до двух цепочек» (`phase-6-plan.md` §9.3) **снят**: ограничение структурное, а не счётчик. Установленный поток обязан быть **либо представлен, либо объявлен** машиночитаемым структурным ограничением (`FLOW_ENUMERATION_INCOMPLETE`) — **молчаливое усечение третьего и последующих потоков запрещено безусловно**. Нижележащий компонент привязывается к ветви только при совпадении provenance; иначе — разрыв `BRANCH_ATTRIBUTION_UNRESOLVED`, никогда привязка «ко всем ветвям» | LOCKED | решение владельца 2026-08-22; `phase-6-s6-plan.md` §13; обновляет `phase-6-plan.md` §9.3 |
| D-100 | **Существование определяет S5, классификация только помечает.** Узел, ребро и атрибут сборки появляются исключительно из статуса компонента S5 (`SUPPORTED` либо `PARTIALLY_SUPPORTED`); классификация S6 вправе лишь размечать уже допущенный элемент, закрытыми ограниченными словарями, детектором той же дисциплины, что D-096. Классификация **не создаёт существование никогда**: текст со словом «burn» не даёт звена burn, если соответствующий компонент S5 его не установил; `buyback ≠ burn`, `treasury ≠ распределение держателям`, `возврат залога ≠ награда`, ярлык механизма ≠ экономический исход. Нераспознанная классификация — `UNKNOWN` и структурный разрыв, а не семантическая догадка; это правило распространяется на `valueSource`, `direction`, `recipientKind`, ярлыки исхода и классификацию актора/действия, кроме случаев, когда значение уже доступно структурно. Инверсия существования и классификации — ядро безопасности среза | LOCKED | решение владельца 2026-08-22; `phase-6-s6-plan.md` §5.3 |
| D-101 | **Идентичность экономического потока — структурная и provenance-первичная; семантика остаётся атрибутом.** Кортеж `valueSource × direction × recipientKind × tokenState` идентичностью **не является и использоваться в этом качестве не может**: два экономически разных механизма могут совпасть по всем четырём полям, а `UNKNOWN`-значения схлопнули бы несвязанные потоки — наихудшее поведение ровно там, где данных меньше всего. `flowId` — детерминированный канонический хеш от `patternVersion`, упорядоченной по шагам Pattern родословной `(step, component, отсортированные допущенные evidenceIds)` и структурного `branchSlotPath`, выведенного из канонической сортировки слотов. Обязательные свойства: (1) разные provenance-подкреплённые ветви не сливаются при равенстве семантических атрибутов; (2) совпадение `assetSymbol` не сливает потоки; (3) различие `assetSymbol` само по себе не задаёт разное экономическое назначение; (4) `UNKNOWN`-атрибуты не схлопывают несвязанные потоки — `UNKNOWN` в вычисление идентичности не входит вовсе, а идентичность слота структурна независимо от того, известна классификация или нет; (5) повтор и смена порядка входа идентичность не меняют; (6) пересчёт из тех же замороженных входов воспроизводит те же идентификаторы. **Запрещены как входы идентичности:** порядок строк БД, произвольный рантайм-ordinal, автоинкремент, время и любое изобретённое семантическое значение. Инверсия: **provenance разделяет, классификация не объединяет никогда** — ошибка или пустота классификатора даёт лишнее разделение, а не ложное слияние. `valueSource`, `direction`, `recipientKind`, `destinationKind`, `tokenState`, `assetSymbol` и классификация механизма — **атрибуты** потока и могут быть `UNKNOWN`. `Same Asset ≠ Same Economic Flow` кодируется так: идентичность актива никогда не является единственной идентичностью потока, но исключать актив из атрибутов не требуется; при отсутствии достоверного структурного поля актива `assetSymbol` остаётся `null`, и **разбирать тикер из свободного текста ради полноты идентичности запрещено** — система обязана оставаться корректной при отсутствующей идентичности актива. **Разбиение кандидатов на слоты ветвей подчиняется тому же правилу и является его частью:** идентичность слота структурна и provenance-первична; два кандидата занимают один слот только тогда, когда детерминированная структурная родословная показывает, что это один и тот же собранный экономический элемент. Совпадение классификации, семантического кортежа, состояния токена, идентичности актива или совпадение значений `UNKNOWN` основанием для объединения слотов не является никогда — `slotKey` строится только из структурных входов `(step, component, parentBranchPath, structuralUnitKey)`, где `structuralUnitKey` — `extractionUnitKey` либо, при его отсутствии, `contentHash` с дайджестом нормализованного фрагмента. **Уточнение владельца 2026-08-22 (аудит S6, LOW-1) — какие из этих входов идентичностно-критичны.** Разбиение выполняется один раз на пару (родословная, компонент), с локальным накопителем и кандидатами, уже отфильтрованными под эту родословную, поэтому `step`, `component` и `parentBranchPath` внутри одного вызова константны и слоты не различают. **(A) Идентичностно-критичный вход ровно один — `structuralUnitKey`.** **(B) `step`, `component`, `parentBranchPath` — структурный контекст**, сохраняемый в форме ключа как защита в глубину и как читаемая привязка слота, но разделения не обеспечивающий. Реальный инвариант изоляции сильнее кортежа: кандидаты соседних ветвей физически не попадают в один накопитель. Следствия, обязательные к честной записи: мутация, удаляющая поля (B), **ненаблюдаема по построению — мутационных зубов у них нет и заявлять их запрещено**; требовать от реализации потреблять избыточный вход ради совпадения с формулой запрещено; менять рантайм ради наблюдаемости избыточного входа запрещено. Инвариант, который обязан выполняться и проверяться: идентичность структурна и provenance-первична; выходы классификатора идентичность не определяют; разные структурные родословные не сливаются при совпадении семантики; одинаковые семантические атрибуты не схлопывают разные ветви; повтор и смена порядка входа идентичность не меняют. `UNKNOWN` — значение атрибута, а не механизм идентичности: правило «каждый `UNKNOWN` получает собственный слот» удалено как семантическое правило идентичности, и родословная определяет слот независимо от того, известна классификация или нет. Несколько Evidence одного структурного кандидата дают один слот с агрегированным provenance. Не установлено детерминированно, что кандидаты принадлежат одному слоту, — они держатся раздельно либо порождают `BRANCH_ATTRIBUTION_UNRESOLVED` / `FLOW_ENUMERATION_INCOMPLETE`; догадка запрещена. Замена классификации при неизменных структурных входах обязана оставить `flowId` прежним; изменение структурной provenance ветви обязано быть способно дать другой `flowId`. Принятое ограничение v1: структурного идентификатора ветви в замороженных входах нет, поэтому два независимых документа об одной ветви дают разные потоки — направление ошибки избыточное разделение, никогда ложное слияние; устранение требует контракта извлечения, а не классификатора | LOCKED | решение владельца 2026-08-22 (переработано до блокировки под тем же номером); `phase-6-s6-plan.md` §6 |
| D-102 | **Разрывы сборки — закрытый словарь из одиннадцати типов; структурная неполнота есть успешный результат S6.** Отсутствующий `DESTINATION`, противоречивый `CURRENT_STATE`, частичный `RECIPIENT`, неразрешённое экономическое назначение потока и несовпадение состояния токена дают детерминированные разрывы и квалификации, а **не** системный сбой. Свободного текста в разрыве нет — только `kind`, `component`, `afterStep`, `provenance`; отдельного канала предупреждений сборки не вводится, иначе он стал бы местом для формулировок вне закрытого словаря. Системный сбой остаётся закрытым списком: невалидный или ненастроенный Pattern, расхождение `patternVersion`, невозможный инвариант S5, элемент сборки без provenance, висячая ссылка на Evidence, ошибка БД. Продолжает D-084  **Уточнение владельца 2026-08-22 (аудит S6, MEDIUM-2) — `TOKEN_STATE_MISMATCH` и ликвидное состояние.** Замороженный S5 (D-097) записывает в `tokenStateMentions` только обнаруженные **квалифицированные** состояния; явной машиночитаемой отметки ликвидного/незаквалифицированного токена в замороженном контракте нет, поэтому пустой список неотличим от «текст называет ликвидный токен». Нормативно для S6 v1: ликвидное состояние из отсутствия **не выводится**; `TOKEN_STATE_MISMATCH` **не ставится** лишь потому, что у одного соседнего компонента есть квалифицированное упоминание, а у другого упоминаний нет; несовпадение требует **положительно представленных несовместимых идентичностей с обеих сторон**; когда идентичность структурно недоступна — сохраняется `UNKNOWN`/неразрешённое поведение. Это **отложенное ограничение контракта**, а не дефект S6: S4/S5 не переоткрываются (D-091, D-097). Возможное будущее решение — явные упоминания ликвидного состояния на этапе извлечения или реконсиляции — принимается отдельным решением владельца после бета-доказательств потребности; семантических догадок в S6 не вводится. | LOCKED | решение владельца 2026-08-22; `phase-6-s6-plan.md` §11, §21 |
| D-103 | **У `MechanismAssemblyResult` нет верхнеуровневого статуса утверждения, вердикта или достаточности.** `SUPPORTED_MECHANISM`, `PROVEN`, `FAILED_CLAIM` и эквивалентные замаскированные вердикты S7 не вводятся. Структурная полнота принадлежит каждому представленному потоку: `COMPLETE_PATH` означает ровно «у этого потока нет структурных разрывов сборки» и **не означает** «утверждение пользователя доказано». Поля с именами `verdict`/`sufficient`/`proven`/`confidence`/`claim` в схеме среза отсутствуют, что проверяется тестом. Граница зафиксирована: S6 отвечает «какой механизм структурно восстанавливается из допущенных результатов компонентов», S7 — «достаточен ли он для утверждения пользователя»; S6 не выносит вывода вида «buyback не сокращает предложение», если это не буквальный пересказ уже установленного факта компонента. Сборка персистируется как **производная проекция** `research_mechanism_assembly` с ключом `(research_job_id, pattern_version)`; источник истины — Evidence, результаты S5 и Pattern/Boundary Contract; удаление и пересчёт из тех же замороженных входов дают семантически идентичную сборку | LOCKED | решение владельца 2026-08-22; `phase-6-s6-plan.md` §20, §22, §23 |
| D-104 | **Срез S6 Фазы 6 принят и заморожен.** Детерминированная сборка механизма (`MechanismAssembler`, `mechanism-assembly-store`, производная проекция `research_mechanism_assembly`) принята на runtime/fix HEAD `af23d8cd39eee79b26f686700a382b3b60d2582c` при финальном документационном коммите разрешений владельца `0b187dc2b13019c8b96de519f4539c3d0d52fb6f`, по вердикту независимого ревью `PHASE 6 S6 FINAL ACCEPTANCE PASSED — READY FOR FREEZE` (BLOCKER 0, HIGH 0, MEDIUM 0, LOW 2, NOTE 3). Замороженная граница: Pattern + Boundary Contract/`ContractView` + `ComponentReconciliationResult[]` от S5 + утверждённая неизменяемая проекция Evidence → детерминированный `MechanismAssemblyResult`. S6 восстанавливает, какой механизм структурно собирается из допущенных результатов компонентов, и **не определяет достаточность утверждения пользователя** — эта граница остаётся за S7. Заморожены: сборка в границах Pattern; структурно-provenance-первичная идентичность слота и потока; ветвление потоков с общим префиксом без произвольного усечения их числа; детерминированная привязка ветвей и явная `BRANCH_ATTRIBUTION_UNRESOLVED`; `Same Asset ≠ Same Economic Flow`; консервативная классификация источника ценности; перенос состояния токена из S5; `TOKEN_STATE_MISMATCH` только по двум положительно представленным несовместимым идентичностям; совместимость текущего и исторического; детерминированные разрывы закрытого словаря; `COMPLETE_PATH` только как структурная полнота потока; трассируемость до S5 и Evidence; детерминированная производная персистенция; replay и восстановление после падения; нулевое потребление исследовательского, провайдерского и модельного бюджета. S6 остаётся детерминированным, model-free, network-free, ограниченным Pattern, связанным с provenance и воспроизводимым. **Принятое поведение v1 по состоянию токена:** отсутствующее ликвидное/неквалифицированное состояние из отсутствия упоминания не выводится; явное извлечение ликвидного состояния требует отдельного решения владельца, и S5/S4 ради этого сейчас не переоткрываются. Evidence не мутируется, `evidence.value_source` не тронут (D-098), записей в память нет. Две LOW и три NOTE записаны как отложенный долг и S7 не блокируют. **После freeze S6 не переоткрывается** ради косметики, более широких семантических классификаторов, умозрительного разрешения сущностей ветвей, угадывания ликвидного состояния токена, привязки по общему префиксу ради удобства, отложенных LOW или требований S7; только при воспроизведённом материальном регрессе против замороженного инварианта, дефекте корректности или безопасности, явном решении владельца, изменяющем D-098…D-103, либо бета-данных, показывающих, что отложенное ограничение материально блокирует корректную сборку. Фаза 5 и `eval:memory` не тронуты, S4 и S5 побайтово идентичны замороженным runtime, `research_enabled = false`, утечки S7 нет. **Это freeze среза, а не freeze Фазы 6**; стратегия S7 разрешена | LOCKED | решение владельца 2026-08-22; `phase-6-s6-freeze.md`; `phase-6-plan.md` §19 |
| D-105 | **S7 v1 судит структурированный набор требований намерения.** Канонический источник: `normalized_intent` → детерминированный `IntentRequirementSet` в CORE / Pattern-управляемых данных. Притворяться, будто произвольный текст задачи уже является машиночитаемой пропозицией, запрещено. S7 v1 отвечает: «удовлетворяет ли восстановленный механизм структурным требованиям этого исследовательского намерения?» и **не** заявляет, что полностью разобрал и проверил каждую пропозицию произвольного естественного языка. Это намеренная граница возможностей беты v1. `claim_key` семантикой не наделяется (D-060, D-068) | LOCKED | решение владельца 2026-08-22; `phase-6-s7-plan.md` |
| D-106 | **Структурный потолок для проверки утверждений.** Для `CLAIM_VERIFICATION` и `CLAIM_FACT_CHECK` пропозиция остаётся свободным текстом, поэтому S7 v1 **никогда** не возвращает полную поддержку самой пропозиции: максимум — `PARTIALLY_SUPPORTED` с кодом `CLAIM_PROPOSITION_NOT_STRUCTURED`. Потолок **структурный, а не эвристический**: определять, «выглядит ли текст утверждения достаточно простым», запрещено, обходить потолок для «очевидных» утверждений запрещено. Полная проверка утверждений в будущем требует явного контракта Claim / слоя Claim Normalization под отдельным решением владельца | LOCKED | решение владельца 2026-08-22; `phase-6-s7-plan.md` |
| D-107 | **Словарь и точная семантика состояний.** Используется существующий канонический словарь вердикта (`SUPPORTED`, `PARTIALLY_SUPPORTED`, `NOT_SUPPORTED`, `INSUFFICIENT_EVIDENCE`); второй конкурирующий словарь истины не создаётся. Семантика заблокирована точно: `SUPPORTED` — все `REQUIRED`-атомы положительно удовлетворены по точным правилам когерентного потока и области; `PARTIALLY_SUPPORTED` — как минимум один **материальный** `REQUIRED`-атом положительно установлен, полная требуемая конъюнкция не установлена, **и нет положительной несовместимости, доминирующей над самим судимым атомом**; `INSUFFICIENT_EVIDENCE` — пропозиция не может быть установлена, потому что доказательства или структура отсутствуют, не разрешены, `UNKNOWN`, неполны либо неисчерпывающи; `NOT_SUPPORTED` — **только положительно установленная несовместимость** с требованием. `NOT_SUPPORTED` **никогда** не означает всего лишь отсутствующее доказательство, `UNKNOWN`, неразрешённую привязку ветви, отсутствующий `NET_EFFECT`, отсутствующий числовой факт, отсутствующий квантор, невозможность разобрать пропозицию или невозможность доказать отрицание. Нет положительной противоречащей структуры — исход по умолчанию `INSUFFICIENT_EVIDENCE` либо `PARTIAL` по закрытым правилам, никогда `NOT_SUPPORTED`. `Отсутствие доказательства ≠ доказательство отсутствия` (D-078) сохраняется | LOCKED | решение владельца 2026-08-22; `phase-6-s7-plan.md` |
| D-108 | **Требование когерентного потока.** Реляционные требования удовлетворяются **одним** когерентным потоком S6, если контракт требования явно не разрешает агрегацию. Cherry-picking запрещён: источник из потока A + получатель из потока B + исход из потока C синтетической поддержки не создают. `выручка → казначейство` вместе с отдельным `эмиссия → стейкеры` **не** поддерживает «выручка распределяется стейкерам». Когерентная родословная S6 обязательна | LOCKED | решение владельца 2026-08-22; `phase-6-s7-plan.md` |
| D-109 | **Числа, кванторы, сила и полярность вне области v1.** S7 v1 не заявляет поддержку точных числовых значений, процентов, квантифицированных утверждений, исчерпывающей семантики «вся / только / большая часть», утверждений силы (`сильный`, `материальный`, `значительный`) и произвольных отрицательных пропозиций — пока точное требование не представлено положительно будущим структурированным контрактом. Замороженные вышестоящие слои не содержат нормализованных авторитетных числовых фактов. Существование механизма не устанавливает «50 % выручки»; существование выкупа не устанавливает «18,48 % предложения»; наличие некоторого потока не устанавливает «вся выручка». **Разбор свободной прозы Evidence в авторитетные числа внутри S7 запрещён.** Эти ограничения — не системный сбой: используются канонические коды причин и структурный потолок | LOCKED | решение владельца 2026-08-22; `phase-6-s7-plan.md` |
| D-110 | **Числового confidence в S7 нет.** Косметический балл не создаётся. D-081 соблюдён: требуемое схемой Proof число вычисляется позже в Proof Core как **детерминированная чистая функция** от `ClaimSupportResult` и вышестоящего структурированного состояния; модель число не выбирает никогда. Proof Core вправе **рендерить** confidence и **не вправе** самостоятельно изменять суждение истины S7 | LOCKED | решение владельца 2026-08-22; `phase-6-s7-plan.md` |
| D-111 | **Персистенция и граница с Proof Core.** `ClaimSupportResult` персистируется как **производная проекция** `research_claim_support`; источник истины — `IntentRequirementSet` (в будущем — контракт `ClaimRequirement`) + `MechanismAssemblyResult` S6 + вышестоящий provenance. Обязательные свойства: детерминированность, воспроизводимость по повтору, идемпотентность, безопасность при конкурентности, «удаление + пересчёт даёт семантически идентичный результат», отсутствие мутации Evidence, отсутствие мутации S6, отсутствие промоушена памяти, **нулевой расход поиска, модели и провайдера**. Proof Core потребляет S7 и **не пересуживает** утверждение | LOCKED | решение владельца 2026-08-22; `phase-6-s7-plan.md` |
| D-112 | **Срез S7 Фазы 6 принят и заморожен.** Детерминированное суждение о достаточности структурированного требования намерения (`claim-evaluator.ts`, `claim-support-store.ts`, производная проекция `research_claim_support`) принято на runtime HEAD `9eccea7f35f10424a47127aaaefda47f922c6cdd`, по вердикту финального ревью владельца `ACCEPT WITH NON-BLOCKING NOTES` (BLOCKER 0, HIGH 0, MEDIUM 0, LOW 2 принятых). S7 честно реализует D-105…D-111 без переинтерпретации. В ходе closure pass исправлен реальный дефект D-106: `normalized_intent = CLAIM_FACT_CHECK` при нуле доказательств механизма отныне даёт `INSUFFICIENT_EVIDENCE`, а не безусловный `PARTIALLY_SUPPORTED` — потолок остаётся максимумом, а не полом. D-107 подтверждён корректным без изменения кода: `PARTIAL`-атом никогда не считается полным удовлетворением. Полная приёмочная матрица (57/57) и полный набор мутационных зубов (56/56, из них 45 применимых выполнено и подтверждено, 11 архитектурно неприменимы в v1 и раскрыты структурно) закрыты. Замороженная граница, семантические правила, потолки утверждения-пропозиции (D-106) и принятые LOW/NOTE зафиксированы в `phase-6-s7-freeze.md`. S4, S5 и S6 остаются побайтово идентичны своим замороженным runtime; `research_enabled = false`; утечки S8/S10/Proof Core/UI/провайдеров/Claim Normalization нет. **Любое будущее изменение замороженной семантики S7 требует нового явного решения владельца и регрессионного ревью — молчаливое изменение в последующих фазах запрещено.** Это freeze среза, а не freeze Фазы 6; стратегия S8 разрешена под отдельным решением владельца | LOCKED | решение владельца 2026-08-22; `phase-6-s7-freeze.md` |
| D-113 | **First Real Run — Stage 1: подключение worker'а к замороженному движку S4→S7.** Owner-review обнаружил разрыв: движок S4→S5→S6→S7 заморожен (D-091, D-097, D-104, D-112), но `runS4ResearchJob` не имел ни одной продакшн точки вызова — `handleResearchJobTask` завершался `errorCode=NOT_IMPLEMENTED` сразу после планирования Фазы 5. Этот срез — НЕ S8/S9/S10, отдельная, ранее не пронумерованная ступень; полная граница и приёмка зафиксированы в `pipeline-integration-stage1.md`. Реализовано: (1) `handleResearchJobTask` теперь вызывает `runS4ResearchJob` через детерминированный, нулевой стоимости, без сети исполнитель (`non-live-executor.ts`) — все четыре роли провайдера S4 и оба фикстурных `ModelCostProfile` переданы явно, `PRODUCTION_MODEL_COST_PROFILES` не тронут, живой резолвер недостижим ни при каком пути; (2) терминальный контракт разделён на `job.state` (исход исполнения), новую аддитивную колонку `research_jobs.termination_reason` (миграция `0019`, почему исполнение остановилось, переиспользует словарь `ControllerStopReason`), `job.error_code` (техническая деталь) и `research_claim_support.status` (доказательный результат) — эти четыре понятия никогда не схлопываются; сбой исполнения (`MissingActivePatternError` и подобные) становится `FAILED`/`SYSTEM_OR_PROVIDER_FAILURE` и никогда не маскируется под `INSUFFICIENT_EVIDENCE`/`NOT_SUPPORTED`; исчерпание бюджета (`BUDGET_EXHAUSTED`) становится `BUDGET_LIMIT_REACHED`, отдельно от `FAILED`, с законно возможным `INSUFFICIENT_EVIDENCE` в `research_claim_support`. S4/S5/S6/S7 исходники движка не тронуты; `research_enabled=false`; S8 (запись памяти из живых прогонов) намеренно отложен владельцем до первых живых альфа-прогонов; Proof Core/UI/billing/Claim Normalization/S9/S10 не начаты. Этот срез сам себя не замораживает — freeze остаётся отдельным решением владельца после ревью | LOCKED | решение владельца 2026-08-22; `pipeline-integration-stage1.md` |
| D-114 | **First Real Run — Stage 1 принят и заморожен.** Подключение продуктового worker'а к замороженному движку S4→S5→S6→S7 (D-113) принято на HEAD `cee7c199d44b20cbfdc95227082f9820cd2eca49`, по вердикту финального ревью владельца `ACCEPT WITH NON-BLOCKING NOTES` (BLOCKER 0, HIGH 0, MEDIUM 0, LOW 3 принятых). Заморожены: терминальный контракт (`job.state`/`job.termination_reason`/`job.error_code`/`research_claim_support.status` — четыре различных понятия, не подлежащие схлопыванию); отображения `WORK_QUEUE_EXHAUSTED → SUCCEEDED`, `BUDGET_EXHAUSTED → BUDGET_LIMIT_REACHED`, `CAPABILITY_BOUNDARY_NO_ELIGIBLE_WORK → SUCCEEDED + errorCode=CAPABILITY_BOUNDARY` (установленная информационная конвенция, не сбой — будущие потребители не вправе предполагать `error_code != null ⇒ FAILED`), исключение исполнения → `FAILED`/`SYSTEM_OR_PROVIDER_FAILURE` без сфабрикованного `research_claim_support`; инвариант «сбой исполнения никогда не маскируется под доказательный вывод S7»; строгая non-live граница (все провайдерские роли и модельные cost-профили — явные фикстуры, `PRODUCTION_MODEL_COST_PROFILES` пуст); TRACE ≠ EVIDENCE; replay/идемпотентность повторного подхвата worker'а. Приняты три LOW как не блокирующие: (1) неточный комментарий про `INTERRUPTED` — ветка структурно недостижима на пути Stage 1, автоматического re-queue нет; (2) `sweepStaleRunningJobs` выполняется только при старте worker'а, не в периодическом maintenance; (3) DEMO-резервация разрешается `RELEASED` при `SUCCEEDED` — принято, поскольку Proof Core не существует (D-084 возвращает квоту до появления Proof); когда Proof Core введёт настоящий Proof-результат, успешное исследование (включая валидный `INSUFFICIENT_EVIDENCE` Proof) обязано ПОТРЕБЛЯТЬ квоту согласно D-084 — семантика квоты Stage 1 не меняется. Принято архитектурное замечание (не блокирует): `BUDGET_EXHAUSTED` в проде достижим только для job'а, возобновлённого после сбоя (два вызова движка) — текущий одноразовый worker-подхват retry/resume-жизненного цикла не содержит; **срез, который его введёт, обязан доказать `BUDGET_EXHAUSTED` end-to-end через настоящий продакшн жизненный цикл**. S4, S5, S6 и S7 остаются побайтово идентичны своим замороженным runtime; `research_enabled = false`; `PRODUCTION_MODEL_COST_PROFILES` пуст. **Любое будущее семантическое изменение терминального контракта Stage 1 или интеграции worker'а требует нового явного решения владельца и регрессионного ревью.** Это freeze среза, а не freeze Фазы 6; Stage 2/S8/S9/S10/Proof Core/UI/Claim Normalization/billing не начаты | LOCKED | решение владельца 2026-08-22; `pipeline-integration-stage1-freeze.md` |
| D-115 | **First Real Run — Stage 2: операционный трейс + внутренний runner.** Продолжает Stage 1 (D-113/D-114) — не переоткрывает его. Цель: сделать видимым, что S4 уже делает, не меняя того, что S4 решает. Реализовано: (1) выделенная append-only таблица `research_trace_events` (миграция `0020`, аддитивная) с закрытыми словарями операций/статусов/причин/бюджетных осей — `research_attempts` НЕ расширен в JSON-журнал операций, остаётся тем же агрегатом уровня попытки; (2) детерминированный `sequence` выделяется атомарно через блокировку строки `research_jobs`, тот же приём, что `claimAttempt` уже использует; (3) `s4-executor.ts` инструментирован ТОЛЬКО наблюдательно — построчная классификация diff подтверждает: ни одно условие, порядок ветвления, порядок кандидатов, правило containment/traceability/admission, классификация источника, правило бюджета или провайдера не изменено (все 105 существующих тестов `phase6-s4-executor.test.ts` проходят без изменений); (4) отдельная non-live `trace-fixture-executor.ts` (провайдер `"non-live-fixture"`), собранная поверх реального `createS4WorkExecutor`, НЕ заменяющая принятый Stage 1 `createNonLiveS4WorkExecutor`; (5) раздельные скрипты `alpha-run`/`alpha-inspect` (исполнение и инспекция строго разделены, инспекция read-only и детерминирована); (6) `handleResearchJobTask` получил опциональный `executorOverride`, без него поведение побайтово идентично принятому Stage 1. **TRACE ≠ EVIDENCE** — жёсткий инвариант: трейс не читается component-reconciliation-store/S5/S6/S7 (подтверждено выделенным статическим regression-тестом), не несёт полей, способных притвориться Evidence, `reason_code` — закрытый словарь (сырой текст исключения, включая URL с `api_key=SECRET`, никогда не персистируется — подтверждено адверсариальным тестом). Сбой персистенции трейса (`TracePersistenceError`) не проглатывается нигде в `s4-executor.ts` и становится `state=FAILED`/`terminationReason=SYSTEM_OR_PROVIDER_FAILURE` — никогда `INSUFFICIENT_EVIDENCE`. Автоматическая очистка/TTL/retention трейса НЕ реализована — явное решение владельца, откладывающее retention-политику до отдельного среза. S4/S5/S6/S7 семантика не изменена; `research_enabled=false`; `PRODUCTION_MODEL_COST_PROFILES` пуст; S8/S9/S10/Proof Core/UI/Claim Normalization/billing не начаты. Этот срез сам себя не замораживает. **ИСТОРИЧЕСКАЯ КОРРЕКЦИЯ (D-116, зафиксирована D-117).** Утверждение выше «сырой текст исключения, включая URL с `api_key=SECRET`, никогда не персистируется» было **точным для сырого текста исключения и `reason_code`**, но **слишком широким**, если читать его как гарантию по всему трейсу: на HEAD `f6858ec` credential-подобные query-параметры **могли** персистироваться в `research_trace_events.target_ref`. Это исправлено D-116 (редактирование закрытого списка параметров в `target_ref`) и принято владельцем в составе freeze Stage 2. Решение D-115 не стирается и остаётся историческим; авторитетной коррекцией является D-116, границы редактирования — D-117 и `pipeline-integration-stage2-freeze.md` | LOCKED | решение владельца 2026-08-22; `pipeline-integration-stage2.md`; коррекция — D-116/D-117 |
| D-116 | **First Real Run — Stage 2: closure pass по независимому ревью (REJECT / DO NOT FREEZE на HEAD `f6858ec`).** Независимое ревью вернуло HIGH 1, MEDIUM 3, LOW 6 — S4/S5/S6/S7 исследовательская семантика не переоткрывалась, закрыты только дефекты пригодности/безопасности Stage 2. Закрыто: (1) **HIGH-1** — `handleResearchJobTask` совершал check-then-act захват job'а (`SELECT`, затем безусловный переход в `RUNNING`); БД-триггер трактует `OLD.state === NEW.state` как no-op, так что второй конкурентный вызов не был бы отклонён даже после первого. Заменено на атомарный `claimResearchJob` (`UPDATE ... WHERE state='QUEUED' RETURNING *`, `research-jobs.ts`) — ровно один вызывающий получает job; `handleResearchJobTask` возвращает структурированный `{claimed}` вместо `void`; `alpha-run.ts` создаёт job через новую опцию `createResearchJob(..., {skipEnqueue:true})` — никакая pg-boss задача не создаётся, реальный worker физически не может конкурировать со скриптом за тот же job. (2) **MEDIUM-1** — `research_trace_events.target_ref` персистировал URL кандидата/фетча вербатим; документация D-115 ложно утверждала обратное. `trace-store.ts`'s `boundTargetRef` теперь применяет `redactUrl()` (детерминированная строковая замена известных credential-параметров на `[REDACTED]`) к каждому персистируемому `target_ref` — не через реальный fetch-URL и не через Evidence/source provenance, только к записи трейса. (3) **MEDIUM-2** — `research_attempts.reason` (отдельно от trace) нёс сырой текст исключения провайдера через `callProvider()`'s интерполяцию `e.message`; заменено на `safeFailureReason(label, e)` — только код границы + класс исключения, никогда сообщение. (4) **MEDIUM-3** — `alpha-run.ts` создавал job без строки `interpretations`; S7 (`claim-support-store.ts`) читает `normalized_intent`/`task_type` только из `interpretations` по `researchJobId` и тихо откатывался на `UNKNOWN` — S7 никогда не оценивал реальное намерение независимо от того, что производила S4-фикстура. Исправлено: `alpha-run.ts` теперь проходит вопрос через настоящий Interpreter (`createInterpretation`) с явно зафиксированным non-live `fake` gateway (`__setInterpreterGateway`, не зависит от `MODEL_GATEWAY` окружения) и линкует `interpretations.researchJobId` — подтверждено сквозным прогоном: `normalized_intent=PROTOCOL_REVENUE_TO_TOKEN`, содержательный `research_claim_support` (не `UNKNOWN`). Приняты тривиальные LOW: исправлен неточный комментарий про усечение `target_ref` в `trace-store.ts`; удалена неиспользуемая `recordTraceEventBestEffort`. Единственный затронутый файл замороженного движка — `s4-executor.ts` (только `callProvider`, safety-only, построчно подтверждено `git diff`); `controller.ts`/`component-reconciler.ts`/`mechanism-assembler.ts`/`claim-evaluator.ts`/`claim-support-store.ts` — нулевой diff. `research_enabled=false`; `PRODUCTION_MODEL_COST_PROFILES` пуст; S8/S9/S10/Proof Core/UI/Claim Normalization/billing не начаты. Этот closure pass сам себя не замораживает — freeze Stage 2 остаётся отдельным решением владельца после повторного ревью | LOCKED | решение владельца 2026-08-23; `pipeline-integration-stage2.md` §9–10 |
| D-117 | **First Real Run — Stage 2 принят и заморожен.** Операционный трейс + внутренний runner (D-115, closure pass D-116) принят на HEAD `8206a79`, по вердикту финального ревью владельца `ACCEPT WITH NON-BLOCKING NOTES` (BLOCKER 0, HIGH 0, MEDIUM 0, LOW 4 принятых). Заморожены: атомарный захват research job (`claimResearchJob`, `UPDATE ... WHERE state='QUEUED' RETURNING *` — ровно один победитель среди конкурентных обработчиков, независимо воспроизведено 2-way и 8-way); граница исполнения `alpha-run` (`createResearchJob(..., {skipEnqueue:true})` — ровно один инициатор исполнения, ноль строк `pgboss.job`, обычный продуктовый enqueue не изменён); Interpreter-путь `alpha-run` (реальный `createInterpretation` с детерминированным non-live `fake` gateway, классифицированный `normalized_intent` вместо `UNKNOWN`, S7 оценивает реальный набор требований); безопасность ошибок провайдера (`research_attempts.reason` не несёт сырой текст исключения — только `${label}_FAILED:${errorClass}`); редактирование URL операционного трейса (`target_ref` редактирует принятый список credential-подобных query-параметров, регистронезависимо; реальный fetch и Evidence/source provenance не затронуты — граница НЕ шире этого, зафиксировано явно); **TRACE ≠ EVIDENCE** (заморожено, S5/S6/S7 не читают `research_trace_events`); read-only/детерминированное поведение `alpha-inspect`. **Историческая коррекция D-115**: исходная формулировка D-115 о недостижимости персистенции credential-URL была точна для сырого текста исключения/`reason_code`, но слишком широка для `target_ref` конкретно (исправлено D-116) — D-115 как решение не стирается, эта запись фиксирует его последующее уточнение. Приняты 4 LOW как не блокирующие: (1) документационная коррекция D-115 — закрыта этим freeze-документом; (2) покрытие редактирования URL — текущий список параметров принят как начальный, не исчерпывающий (`auth_token`/`refresh_token`/`client_secret`/`password`/`session`/userinfo/fragment остаются непокрытыми) — **обязательный пересмотр до/во время включения живых провайдеров S10**; (3) `safeFailureReason` использует `constructor.name` без явного предела длины — секрет не воспроизведён, рассмотреть ограничение при security hardening S10; (4) `__setInterpreterGateway` доступен non-live alpha tooling — ни один публичный/серверный путь его не импортирует; будущий код не должен импортировать alpha tooling в публичный/серверный runtime без явного security-ревью. S4 изменён только safety-only (`callProvider`/`safeFailureReason`, D-116); S5/S6/S7 остаются побайтово идентичны своим замороженным runtime; Stage 1 accepted runtime (`cee7c199d44b20cbfdc95227082f9820cd2eca49`) не тронут; `research_enabled = false`; `PRODUCTION_MODEL_COST_PROFILES` пуст; S8/S9/S10/Proof Core/UI/Claim Normalization/billing/FIRST REAL ATLAS RUN не начаты. **Любое будущее семантическое изменение атомарного захвата, TRACE ≠ EVIDENCE, смысла событий трейса, границы исполнения alpha-run или read-only поведения alpha-inspect требует нового явного решения владельца и регрессионного ревью — молчаливое изменение запрещено.** Это freeze среза, а не freeze Фазы 6/следующих срезов | LOCKED | решение владельца 2026-08-23; `pipeline-integration-stage2-freeze.md` |
| D-119 | **S10 — Acceptance Closure по независимому ревью (REJECT / DO NOT ACCEPT на HEAD `eac7b87`, BLOCKER 2, HIGH 1, MEDIUM 2, LOW 3).** Продолжает D-118 — не переоткрывает research-семантику S5/S6/S7, закрывает только дефекты пригодности/учёта S4/провайдерского слоя перед первым живым прогоном. Закрыто: **BLOCKER-1 (capability-fatal канал исполнения)** — до этого закрытия полный отказ живой способности (SearchGateway/count_tokens/generation недоступны после допустимого retry, устойчивый rate-limit) деградировал в обычный `WorkExecutionResult.FAILED`, доходил до `WORK_QUEUE_EXHAUSTED → SUCCEEDED`, и S7 писал `INSUFFICIENT_EVIDENCE` — нарушение замороженного терминального контракта (D-114: «CAPABILITY UNAVAILABLE никогда не должно становиться EVIDENTIARY INSUFFICIENCY»). Введён новый типизированный `CapabilityFatalError` (`capability-fatal-error.ts`), БРОСАЕМЫЙ (не возвращаемый как обычный результат) из `s4-executor.ts` при доказанной недоступности способности — распространяется структурно через `controller.ts`/`run-job.ts` (у обоих нет промежуточного try/catch, подтверждено осмотром) до существующей границы перехвата Stage 1 в `worker.ts`, без единого изменения `controller.ts`/`run-job.ts`/S5/S6/S7: `job.state=FAILED`, `terminationReason=SYSTEM_OR_PROVIDER_FAILURE`, `errorCode=CapabilityFatalError`, ни одной строки `research_claim_support`. Локальные/продолжаемые сбои (нулевые кандидаты, 404/403/429/timeout одного источника, одна неудачная экстракция, один источник, превышающий одобренный input-потолок модели) остаются локальными — `ModelInputOversizedError` НИКОГДА не капабилити-фатален (явное решение владельца: повтор не изменит размер источника) и никогда не создаёт `CONTRADICTED`/`NOT_SUPPORTED`/finding отсутствия — подтверждено: партия, где ВСЕ документы превышают потолок, теперь резолвится в `SKIPPED` (`s4-executor.ts`: счётчик `nonOversizedExtractionFailures` отделяет «всё превышено» от «экстрактор действительно недоступен», ранее конфлировавшихся в единое `FAILED/EVIDENCE_EXTRACTOR_UNAVAILABLE`). **BLOCKER-2 (retry резервирует на каждую внешнюю попытку)** — до этого закрытия `retryOnceIfTransient` жил внутри провайдеров (`search-gateway-brave.ts`/`query-proposer-anthropic.ts`/`evidence-extractor-anthropic.ts`), так что одна S4-резервация могла авторизовать два реальных внешних вызова. Retry перенесён из провайдеров (теперь каждый делает РОВНО одну внешнюю попытку) в новую единую функцию `reserveAndCallWithRetry` (`s4-executor.ts`) — резервирует бюджет (`searchQueries`/`modelCostMicro`) ПЕРЕД каждой попыткой, включая повтор; если резервация повтора отклонена бюджетом — повтора не происходит (классифицируется `local`, не `fatal`, поскольку способность не была доказано недоступна); если транзиентный сбой переживает и вторую попытку — способность доказанно недоступна (`fatal`, `CapabilityFatalError`). `count_tokens` остаётся вне оси `modelCostMicro` (небиллируемо, явно задокументировано) и сохраняет собственный внутренний retry в `token-gate.ts` — недоступность после этого retry немедленно классифицируется `fatal`. Anthropic SDK клиент остаётся `maxRetries: 0` (подтверждено статическим тестом в обоих провайдерских файлах) — никакого скрытого SDK-retry. **HIGH-1 (посчитанный запрос должен точно совпадать с generation-запросом)** — `count_tokens` ранее получал только `model`/`system`/`messages`, тогда как generation дополнительно передавал `output_config`; `countThenGate` (`token-gate.ts`) теперь принимает обязательный параметр `outputConfig`, и оба провайдерских файла строят ОДИН общий `baseRequest = {model, system, messages, output_config}`, используемый структурно идентично для `countThenGate` И `messages.create` — расхождение форм запроса больше не может возникнуть случайно. **MEDIUM-1 (одна audit-запись стоимости на реальную попытку модели)** — ранее полное usage копировалось на каждую строку `QUERY_PROPOSED` (на предложенный запрос) и `EXTRACT_OK` (на допущенный факт), так что `SUM(actual_cost_micro)` завышал реальную стоимость провайдера кратно числу результатов одного вызова. Введён новый закрытый `trace_operation_type` `MODEL_CALL_ATTEMPTED` (миграция `0022`) — РОВНО одна audit-строка на реальный внешний вызов генерации (успех или неудача), несёт usage/стоимость; `QUERY_PROPOSED`/`EXTRACT_OK` больше не несут дублированных полей usage. `alpha-run.ts`/`alpha-inspect.ts` теперь суммируют `actual_cost_micro` ТОЛЬКО по строкам `operation_type = 'MODEL_CALL_ATTEMPTED'` (было — по всем строкам трейса без фильтра) и без сужающего `::int` (SUM остаётся numeric-safe). **MEDIUM-2 (billing-предположение об отсутствии prompt caching)** — S10 `INTERNAL_ALPHA_V1` намеренно не использует prompt caching; новое поле `ModelUsage.unsupportedBillingUsage` (`providers/types.ts`), вычисляемое из `cache_creation_input_tokens`/`cache_read_input_tokens` ответа Anthropic — если ненулевое, `s4-executor.ts` НЕ вычисляет `actualCostMicro` из одних `input_tokens`/`output_tokens` (оставляет `null`, `reasonCode=UNSUPPORTED_BILLING_USAGE`, новое значение закрытого `trace_reason_code`), вместо молчаливого занижения реальной стоимости. Приняты тривиальные §7 LOW: (a) malformed JSON от Brave (HTTP 200, невалидное тело) переклассифицирован как нетранзиентный — детерминированный сбой парсинга, повтор идентичного запроса воспроизвёл бы тот же результат (`search-gateway-brave.ts`); (b)/(c) устранение `::int` и фильтр по `MODEL_CALL_ATTEMPTED` в `alpha-run.ts`/`alpha-inspect.ts` (см. MEDIUM-1 выше). НЕ реализовано и явно раскрыто: операционная видимость номера попытки `count_tokens` (1 vs retry 2) — не добавлена отдельная trace-запись, полагаясь на факт, что `count_tokens` retry-неудача уже становится `fatal` и видна через `CapabilityFatalError`/`MODEL_CALL_ATTEMPTED` границу. Новый closure regression-файл `tests/s10-acceptance-closure.test.ts` (19 тестов, полное покрытие BLOCKER-1 A–F + E2E, BLOCKER-2 1–5, HIGH-1, MEDIUM-1, MEDIUM-2). S4/S5/S6/S7 исследовательская семантика не переоткрывалась — единственный затронутый файл замороженного движка остаётся `s4-executor.ts` (execution/accounting-only, `controller.ts`/`component-reconciler.ts`/`mechanism-assembler.ts`/`claim-evaluator.ts`/`claim-support-store.ts` — нулевой diff); `research_enabled=false`; `internal_alpha_enabled` остаётся default `false`; живого трафика Brave/Anthropic в рамках этого closure НЕ было. Этот срез сам себя не замораживает — freeze S10 остаётся отдельным решением владельца после повторного независимого ревью | LOCKED | решение владельца 2026-08-23; `s10-live-provider-enablement.md`; коррекция — этот closure pass |
| D-118 | **S10 — Live Provider Enablement + Internal Alpha Gate.** Продолжает Stage 2 (D-115/D-116/D-117) — не переоткрывает его. Финальный технический мост перед FIRST REAL ATLAS RUN; сам первый живой прогон этим решением НЕ выполнен. Подключены 4 живых провайдера (Brave SearchGateway, native ContentFetcher, Anthropic QueryProposer, Anthropic EvidenceExtractor, оба `claude-haiku-4-5`, цены $1/$5 за 1M токенов вход/выход, `priceVersion="anthropic-2026-08"`). **Owner strategy correction реализована**: `model-cost-profile.ts`'s каталог теперь role-квалифицирован (`${role}:${modelId}` вместо голого `modelId`) — QUERY_PROPOSER (maxInputTokens=4000, maxOutputTokens=512) и EVIDENCE_EXTRACTOR (maxInputTokens=48000, maxOutputTokens=1536) больше не могут перепутать профили для одной `claude-haiku-4-5`; отсутствие точной (role, model) пары остаётся fail-closed. **D-090 закрыт**: COUNT-THEN-GATE (`providers/token-gate.ts`) — точный `count_tokens` ПЕРЕД каждым generation-вызовом обеих ролей, `ModelInputOversizedError`/`TokenCountUnavailableError` вместо усечения/оценки; классифицировано в новые закрытые `trace_reason_code` (`MODEL_INPUT_OVERSIZED`/`TOKEN_COUNT_UNAVAILABLE`) и новый `trace_operation_type` (`MODEL_CALL_SKIPPED` для QueryProposer, у которого не было pre-call trace-события). `INTERNAL_ALPHA_V1` — один неизменный envelope (12/24/2 000 000/900/1), код-константа, не product_config-строка. Реальное usage/стоимость модели — 3 новых audit-only nullable колонки на `research_trace_events` (миграция `0021`), заполняются ТЕМ ЖЕ утверждённым профилем что резервацию, НИКОГДА не декрементируют `research_jobs.*Reserved` (владелец: явно не вторая бюджетная инстанция). Retry policy (`providers/retry.ts`, `retryOnceIfTransient`) — 1 retry/2 попытки максимум, только для транзиентных ошибок (429/5xx/сеть), для SEARCH/MODEL/count_tokens; FETCH остаётся 0 retries, ContentFetcher не тронут. `internal_alpha_enabled` — новый DB-гейт (product_config, default false, отдельный от `research_enabled`, который НЕ тронут, остаётся `false`); `createLiveS4WorkExecutor` (`live-executor.ts`) — единственная функция, способная вернуть live-подключённый `WorkExecutor`, требует ОБОИХ `internal_alpha_enabled=true` И явного `alpha-run --mode=live` (нет default/fallback между `fixture`/`live` ни в одну сторону); недостающий гейт/credential/cost-профиль — fail closed ДО создания job, с полным списком проблем разом. Internal Alpha Data Boundary: `INTERNAL_ALPHA_LIVE_PROJECT_SLUGS` — явный allowlist (`pump_fun`, §18 owner-approved первая живая цель), live-режим отклоняет произвольный project slug ДО создания job/interpretation/резервации. Pre-live URL hardening (Stage 2 LOW-2 закрыт, теперь обязательный): `redactUrl` расширен (`auth_token`/`refresh_token`/`client_secret`/`password`/`session`) + URL userinfo + credential-подобные фрагменты — остаётся ТОЛЬКО санитизацией трейса, реальный fetch URL и Evidence/source provenance не затронуты. `safeFailureReason` (Stage 2 LOW-3 закрыт) ограничена 64 символами. Interpreter tooling boundary подтверждён статическим тестом: `__setInterpreterGateway`/`alpha-run` недостижимы из `app/` (публичные роуты), `src/server/services/`, и `worker.ts`. S4/S5/S6/S7 исследовательская семантика не переоткрывалась — единственные safety-only изменения в S4: `callProvider`'s role-параметр и `safeFailureReason`'s длина, count-then-gate/retry в провайдерских реализациях (не в S4 самом). `research_enabled=false`; S8/S9/Proof Core/UI/Claim Normalization/billing/FIRST REAL ATLAS RUN не начаты. Этот срез сам себя не замораживает | LOCKED | решение владельца 2026-08-23; `s10-live-provider-enablement.md` |

---

## Закрытые дефекты Фазы 4

Живая ручная приёмка пройдена 2026-08-21 против реального Anthropic
gateway. Эти решения **закрыты и не пересматриваются**.

| ID | Вопрос | Варианты и рекомендация | Где описано |
|---|---|---|---|
| D-037 | Глубина уточнения Interpreter | **ПРИМЕНЕНО — вариант 2** (выбор владельца, 2026-08-21): «зачем нужен токен» и «доходит ли доход до держателя» считаются одной задачей Token Value Capture, ответ сразу, без уточнения | `phase-4-freeze.md` §5; `src/server/interpreter/prompt.ts` (правило 4) |
| D-038 | Пропуск `understood_summary` на маршруте уточнения | **ПРИМЕНЕНО.** Один повтор с замечанием модели, деградация — только после него (было: деградация сразу, экран без строки «Понял») | наблюдение живого прогона; `src/server/interpreter/interpret.ts` (`callModel`) |
| D-039 | Грамотность ответов модели («заработивает») | **ПРИМЕНЕНО.** Строка в промпте про грамотность языка (правило 9). Модель не менялась — `interpreter_model` остаётся `claude-haiku-4-5` (D-026) | наблюдение живого прогона; `src/server/interpreter/prompt.ts` |

---

## Открытые решения

Ни одно из них не блокирует разработку Фазы 5.

| ID | Вопрос | Варианты и рекомендация | Где описано |
|---|---|---|---|
| D-045 | Финальный copy пользовательских текстов | Тексты состояний, уточнений, границ области. Утверждает владелец после просмотра реальных экранов | `phase-3-plan.md` §6, `phase-4-freeze.md` |

---

## Как пользоваться

1. **Начало любой задачи** — прочитать этот файл. Он ~10 000 символов
   против ~584 000 во всём `docs/`.
2. **Решение со статусом `LOCKED`** не обсуждается. Требуется изменить —
   `STRATEGY REVIEW REQUIRED` и остановка.
3. **Решение со статусом `OPEN`** блокирует зависящую работу. Не
   выбирать за владельца.
4. **Новое решение владельца** — строка сюда в том же коммите. Решение,
   оставшееся только в переписке, будет переспрошено.
