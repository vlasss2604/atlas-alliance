# ATLAS PROOF — Реестр решений

> **Это первый файл, который читают в любом новом чате.**
> Он существует, чтобы уже принятые решения не обсуждались заново.
>
> Статусы:
> `LOCKED` — не пересматривать. Если работа требует изменить решение —
> вывести `STRATEGY REVIEW REQUIRED` и остановиться.
> `OPEN` — ждёт решения владельца. Реализацию, зависящую от него, не начинать.
> `SUPERSEDED` — заменено; указано, чем именно.
>
> Реестр **append-only**: строки не удаляются, статус меняется.
> Новое решение владельца записывается сюда в том же коммите, что и работа.

---

## Продукт и доступ

| ID | Решение | Статус | Где обосновано |
|---|---|---|---|
| D-001 | Приоритет источников: `01_LOCKED_DECISIONS.md` выше canonical. Legacy-фичи (Links, Deep Check, TON/GRAM, тарифы FREE/START/PLUS/PRO, 4 стадии прогресса) в код не попадают | LOCKED | `atlas-audit.md`, решение A2 |
| D-002 | Стек: Next.js 16 App Router + отдельный Node-worker, PostgreSQL 16 + Drizzle, pg-boss, SSE + polling fallback, Tailwind 4, vitest + реальный Postgres, Playwright | LOCKED | `atlas-audit.md`, решение A1 |
| D-003 | DEMO: 3 Proof пожизненно, потолок способности `TARGETED_REFRESH`, ограниченный список проектов. DEMO — настоящий ATLAS в миниатюре, ограничен охватом и бюджетом, но не качеством | LOCKED | `01_LOCKED_DECISIONS.md` §2 |
| D-004 | ARI • CORE: 2999 Stars в месяц, `FRESH_RESEARCH`, динамический список `ACTIVE_CORE`-проектов, а не фиксированный каталог | LOCKED | `01_LOCKED_DECISIONS.md` §3 |
| D-005 | Proof приватен по умолчанию. Публичных Proof-ссылок в v1 нет; значения `PUBLIC` не существует в схеме | LOCKED | `01_LOCKED_DECISIONS.md` §7 |
| D-006 | Scope и Entitlement — разные проверки. Проект может быть внутри области ATLAS и при этом недоступен DEMO | LOCKED | `atlas-intent`, `gates.ts` |
| D-007 | Подписка `CANCEL_AT_PERIOD_END` даёт доступ до `valid_until` (вариант A) | LOCKED | `phase-1-plan.md` §5 |
| D-008 | Квота DEMO — reservation model: допуск считает `RESERVED + CONSUMED`, пользователю показывается только `CONSUMED`. Неудачный гейт квоту не тратит | LOCKED | `phase-1-plan.md` |
| D-009 | Максимум 2 уточнения, затем flow закрывается фиксированным сообщением. Лимит держит CHECK в БД | LOCKED | `01_LOCKED_DECISIONS.md` §5 |

## Архитектура и инварианты

| ID | Решение | Статус | Где обосновано |
|---|---|---|---|
| D-010 | Инварианты живут в БД, а не в дисциплине кода: партиальные уникальные индексы (один активный job, dedupe активных задач, одна entitling-подписка, одна ACTIVE-версия Pattern), CHECK-констрейнты, триггеры графа переходов, append-only журнал | LOCKED | `phase-1-plan.md` §4 |
| D-011 | PostgreSQL — источник истины. SSE — только транспорт уведомлений. Три слоя сходимости: снимок при подключении, NOTIFY, периодическая сверка | LOCKED | `phase-3-plan.md` |
| D-012 | ResearchJob — персистентная сущность, а не блокирующий вызов. Если UI обещает «вернитесь позже», сервер обязан это выполнить | LOCKED | `02_ARCHITECTURE_AND_PHASES` |
| D-013 | Пользовательский ввод — всегда данные, никогда инструкции. Границы блока данных экранируются | LOCKED | `phase-4-plan.md` §5 |
| D-014 | Модель предполагает — каталог решает. Резолюция проекта детерминирована сервером; LLM не может «заявить» проект в scope | LOCKED | `phase-4-plan.md` §2.6 |
| D-015 | Entitlement Gate проверяет **все** названные сущности задачи. Половина сравнения не выполняется | LOCKED | `atlas-intent`, adversarial review Фазы 4, HIGH-1 |
| D-016 | Два слоя понимания: `understood_summary` (язык пользователя, показывается) и `research_task` (английский, вход движка, наружу не идёт) | LOCKED | `phase-4-freeze.md` §1 |
| D-017 | Исследование стартует только при `status=READY` **и** `route=DEEP_RESEARCH` | LOCKED | `phase-4-plan.md` §3.4 |
| D-018 | Аутентификация: HMAC-проверка initData на сервере, сессия в HttpOnly-cookie, CSRF-токен, allowlist только своих origin, bootstrap-исключение для `POST /api/auth/telegram` | LOCKED | `phase-2-plan.md` |
| D-019 | Rate limit хранится в таблице `auth_rate_limits`; бакет по Telegram-id заводится только после успешной проверки подписи (иначе — вектор блокировки чужого аккаунта) | LOCKED | `phase-2-plan.md` §2.1 |
| D-046 | `quick_answer` на маршруте `OUTSIDE_CURRENT_DOMAIN` нормализуется в `null`, а не роняет контракт — поле декоративное там (продукт показывает свой фиксированный текст, `toView()` его всё равно отбрасывает). На `DEEP_RESEARCH` строгость сохранена намеренно (LOCKED D-027: никакого AI-текста рядом с Proof). **Переподтверждено живой ручной приёмкой Фазы 4, 2026-08-21** | LOCKED | регрессия «Стоит ли покупать SUI сейчас?» → 502; `src/server/interpreter/schema.ts` (`normalizeModelOutput`); `phase-4-freeze.md` §6 |
| D-047 | `route=CLARIFICATION_REQUIRED` механически форсирует `status=NEEDS_CLARIFICATION`, тем же способом, что уже применён к маршрутам-объяснениям (route → status — наша проекция, не суждение модели). До этого модель, вернувшая верный маршрут при `status=READY`, получала жёсткий отказ схемы ("READY with CLARIFICATION_REQUIRED") вместо честного уточняющего вопроса. **Переподтверждено живой ручной приёмкой Фазы 4, 2026-08-21** | LOCKED | регрессия (лог: `[interpreter] unavailable READY with CLARIFICATION_REQUIRED`, 502) на «доход протокола реально доходит до держателей токена?»; `src/server/interpreter/schema.ts` (`normalizeModelOutput`); `phase-4-freeze.md` §6 |

## ARI, память и обучение

| ID | Решение | Статус | Где обосновано |
|---|---|---|---|
| D-020 | Граница обучения §11 сохраняется. Автоматическое извлечение уроков даёт только **кандидатов** | LOCKED | `ARI_LEARNING_LOOP.md` |
| D-021 | Переход `CANDIDATE → ACTIVE` выполняет человек. Автоматического перевода нет | LOCKED | `ARI_LEARNING_LOOP.md` §11 |
| D-022 | CORE управляется человеком. Новая версия CORE — только после слепой/adversarial регрессии | LOCKED | `ARI_LEARNING_LOOP.md` |
| D-023 | Извлечение кандидатов **не** ограничено `VERIFIED`; ограничен только промоушен. Пороги извлечения и промоушена — разные | LOCKED | решение владельца по Learning Loop |
| D-024 | Пользовательская активность даёт сигналы, а не истину. Спрос не активирует тему | LOCKED | `atlas-memory` |
| D-025 | Lifecycle памяти не обходится никогда, даже в тестах — единственный барьер против отравления | LOCKED | `02_ARCHITECTURE_AND_PHASES`, security-мандат Фазы 5 |
| D-026 | Модель Interpreter — `claude-haiku-4-5`; меняется ключом `interpreter_model` без деплоя | LOCKED | `phase-4-freeze.md` §2 |
| D-027 | `QUICK_EXPLANATION` / `NO_RESEARCH_NEEDED` показывают короткий ответ ARI (вариант A) — без Proof, без Research Memory, с лимитом длины | LOCKED | `phase-4-freeze.md` §2 |
| D-028 | `interpreter_enabled = true` с Фазы 4; `research_enabled = false` до Фазы 6 | LOCKED | `phase-4-freeze.md` §2 |

## Процесс разработки

| ID | Решение | Статус | Где обосновано |
|---|---|---|---|
| D-029 | Цикл фазы: план → решение владельца → реализация → тесты → adversarial review со свежим контекстом → разбор находок → отчёт | LOCKED | `DEVELOPMENT_WORKFLOW.md` |
| D-030 | Роли — закрытый список: Main Claude, Adversarial Reviewer, Blind Evaluator (с Фаз 5–6). Новая роль только после измеренной проблемы | LOCKED | `DEVELOPMENT_WORKFLOW.md` |
| D-031 | Параллельная реализация несколькими агентами не используется | LOCKED | `DEVELOPMENT_WORKFLOW.md` |
| D-032 | Распределение моделей: Opus — архитектура, security, планы фаз, adversarial review; Sonnet — реализация, тесты, миграции, UI; Haiku — механическая генерация и runtime Interpreter | LOCKED | `DEVELOPMENT_WORKFLOW_OPTIMIZATION.md` §2–3 |
| D-033 | Чаты разделены по стадиям фазы. Право записи в код — только у чата реализации | LOCKED | `DEVELOPMENT_WORKFLOW_OPTIMIZATION.md` §5 |
| D-034 | Иерархия контекста: `DECISIONS` → LOCKED → план фазы → нужный skill. canonical поднимается точечно | LOCKED | `DEVELOPMENT_WORKFLOW_OPTIMIZATION.md` §4 |
| D-035 | Владелец продукта — отдельный обязательный слой решений между планированием и реализацией. Opus предъявляет варианты, владелец выбирает, Sonnet исполняет | LOCKED | `DEVELOPMENT_WORKFLOW_OPTIMIZATION.md` §7.1 |
| D-036 | Никогда не имитируется то, чего не произошло: ни прогресс исследования, ни пройденный тест, ни выполненная проверка | LOCKED | `PRODUCT_QUALITY_DIRECTIVE.md` |

---

## Фаза 5 — Research Memory (Strategy Lock)

| ID | Решение | Статус | Где обосновано |
|---|---|---|---|
| D-040 | Retrieval v1 — **structured-first** по онтологии (проект, тема, интент, шаг Pattern, claim-ключи) + FTS/`pg_trgm`. pgvector и эмбеддинги не вводятся. `MemoryRetrievalGateway` сохраняется как шов для будущего гибрида, если оценка покажет пропуски | LOCKED | `phase-5-plan.md` §3.4 |
| D-041 | `proofs.verification_status` (`DRAFT`/`REVIEWED`/`VERIFIED`) добавляется. `VERIFIED` гейтит **промоушен в ACTIVE-память**, а не появление кандидата. Промоушен выполняет человек | LOCKED | `phase-5-plan.md` §5.2 |
| D-042 | Кросс-проектное обобщение в v1 **запрещено**: память проекта A не закрывает шаг проекта B. Путь к переиспользуемым паттернам механизмов сохранён на будущее, но открывается отдельным решением | LOCKED | `phase-5-plan.md` §8 |
| D-043 | Ключ конфига `memory_enabled` — оценка Memory OFF vs ON штатный режим, а не хак в тестах | LOCKED | `phase-5-plan.md` §7 |
| D-044 | Словарь свежести нормализуется: `LOW_CHANGE` / `MEDIUM_CHANGE` / `HIGH_CHANGE`; `evidence` приводится одной миграцией, пока таблица пуста | LOCKED | `phase-5-plan.md` §5.3 |
| D-056 | Память обязана **менять Research Boundary Contract** (`already_satisfied_steps`, `reusable_evidence`, `required_fresh_evidence`, `missing_steps`, `known_information`, бюджет, `novelty_state`), а не подмешивать прошлый текст в промпт. Проверяемый вопрос: «исследовал бы Atlas иначе, потому что вспомнил?» | LOCKED | `phase-5-plan.md` §4.2 |
| D-057 | Режимы `MEMORY` / `TARGETED_REFRESH` / `FRESH_RESEARCH`. Переиспользование **никогда** не отменяет требование свежести | LOCKED | `phase-5-plan.md` §4.4 |
| D-058 | **Вывод режима исследования.** `missing > 0` → `FRESH_RESEARCH`; `missing = 0` и `requiredFresh > 0` → `TARGETED_REFRESH`; все шаги закрыты → `MEMORY`. `FRESH_RESEARCH` **не** означает «выбросить память и переделать все 8 шагов»: `already_satisfied_steps` продолжают ограничивать Фазу 6. Режим описывает **тип** новой работы, контракт — её **объём**. Зажатие потолком `capability_at_start` сохраняется, и при зажатии `excludedScope`/`stopConditions` заполняются ВСЕГДА, независимо от числа закрытых шагов | LOCKED | `phase-5-plan.md` §4.4; ревью HIGH-1 |
| D-059 | **Поведение health.** `OK` — может закрывать шаг. `QUESTIONABLE`/`REVERIFY`/`STALE` — извлекаются и направляют перепроверку, но шаг не закрывают (→ `REQUIRED_FRESH`). `DEPRECATED` — не извлекается вовсе: знание признано неверным, а не устаревшим (→ шаг становится `MISSING`). Принцип: закрывать шаг может только здоровая переиспользуемая память | LOCKED | `phase-5-plan.md` §2.3a; ревью HIGH-2 |
| D-060 | **Контракт покрытия шага Pattern v1.** Вводится «компонент шага»; список фиксирован внутри `research_patterns.content` (то есть внутри CORE, D-022): 1 `SOURCE_OF_VALUE`; 2 `FLOW_PATH`; 3 `MECHANISM_SPEC`+`GOVERNANCE_BASIS`; 4 `EXECUTION_EVIDENCE`; 5 `CURRENT_STATE`; 6 `DESTINATION`+`RECIPIENT`; 7 `NET_EFFECT`; 8 `DURABILITY_BASIS`. `research_memory.component` валидируется против активного Pattern — произвольная пара (claim_key, pattern_step) больше не может закрыть чужой шаг. Шаг `ALREADY_SATISFIED` только когда **каждый** его компонент покрыт пригодной записью; частичное покрытие → `REQUIRED_FRESH` с перечислением непокрытых компонентов. Универсальная онтология claim-ключей не вводится | LOCKED | `phase-5-plan.md` §4.1a; ревью MEDIUM-4 |
| D-061 | **Определение false reuse.** Шаг помечен `ALREADY_SATISFIED`, и хотя бы один элемент памяти, обеспечивший это решение, контрольный набор объявил непригодным. Причины непригодности: `STALE`, `LOW_CONFIDENCE`, `WRONG_PROJECT`, `UNHEALTHY`, `UNPROMOTED`, `WRONG_STEP_MAPPING`, `CONTRADICTED`. Считается по шагам: числитель — шаги, закрытые с участием непригодного элемента; знаменатель — все шаги `ALREADY_SATISFIED` набора. `false_reuse_rate = 0` остаётся условием приёмки. Заменяет прежнее определение (только «непромоутнутое»), которое было структурно недостижимо и потому всегда давало ноль | LOCKED | `phase-5-plan.md` §7.3; ревью MEDIUM-2; уточняет D-049 |
| D-062 | **Правило противоречия.** Две записи `ACTIVE`, прошедшие фильтры health и свежести, относящиеся к одному `(pattern_step, component)` и несущие **различный `mechanism_state`**, образуют конфликт: шаг не может быть `ALREADY_SATISFIED`, он получает `REQUIRED_FRESH` с причиной `CONTRADICTED` и перечислением конфликтующих `memoryId`. Обнаруживается только конфликт состояний; расхождение свободного текста `statement` конфликтом не считается (парафраз), семантическое обнаружение противоречий — задача Critic-слоя, не Фазы 5 | LOCKED | `phase-5-plan.md` §4.3b; ревью LOW-2 |
| D-063 | **Критерий Blind Evaluator.** Оценщик судит качество плана без утечки происхождения: из вида удаляются `reason`, `memoryIds`, `memory_used` и `knownInformation` (последнее содержит тексты памяти). Он **не обязан** не знать, была ли включена память — при `memory_enabled=false` все 8 шагов всегда `MISSING`, поэтому OFF-контракт опознаётся структурно, и прежняя формулировка обещала недостижимое. Слепая оценка применяется к сравнению ON-планов между собой; пара OFF/ON остаётся количественным сравнением метрик | LOCKED | `phase-5-plan.md` §7.5; ревью MEDIUM-3; уточняет D-049 |
| D-064 | **Предусловие пригодности DEMO.** По D-058 первый вопрос о проекте без памяти даёт 8 `MISSING` → `FRESH_RESEARCH` → потолок DEMO (`TARGETED_REFRESH`) → честная граница способности вместо запуска. Это ровно поведение LOCKED §2 («требуется полноценное исследование с нуля → Guard блокирует»), впервые ставшее исполнимым. Следствие: DEMO станет пригоден только после того, как владелец исследует три `demo_project_slugs` и промоутнет память до `ACTIVE` с полным покрытием восьми шагов. Сид создавать verified-знание не может и не должен. Не задача кода Фазы 5; блокирует полезность DEMO в Фазе 6 | LOCKED | `phase-5-plan.md` §4.4a; `01_LOCKED_DECISIONS.md` §2 |
| D-065 | **Барьер промоушена — в БД, а не только в приложении.** Триггер lifecycle обязан проверять, что `promoted_by` принадлежит пользователю с ролью `ADMIN`, а не только что поле непустое. Ревью доказало прямым SQL: обычный пользователь записывается как `promoted_by`, и переход в `ACTIVE` проходит. D-010 требует инвариантов в БД, D-025 называет lifecycle единственным барьером против отравления — проверка только в коде приложения этим требованиям не удовлетворяет | LOCKED | ревью MEDIUM-5 |
| D-066 | **Метрики считаются там, где они определены.** `recall`/`precision` усредняются только по сценариям с непустым ожидаемым множеством; сценарии с пустым ожиданием (нет памяти, просрочено, отравление, чужой проект, граница доступа) уходят в отдельную метрику отсутствия ложных срабатываний. Прежняя схема присваивала им `1`/`1` по определению и завышала агрегат | LOCKED | ревью MEDIUM-6 |
| D-048 | FACT / RESEARCH / PROOF memory разделены. Системная Research Memory **не зависит от хранения пользовательских данных**: provenance копируется при промоушене, ссылок с `RESTRICT` на пользовательский `evidence` нет — иначе удаление аккаунта блокируется | LOCKED | `phase-5-plan.md` §5.1 |
| D-049 | Метрики Фазы 5: recall, precision, noise rate, **false reuse rate**, снятые шаги, обновлённые шаги, дельта запланированных поисков, качество плана. `false_reuse_rate = 0` — условие приёмки. Blind Evaluator судит контракты, не зная, была ли память включена | LOCKED | `phase-5-plan.md` §7 |
| D-050 | Research Boundary Contract строится на 8 шагах Pattern v1 и явно фиксирует: что известно, что переиспользуется, что обновляется, чего не хватает, что вне области | LOCKED | `phase-5-plan.md` §4.1–4.2 |
| D-051 | **Стандарт приёмки слоя интеллекта**: концепция → архитектура → реализация → автотесты → живая ручная приёмка → починка и регрессия → freeze. ~5 сценариев обычно, ~7–10 для критических слоёв, углы: норма, неполный ввод, живая формулировка, граница, отказ. После починки — упавший сценарий плюс соседние | LOCKED | `DEVELOPMENT_WORKFLOW.md` |
| D-052 | Планировщик Фазы 5 **детерминирован**: раскладку памяти по шагам делает код, а не модель. Даёт воспроизводимую оценку, аудируемость и независимость от провайдера | LOCKED | `phase-5-plan.md` §4.3a |
| D-053 | Token Value Capture — первая обученная область, **не окончательная граница ATLAS**. Долгосрочная цепочка слоёв зафиксирована как направление и **не входит в объём Фазы 5**. Расширение области — отдельное решение владельца | LOCKED | `phase-5-plan.md` §10a |
| D-055 | `VERIFIED` в Фазе 5 выставляется контролируемым скриптом владельца/админа — **не ждём UI Фазы 9**. Требования: только человек, никакого автоматического промоушена моделью, полноценный админ-UI не строится, действие остаётся аудируемым. Это операционный механизм, чтобы прогнать и проверить жизненный цикл Фазы 5. Создание кандидата и промоушен в `VERIFIED`/`ACTIVE` остаются разными понятиями; эволюция CORE — вне Фазы 5 | LOCKED | решение владельца 2026-08-21 |
| D-054 | Ввод пользователя — не знание. Вывод модели — не знание. Извлечённая память — не автоматически текущая истина. Переиспользование остаётся provenance-aware, freshness-aware и auditable | LOCKED | `phase-5-plan.md` §8.0 |
| D-067 | **Фаза 5 заморожена** владельцем 2026-08-21 на каноническом HEAD `3ff98347ae468122a3ac90abcc612c378fe2af99` (ветка `claude/phase-5-research-memory`), вердикт `PHASE 5 FINAL REVIEW PASSED — READY TO FREEZE`. Архитектура Фазы 5 не переоткрывается. Четыре LOW-находки повторного ревью (`promoted_by` переписывается после промоушена; `loadActivePattern()` без явного `status='ACTIVE'`; вакуумное `SATISFIED` при пустом `requiredComponents`; `WRONG_STEP_MAPPING` без golden-сценария) остаются **явно отложенным долгом** и freeze не блокируют. Фаза 6 — **НЕ НАЧАТА**, `research_enabled = false` (D-028) | LOCKED | `phase-5-freeze.md` |
| D-068 | **`false_reuse_rate` сам по себе структурно недостаточен** для обнаружения класса регрессий D-062 («противоречие ниже порога переиспользования»). Приёмка Фазы 5 обязана и дальше оценивать вместе: `false_reuse_rate`, `negative safety` и сравнение с **объявленной** контрольной истиной сценария (объявленной вручную, не выведенной из вывода планировщика, D-061). Агрегат, зависящий от суждения проверяемого компонента, измеряет согласованность, а не правильность | LOCKED | `phase-5-freeze.md` §4; финальная перепроверка блокеров D-062 |

---

## Фаза 6 — Research Engine v1 (Strategy Lock, 2026-08-21)

Владелец утвердил направление архитектуры и оба блокирующих решения
(D-071, D-083). После утверждения проведён прореживающий проход по
предложениям D-069…D-087: детали реализации и приёмки постоянными
решениями не становятся. Классификация и судьба каждого предложения —
`phase-6-plan.md` §17.2. Номера D-069, D-073, D-082, D-085, D-086, D-087
не заведены и не переиспользуются.

| ID | Решение | Статус | Где обосновано |
|---|---|---|---|
| D-070 | **Границы интеллекта в исполнении.** Область, бюджет, допустимость действия и момент остановки определяет код; модель предлагает запросы, извлекает и нормализует содержимое внутри этих границ и никогда не выбирает следующее действие, не расширяет область и не объявляет остановку. Продолжение D-052 из планирования в исполнение. Проверка: подмена всех модельных ролей fake-реализацией не меняет ни область, ни бюджет, ни допустимость исполнения, ни причины остановки | LOCKED | `phase-6-plan.md` §4.1, §17.3 |
| D-071 | **Граница способности — не исследование.** План выше `capability_at_start` завершает job как `FAILED` с `errorCode='CAPABILITY_BOUNDARY'`: Proof не создаётся, DEMO-квота возвращается. Новое терминальное состояние в v1 не вводится. Решение владельца: граница доступа не является выполненным исследованием и не должна ни изготавливать Proof, ни списывать квоту | LOCKED | решение владельца 2026-08-21; `phase-6-plan.md` §13, §15 сценарий 11; уточняет D-064 |
| D-072 | **Единица работы — компонент шага Pattern.** Рабочий список берётся только из `stepDecisions[].components[]`; `alreadySatisfiedSteps`/`requiredFreshEvidence`/`missingSteps` — проекции, авторитетными не являются. Компонент `SATISFIED` внутри шага `REQUIRED_FRESH` не переисследуется вне явного пути инвалидации (§5.3 плана). Движок не переинтерпретирует и не «чинит» контракт: расхождение выведенной области с `excludedScope` — жёсткий отказ job'а | LOCKED | `phase-6-plan.md` §3.2–3.3, §5.1–5.3; поглощает предложение D-073 |
| D-074 | **Сила источника не считается повторами.** Класс источника (детерминированный) и официальность домена (подтверждённая человеком) — независимые оси. `SOCIAL` не поддерживает вывод никогда — только порождает исследование. Число повторов утверждения не входит в правило достаточности. Официальность `CLAIMED` ограничивает компонент до `PARTIALLY_SUPPORTED` | LOCKED | `phase-6-plan.md` §7.2–7.3; канон §20 |
| D-075 | **Движок не повышает память никогда.** Единственное разрешённое автоматическое изменение — понижение здоровья `OK → REVERIFY` при доказанном свежем противоречии: понижение не создаёт знание и потому не является обходом D-025. Создание записей — только `OBSERVED`; повышение и переход в `ACTIVE` остаются человеческими (D-021) | LOCKED | `phase-6-plan.md` §10.3, §12.4 |
| D-076 | **Доказательство без provenance не существует.** Evidence возможен только с `retrieved_url` + `content_hash` + `fetched_at`; сниппет поисковой выдачи доказательством не является — движок обязан открыть документ сам | LOCKED | `phase-6-plan.md` §6.4, §7.1 |
| D-077 | **Пять понятий раздельны:** SOURCE / EVIDENCE / FACT / RESEARCH MEMORY / PROOF CLAIM. Вывод Proof не становится Evidence или памятью без человека; память не доказывает свежесть. Схлопывание понятий ради удобства реализации запрещено | LOCKED | указание владельца 2026-08-21; `phase-6-plan.md` §6.1 |
| D-078 | **Отсутствие доказательства не доказывает отсутствие.** Недоказанное текущее состояние — `UNKNOWN`/`UNRESOLVED`, а не «механизм выключен». Вытеснение старого утверждения требует одновременно: несёт текущее состояние, класс источника не ниже, строго новее; иначе это противоречие, а не обновление | LOCKED | `phase-6-plan.md` §8.4; CHECK 2 `atlas-core` |
| D-079 | **Вердикт выводится из формы экономической цепочки**, а не из качества прозы: отсутствующее звено делает сильный вывод невозможным структурно (`buyback ≠ burn`, `выручка протокола ≠ выручка токена`, `полезность системы ≠ захват ценности`) | LOCKED | `phase-6-plan.md` §9 |
| D-080 | **Достаточность считается по компоненту**, количество источников в правило не входит: требуется прямое доказательство пригодного для этого компонента класса, внутри окна свежести, без неразрешённого противоречия | LOCKED | `phase-6-plan.md` §11.2 |
| D-081 | **Confidence детерминирован** — функция кода от состояний компонентов, классов источников и ограничений; модель число не называет | LOCKED | `phase-6-plan.md` §11.4 |
| D-083 | **Proof v1 = Proof Core.** Фаза 6 создаёт вердикт, confidence, слои 1–4, 6, 7 (LOCKED §7), Evidence/Sources/Gaps; слой 5 намеренно пуст. Отложено: Proof Map, layered UI, Telegram/контентное оформление, финальная редакторская система copy, визуальная подача сверх минимальной продуктовой поверхности. Назначение Proof Core — сделать Фазу 6 вручную проверяемой владельцем как настоящим пользователем | LOCKED | решение владельца 2026-08-21; `phase-6-plan.md` §12.3 |
| D-084 | **`INSUFFICIENT_EVIDENCE` — успешный исход исследования** с потреблением квоты; любой отказ до появления Proof квоту возвращает. Исчерпание бюджета не порождает уверенность | LOCKED | `phase-6-plan.md` §11.3, §13; `atlas-proof` |
| D-088 | **Владение Evidence — две координаты, обе в БД.** Канонический цикл Фазы 6 — `Research Job → Source → Evidence → сопоставление → Proof`, поэтому `evidence.proof_id` становится nullable, а `evidence.research_job_id` добавляется как `NOT NULL`: Evidence существует до Proof. Proof-заглушки ради удовлетворения FK запрещены. Валидных состояний ровно два — `JOB_ONLY` (job есть, Proof ещё/уже не будет) и `PROOF_BOUND` (Proof того же job'а); `ORPHAN` структурно невозможен. Привязка к Proof чужого job'а запрещена составным внешним ключом `(proof_id, research_job_id) → proofs(id, research_job_id)`, а не дисциплиной кода (D-010). Миграция аддитивна и обратно-безопасна: backfill из `proofs.research_job_id` полон, существующие связи Evidence↔Proof сохраняются, семантика удаления не меняется | LOCKED | решение владельца P1, 2026-08-21; `phase-6-plan.md` §6.3–6.3a |
| D-089 | **Проектные классы источника ставит человек, а не вывод.** Классы `OFFICIAL_DOCS`, `GOVERNANCE`, `OFFICIAL_REPORT` для конкретного проекта выводятся исключительно из явного поля `routeClass` в содержимом `ACTIVE` записи `project_memory_items.kind='SOURCE_ROUTE'` того же проекта и того же домена. Поисковый провайдер, содержимое загруженной страницы, извлечение моделью, формулировка URL и имя домена этих классов не дают никогда. `routeClass` применяется только там, где публичный детерминированный классификатор не опознал домен положительно — иначе `routeClass` на общей площадке или соцсети обходил бы D-074. `CONFIRMED` сам по себе сильного класса не даёт: официальный аккаунт проекта остаётся `SOCIAL` + `CONFIRMED`. `ACTIVE` SOURCE_ROUTE без `routeClass` валиден и даёт только официальность; исторические записи вида `{ domain }` совместимы без миграции. Создание и повышение SOURCE_ROUTE, как и изменение `routeClass`, остаются человеческими (D-021, D-025, D-075); неопознанный источник не повышается ради достаточности S5. **Уточняет D-074** | LOCKED | решение владельца 2026-08-21; `phase-6-plan.md` §7.2–7.2a |
| D-090 | **`maxModelCostMicro` — потолок авторизованного расхода на модельные вызовы в микро-USD**, а не абстрактная внутренняя единица (подтверждает семантику, уже заявленную в `config/product.ts`). Покрывает модельные роли движка (`QueryProposer`, `EvidenceExtractor` и будущие); денежный расход `SearchGateway` в него не входит и ограничен отдельно `maxSearchQueries`. Для каждого вызова консервативная верхняя граница выводится из утверждённого профиля модели (идентификатор, цены входа/выхода, максимумы входа/выхода, версия прайса) и резервируется атомарно **до** обращения к провайдеру; при нехватке остатка вызов не выполняется. Граница действительна только если вызов ограничен теми же числами: ограничить → оценить → зарезервировать → вызвать. Нет утверждённого профиля у production-модели — **fail closed**, типизированная ошибка конфигурации/бюджета, живого вызова нет. Резерв — авторизованный максимум, а не факт расхода (D-036): сверка с фактическим потреблением возможна только вниз и по отдельно утверждённому правилу, никогда вверх и никогда выше замороженного потолка job'а. Константы `5_000`/`15_000` не являются доказанными денежными границами и до `research_enabled = true` подлежат замене либо формальному выводу из профиля | LOCKED | решение владельца 2026-08-21; `phase-6-plan.md` §5.6 |

---

## Закрытые дефекты Фазы 4

Живая ручная приёмка пройдена 2026-08-21 против реального Anthropic
gateway. Эти решения **закрыты и не пересматриваются**.

| ID | Вопрос | Варианты и рекомендация | Где описано |
|---|---|---|---|
| D-037 | Глубина уточнения Interpreter | **ПРИМЕНЕНО — вариант 2** (выбор владельца, 2026-08-21): «зачем нужен токен» и «доходит ли доход до держателя» считаются одной задачей Token Value Capture, ответ сразу, без уточнения | `phase-4-freeze.md` §5; `src/server/interpreter/prompt.ts` (правило 4) |
| D-038 | Пропуск `understood_summary` на маршруте уточнения | **ПРИМЕНЕНО.** Один повтор с замечанием модели, деградация — только после него (было: деградация сразу, экран без строки «Понял») | наблюдение живого прогона; `src/server/interpreter/interpret.ts` (`callModel`) |
| D-039 | Грамотность ответов модели («заработивает») | **ПРИМЕНЕНО.** Строка в промпте про грамотность языка (правило 9). Модель не менялась — `interpreter_model` остаётся `claude-haiku-4-5` (D-026) | наблюдение живого прогона; `src/server/interpreter/prompt.ts` |

---

## Открытые решения

Ни одно из них не блокирует разработку Фазы 5.

| ID | Вопрос | Варианты и рекомендация | Где описано |
|---|---|---|---|
| D-045 | Финальный copy пользовательских текстов | Тексты состояний, уточнений, границ области. Утверждает владелец после просмотра реальных экранов | `phase-3-plan.md` §6, `phase-4-freeze.md` |

---

## Как пользоваться

1. **Начало любой задачи** — прочитать этот файл. Он ~10 000 символов
   против ~584 000 во всём `docs/`.
2. **Решение со статусом `LOCKED`** не обсуждается. Требуется изменить —
   `STRATEGY REVIEW REQUIRED` и остановка.
3. **Решение со статусом `OPEN`** блокирует зависящую работу. Не
   выбирать за владельца.
4. **Новое решение владельца** — строка сюда в том же коммите. Решение,
   оставшееся только в переписке, будет переспрошено.
