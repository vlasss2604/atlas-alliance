# ATLAS PROOF — Реестр решений

> **Это первый файл, который читают в любом новом чате.**
> Он существует, чтобы уже принятые решения не обсуждались заново.
>
> Статусы:
> `LOCKED` — не пересматривать. Если работа требует изменить решение —
> вывести `STRATEGY REVIEW REQUIRED` и остановиться.
> `OPEN` — ждёт решения владельца. Реализацию, зависящую от него, не начинать.
> `SUPERSEDED` — заменено; указано, чем именно.
>
> Реестр **append-only**: строки не удаляются, статус меняется.
> Новое решение владельца записывается сюда в том же коммите, что и работа.

---

## Продукт и доступ

| ID | Решение | Статус | Где обосновано |
|---|---|---|---|
| D-001 | Приоритет источников: `01_LOCKED_DECISIONS.md` выше canonical. Legacy-фичи (Links, Deep Check, TON/GRAM, тарифы FREE/START/PLUS/PRO, 4 стадии прогресса) в код не попадают | LOCKED | `atlas-audit.md`, решение A2 |
| D-002 | Стек: Next.js 16 App Router + отдельный Node-worker, PostgreSQL 16 + Drizzle, pg-boss, SSE + polling fallback, Tailwind 4, vitest + реальный Postgres, Playwright | LOCKED | `atlas-audit.md`, решение A1 |
| D-003 | DEMO: 3 Proof пожизненно, потолок способности `TARGETED_REFRESH`, ограниченный список проектов. DEMO — настоящий ATLAS в миниатюре, ограничен охватом и бюджетом, но не качеством | LOCKED | `01_LOCKED_DECISIONS.md` §2 |
| D-004 | ARI • CORE: 2999 Stars в месяц, `FRESH_RESEARCH`, динамический список `ACTIVE_CORE`-проектов, а не фиксированный каталог | LOCKED | `01_LOCKED_DECISIONS.md` §3 |
| D-005 | Proof приватен по умолчанию. Публичных Proof-ссылок в v1 нет; значения `PUBLIC` не существует в схеме | LOCKED | `01_LOCKED_DECISIONS.md` §7 |
| D-006 | Scope и Entitlement — разные проверки. Проект может быть внутри области ATLAS и при этом недоступен DEMO | LOCKED | `atlas-intent`, `gates.ts` |
| D-007 | Подписка `CANCEL_AT_PERIOD_END` даёт доступ до `valid_until` (вариант A) | LOCKED | `phase-1-plan.md` §5 |
| D-008 | Квота DEMO — reservation model: допуск считает `RESERVED + CONSUMED`, пользователю показывается только `CONSUMED`. Неудачный гейт квоту не тратит | LOCKED | `phase-1-plan.md` |
| D-009 | Максимум 2 уточнения, затем flow закрывается фиксированным сообщением. Лимит держит CHECK в БД | LOCKED | `01_LOCKED_DECISIONS.md` §5 |

## Архитектура и инварианты

| ID | Решение | Статус | Где обосновано |
|---|---|---|---|
| D-010 | Инварианты живут в БД, а не в дисциплине кода: партиальные уникальные индексы (один активный job, dedupe активных задач, одна entitling-подписка, одна ACTIVE-версия Pattern), CHECK-констрейнты, триггеры графа переходов, append-only журнал | LOCKED | `phase-1-plan.md` §4 |
| D-011 | PostgreSQL — источник истины. SSE — только транспорт уведомлений. Три слоя сходимости: снимок при подключении, NOTIFY, периодическая сверка | LOCKED | `phase-3-plan.md` |
| D-012 | ResearchJob — персистентная сущность, а не блокирующий вызов. Если UI обещает «вернитесь позже», сервер обязан это выполнить | LOCKED | `02_ARCHITECTURE_AND_PHASES` |
| D-013 | Пользовательский ввод — всегда данные, никогда инструкции. Границы блока данных экранируются | LOCKED | `phase-4-plan.md` §5 |
| D-014 | Модель предполагает — каталог решает. Резолюция проекта детерминирована сервером; LLM не может «заявить» проект в scope | LOCKED | `phase-4-plan.md` §2.6 |
| D-015 | Entitlement Gate проверяет **все** названные сущности задачи. Половина сравнения не выполняется | LOCKED | `atlas-intent`, adversarial review Фазы 4, HIGH-1 |
| D-016 | Два слоя понимания: `understood_summary` (язык пользователя, показывается) и `research_task` (английский, вход движка, наружу не идёт) | LOCKED | `phase-4-freeze.md` §1 |
| D-017 | Исследование стартует только при `status=READY` **и** `route=DEEP_RESEARCH` | LOCKED | `phase-4-plan.md` §3.4 |
| D-018 | Аутентификация: HMAC-проверка initData на сервере, сессия в HttpOnly-cookie, CSRF-токен, allowlist только своих origin, bootstrap-исключение для `POST /api/auth/telegram` | LOCKED | `phase-2-plan.md` |
| D-019 | Rate limit хранится в таблице `auth_rate_limits`; бакет по Telegram-id заводится только после успешной проверки подписи (иначе — вектор блокировки чужого аккаунта) | LOCKED | `phase-2-plan.md` §2.1 |
| D-046 | `quick_answer` на маршруте `OUTSIDE_CURRENT_DOMAIN` нормализуется в `null`, а не роняет контракт — поле декоративное там (продукт показывает свой фиксированный текст, `toView()` его всё равно отбрасывает). На `DEEP_RESEARCH` строгость сохранена намеренно (LOCKED D-027: никакого AI-текста рядом с Proof). **Переподтверждено живой ручной приёмкой Фазы 4, 2026-08-21** | LOCKED | регрессия «Стоит ли покупать SUI сейчас?» → 502; `src/server/interpreter/schema.ts` (`normalizeModelOutput`); `phase-4-freeze.md` §6 |
| D-047 | `route=CLARIFICATION_REQUIRED` механически форсирует `status=NEEDS_CLARIFICATION`, тем же способом, что уже применён к маршрутам-объяснениям (route → status — наша проекция, не суждение модели). До этого модель, вернувшая верный маршрут при `status=READY`, получала жёсткий отказ схемы ("READY with CLARIFICATION_REQUIRED") вместо честного уточняющего вопроса. **Переподтверждено живой ручной приёмкой Фазы 4, 2026-08-21** | LOCKED | регрессия (лог: `[interpreter] unavailable READY with CLARIFICATION_REQUIRED`, 502) на «доход протокола реально доходит до держателей токена?»; `src/server/interpreter/schema.ts` (`normalizeModelOutput`); `phase-4-freeze.md` §6 |

## ARI, память и обучение

| ID | Решение | Статус | Где обосновано |
|---|---|---|---|
| D-020 | Граница обучения §11 сохраняется. Автоматическое извлечение уроков даёт только **кандидатов** | LOCKED | `ARI_LEARNING_LOOP.md` |
| D-021 | Переход `CANDIDATE → ACTIVE` выполняет человек. Автоматического перевода нет | LOCKED | `ARI_LEARNING_LOOP.md` §11 |
| D-022 | CORE управляется человеком. Новая версия CORE — только после слепой/adversarial регрессии | LOCKED | `ARI_LEARNING_LOOP.md` |
| D-023 | Извлечение кандидатов **не** ограничено `VERIFIED`; ограничен только промоушен. Пороги извлечения и промоушена — разные | LOCKED | решение владельца по Learning Loop |
| D-024 | Пользовательская активность даёт сигналы, а не истину. Спрос не активирует тему | LOCKED | `atlas-memory` |
| D-025 | Lifecycle памяти не обходится никогда, даже в тестах — единственный барьер против отравления | LOCKED | `02_ARCHITECTURE_AND_PHASES`, security-мандат Фазы 5 |
| D-026 | Модель Interpreter — `claude-haiku-4-5`; меняется ключом `interpreter_model` без деплоя | LOCKED | `phase-4-freeze.md` §2 |
| D-027 | `QUICK_EXPLANATION` / `NO_RESEARCH_NEEDED` показывают короткий ответ ARI (вариант A) — без Proof, без Research Memory, с лимитом длины | LOCKED | `phase-4-freeze.md` §2 |
| D-028 | `interpreter_enabled = true` с Фазы 4; `research_enabled = false` до Фазы 6 | LOCKED | `phase-4-freeze.md` §2 |

## Процесс разработки

| ID | Решение | Статус | Где обосновано |
|---|---|---|---|
| D-029 | Цикл фазы: план → решение владельца → реализация → тесты → adversarial review со свежим контекстом → разбор находок → отчёт | LOCKED | `DEVELOPMENT_WORKFLOW.md` |
| D-030 | Роли — закрытый список: Main Claude, Adversarial Reviewer, Blind Evaluator (с Фаз 5–6). Новая роль только после измеренной проблемы | LOCKED | `DEVELOPMENT_WORKFLOW.md` |
| D-031 | Параллельная реализация несколькими агентами не используется | LOCKED | `DEVELOPMENT_WORKFLOW.md` |
| D-032 | Распределение моделей: Opus — архитектура, security, планы фаз, adversarial review; Sonnet — реализация, тесты, миграции, UI; Haiku — механическая генерация и runtime Interpreter | LOCKED | `DEVELOPMENT_WORKFLOW_OPTIMIZATION.md` §2–3 |
| D-033 | Чаты разделены по стадиям фазы. Право записи в код — только у чата реализации | LOCKED | `DEVELOPMENT_WORKFLOW_OPTIMIZATION.md` §5 |
| D-034 | Иерархия контекста: `DECISIONS` → LOCKED → план фазы → нужный skill. canonical поднимается точечно | LOCKED | `DEVELOPMENT_WORKFLOW_OPTIMIZATION.md` §4 |
| D-035 | Владелец продукта — отдельный обязательный слой решений между планированием и реализацией. Opus предъявляет варианты, владелец выбирает, Sonnet исполняет | LOCKED | `DEVELOPMENT_WORKFLOW_OPTIMIZATION.md` §7.1 |
| D-036 | Никогда не имитируется то, чего не произошло: ни прогресс исследования, ни пройденный тест, ни выполненная проверка | LOCKED | `PRODUCT_QUALITY_DIRECTIVE.md` |

---

## Фаза 5 — Research Memory (Strategy Lock)

| ID | Решение | Статус | Где обосновано |
|---|---|---|---|
| D-040 | Retrieval v1 — **structured-first** по онтологии (проект, тема, интент, шаг Pattern, claim-ключи) + FTS/`pg_trgm`. pgvector и эмбеддинги не вводятся. `MemoryRetrievalGateway` сохраняется как шов для будущего гибрида, если оценка покажет пропуски | LOCKED | `phase-5-plan.md` §3.4 |
| D-041 | `proofs.verification_status` (`DRAFT`/`REVIEWED`/`VERIFIED`) добавляется. `VERIFIED` гейтит **промоушен в ACTIVE-память**, а не появление кандидата. Промоушен выполняет человек | LOCKED | `phase-5-plan.md` §5.2 |
| D-042 | Кросс-проектное обобщение в v1 **запрещено**: память проекта A не закрывает шаг проекта B. Путь к переиспользуемым паттернам механизмов сохранён на будущее, но открывается отдельным решением | LOCKED | `phase-5-plan.md` §8 |
| D-043 | Ключ конфига `memory_enabled` — оценка Memory OFF vs ON штатный режим, а не хак в тестах | LOCKED | `phase-5-plan.md` §7 |
| D-044 | Словарь свежести нормализуется: `LOW_CHANGE` / `MEDIUM_CHANGE` / `HIGH_CHANGE`; `evidence` приводится одной миграцией, пока таблица пуста | LOCKED | `phase-5-plan.md` §5.3 |
| D-056 | Память обязана **менять Research Boundary Contract** (`already_satisfied_steps`, `reusable_evidence`, `required_fresh_evidence`, `missing_steps`, `known_information`, бюджет, `novelty_state`), а не подмешивать прошлый текст в промпт. Проверяемый вопрос: «исследовал бы Atlas иначе, потому что вспомнил?» | LOCKED | `phase-5-plan.md` §4.2 |
| D-057 | Режимы `MEMORY` / `TARGETED_REFRESH` / `FRESH_RESEARCH`. Переиспользование **никогда** не отменяет требование свежести | LOCKED | `phase-5-plan.md` §4.4 |
| D-048 | FACT / RESEARCH / PROOF memory разделены. Системная Research Memory **не зависит от хранения пользовательских данных**: provenance копируется при промоушене, ссылок с `RESTRICT` на пользовательский `evidence` нет — иначе удаление аккаунта блокируется | LOCKED | `phase-5-plan.md` §5.1 |
| D-049 | Метрики Фазы 5: recall, precision, noise rate, **false reuse rate**, снятые шаги, обновлённые шаги, дельта запланированных поисков, качество плана. `false_reuse_rate = 0` — условие приёмки. Blind Evaluator судит контракты, не зная, была ли память включена | LOCKED | `phase-5-plan.md` §7 |
| D-050 | Research Boundary Contract строится на 8 шагах Pattern v1 и явно фиксирует: что известно, что переиспользуется, что обновляется, чего не хватает, что вне области | LOCKED | `phase-5-plan.md` §4.1–4.2 |
| D-051 | **Стандарт приёмки слоя интеллекта**: концепция → архитектура → реализация → автотесты → живая ручная приёмка → починка и регрессия → freeze. ~5 сценариев обычно, ~7–10 для критических слоёв, углы: норма, неполный ввод, живая формулировка, граница, отказ. После починки — упавший сценарий плюс соседние | LOCKED | `DEVELOPMENT_WORKFLOW.md` |
| D-052 | Планировщик Фазы 5 **детерминирован**: раскладку памяти по шагам делает код, а не модель. Даёт воспроизводимую оценку, аудируемость и независимость от провайдера | LOCKED | `phase-5-plan.md` §4.3a |
| D-053 | Token Value Capture — первая обученная область, **не окончательная граница ATLAS**. Долгосрочная цепочка слоёв зафиксирована как направление и **не входит в объём Фазы 5**. Расширение области — отдельное решение владельца | LOCKED | `phase-5-plan.md` §10a |
| D-055 | `VERIFIED` в Фазе 5 выставляется контролируемым скриптом владельца/админа — **не ждём UI Фазы 9**. Требования: только человек, никакого автоматического промоушена моделью, полноценный админ-UI не строится, действие остаётся аудируемым. Это операционный механизм, чтобы прогнать и проверить жизненный цикл Фазы 5. Создание кандидата и промоушен в `VERIFIED`/`ACTIVE` остаются разными понятиями; эволюция CORE — вне Фазы 5 | LOCKED | решение владельца 2026-08-21 |
| D-054 | Ввод пользователя — не знание. Вывод модели — не знание. Извлечённая память — не автоматически текущая истина. Переиспользование остаётся provenance-aware, freshness-aware и auditable | LOCKED | `phase-5-plan.md` §8.0 |

---

## Закрытые дефекты Фазы 4

Живая ручная приёмка пройдена 2026-08-21 против реального Anthropic
gateway. Эти решения **закрыты и не пересматриваются**.

| ID | Вопрос | Варианты и рекомендация | Где описано |
|---|---|---|---|
| D-037 | Глубина уточнения Interpreter | **ПРИМЕНЕНО — вариант 2** (выбор владельца, 2026-08-21): «зачем нужен токен» и «доходит ли доход до держателя» считаются одной задачей Token Value Capture, ответ сразу, без уточнения | `phase-4-freeze.md` §5; `src/server/interpreter/prompt.ts` (правило 4) |
| D-038 | Пропуск `understood_summary` на маршруте уточнения | **ПРИМЕНЕНО.** Один повтор с замечанием модели, деградация — только после него (было: деградация сразу, экран без строки «Понял») | наблюдение живого прогона; `src/server/interpreter/interpret.ts` (`callModel`) |
| D-039 | Грамотность ответов модели («заработивает») | **ПРИМЕНЕНО.** Строка в промпте про грамотность языка (правило 9). Модель не менялась — `interpreter_model` остаётся `claude-haiku-4-5` (D-026) | наблюдение живого прогона; `src/server/interpreter/prompt.ts` |

---

## Открытые решения

Ни одно из них не блокирует разработку Фазы 5.

| ID | Вопрос | Варианты и рекомендация | Где описано |
|---|---|---|---|
| D-045 | Финальный copy пользовательских текстов | Тексты состояний, уточнений, границ области. Утверждает владелец после просмотра реальных экранов | `phase-3-plan.md` §6, `phase-4-freeze.md` |

---

## Как пользоваться

1. **Начало любой задачи** — прочитать этот файл. Он ~10 000 символов
   против ~584 000 во всём `docs/`.
2. **Решение со статусом `LOCKED`** не обсуждается. Требуется изменить —
   `STRATEGY REVIEW REQUIRED` и остановка.
3. **Решение со статусом `OPEN`** блокирует зависящую работу. Не
   выбирать за владельца.
4. **Новое решение владельца** — строка сюда в том же коммите. Решение,
   оставшееся только в переписке, будет переспрошено.
