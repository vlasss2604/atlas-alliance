# Фаза 6, срез S6 — Mechanism Assembly. Стратегия и контракт

> Статус: **УТВЕРЖДЕНО владельцем 2026-08-22** на коммите стратегии
> `b5f36cf`. D-098, D-100, D-102, D-103 приняты как есть; D-099 принято
> с уточнением (ветвление возможно раньше терминала; потоки с общим
> префиксом); **D-101 переработано до блокировки** — семантический кортеж
> заменён структурно-provenance идентичностью. Обе правки внесены ниже
> как нормативные. Разработка S6 может начинаться; настоящий документ —
> контракт реализации.
>
> База: замороженный S5 — HEAD `a657db39eee22353d5243ff3ea55a0a3d308be60`,
> freeze `54df1a9` (D-097); замороженный S4 — `d7e5b8a`, freeze `a258b99`
> (D-091). Ни один срез не переоткрывается. Фаза 5 заморожена,
> `research_enabled = false`.
>
> Всё ниже спроектировано на полях, которые S5 и S4 **действительно
> пишут**, а не на полях, которые план когда-то перечислил. Там, где
> замороженные данные не содержат нужного — это сказано прямо, а не
> обойдено выводом.
>
> Принцип среза: S5 отвечает «что установлено про **этот** компонент»;
> S6 отвечает «какой **механизм** складывается из установленного» — и
> ничего сверх. Достаточность и вердикт остаются за S7.

---

## 0. Что реально доступно S6 (проверено по коду, не по плану)

Это определяет всё остальное, поэтому идёт первым.

**S5 пишет в `ComponentReconciliationResult`** (`component-reconciler.ts`,
таблица `research_component_results`):

```
step · component · status · reasonCodes[] · supportingEvidenceIds[]
contradictingEvidenceIds[] · excludedEvidence[] · currentState
temporalBasis{basisField, at} · tokenStateMentions[] · requiresFreshEvidence
```

**Pattern v1 даёт** (`domain/pattern.ts`, `PATTERN_V1_CONTENT`): 8 шагов,
`requiredComponents` (10 компонентов, распределены по шагам),
`componentRequirements` (`establishingClasses`, `requiresCurrentState`,
`requiresLiveMechanismState`, `freshnessClass`, `tokenStateSensitive`,
`requiredTokenState`). `componentRequirementsFor` бросает
`PatternConfigurationError` на отсутствующий компонент — молчаливого
дефолта нет.

**`ContractView` даёт**: `patternVersion`, `mode`, `workQueue`, `reused`,
`excludedComponents`, `stopConditions`.

### 0.1 Четыре следствия, которые невозможно обойти

**1. Ни actor, ни action, ни asset, ни valueSource в замороженных данных
не существуют.** `evidence.value_source` — колонка есть, S4 её не пишет,
остаётся `NULL` (то же самое обнаружилось в S5 и было честно записано).
`EconomicLink { fromActor, action, asset, valueSource, toActor }` из
плана §9.1 **не выводится из имеющихся структурных полей ни при каком
детерминированном правиле**. Требовать этого от S4 — переоткрыть
замороженный срез.

**2. `destinationKind` и `recipientKind` тоже не существуют как поля.**
Единственное, что о них известно структурно, — статус компонентов
`DESTINATION` и `RECIPIENT` от S5 плюс их `tokenStateMentions`.

**3. Единственные машиночитаемые семантические поля, дошедшие до S6, —
`currentState` (нормализованный словарь CORE CHECK 2 + `PAUSED`) и
`tokenStateMentions` (закрытый лексический детектор S5).** Всё остальное
содержательное лежит в свободном тексте `fragment` / `summary`.

**4. Порядок цепочки уже задан Pattern'ом**, а не выводится: шаги 1→8
и распределение компонентов по ним — данные CORE, которые меняет человек.

### 0.2 Что из этого следует для стратегии

Есть ровно три честных выхода, и выбирать нужно осознанно:

| Выход | Цена |
|---|---|
| A. Ввести модель в S6 для разбора прозы | нарушает предпочтение владельца §24, вводит поверхность инъекции, требует бюджета и профиля стоимости (D-090) |
| B. Собирать только структурный скелет из статусов S5, без классификации | честно, но `buyback ≠ burn` и `Reward ≠ Revenue` останутся невыразимыми — а это ядро методологии |
| C. Существование звена — **только** из статуса S5; классификация звена — детерминированный закрытый словарь по уже допущенному Evidence, `UNKNOWN` по умолчанию | сохраняет детерминизм и даёт структурные различия CORE |

**Рекомендация — C**, с жёсткой инверсией, описанной в §5.3:
**классификация никогда не создаёт звено, она только помечает звено,
существование которого установил S5.** Слово «burn» в тексте не может
породить звено burn, если компонент `DESTINATION` не установлен S5.

---

## 1. Цель S6 одним предложением

> Превратить детерминированные результаты компонентов S5 в **структурное
> машиночитаемое представление экономического механизма** — со звеньями,
> разрывами, идентичностью потока и полной трассируемостью до Evidence —
> не вынося суждения о том, доказано ли утверждение пользователя.

---

## 2. Объём

| Возможность | Решение |
|---|---|
| Потребление `ComponentReconciliationResult` | **IN** |
| Потребление структуры утверждённого Pattern | **IN** |
| Соединение совместимых результатов компонентов | **IN** |
| Идентичность экономического потока | **IN** — структурная, §6 (D-101) |
| Отношения recipient / destination | **IN** — §5, §9 |
| Перенос состояния токена | **IN** — §8 |
| Привязка `valueSource` | **IN** как атрибут сборки — §7 |
| Явные разрывы между установленными компонентами | **IN** — §11 |
| Перенос состояния механизма | **IN** — §10 |
| Provenance от узлов/рёбер к S5 и Evidence | **IN** — §18 |
| Детерминированный машиночитаемый вывод | **IN** — §19 |
| Персистенция как производная проекция | **IN** — §20 |
| Идемпотентность и replay | **IN** — §20 |
| Машиночитаемые предупреждения сборки | **IN** как `gaps` — отдельного канала warnings нет (§11.3) |
| `COMPLETE_PATH` / `PARTIAL_PATH` как форма | **IN** — на уровне потока, не как вердикт (§22) |
| Параллельные потоки и ветвление | **IN** — отдельные `MechanismFlow` с общим префиксом (§6, §13) |
| Числовой confidence | **DEFERRED** — S7 (D-081) |
| Достаточность всей цепочки, вердикт утверждения | **FORBIDDEN** — S7 (§23) |
| Проза Proof, ProofFilter/Composer, Critic | **FORBIDDEN** |
| Переоткрытие S5/S4, пересчёт авторитета Evidence | **FORBIDDEN** |
| Изменение статуса S5 | **FORBIDDEN** |
| Промоушен памяти | **FORBIDDEN** — D-021, D-025, D-075 |
| Изобретение отсутствующих звеньев | **FORBIDDEN** — §11 |
| «Тот же токен ⇒ тот же поток» | **FORBIDDEN** — §6 |
| «Тот же получатель ⇒ то же экономическое право» | **FORBIDDEN** — §9 |
| Вывод `burn` из `buyback`, ярлык как исход | **FORBIDDEN** — §16 |
| Вызов модели, сеть | **FORBIDDEN** — §24 |

---

## 3. Входной контракт

### 3.1 Читает ли S6 Evidence напрямую

**Да — но только те строки, которые S5 уже допустил, и только для
разметки, никогда для переадjudication.**

Принцип: **S5 — единственный судья на уровне компонента.** S6 не
пересматривает допустимость, класс, официальность, свежесть и статус. Но
без доступа к тексту допущенного Evidence невозможны ни `valueSource`,
ни `destinationKind`, ни `recipientKind` (§0.1) — а без них методология
CORE (CHECK 1, CHECK 3, `Reward ≠ Revenue`) не представима структурно.

**Правило доступа — закрытое:**

1. S6 читает **только** строки, чьи `id` присутствуют в
   `supportingEvidenceIds` или `contradictingEvidenceIds` результата S5.
   Любая другая строка `evidence` для S6 не существует.
2. S6 читает **только** перечисленные ниже неизменяемые поля.
3. S6 **никогда** не пишет в `evidence` — ни одной колонки, включая
   `value_source` (§7).
4. S6 **никогда** не выводит из этих полей допустимость, статус или
   авторитет — только классификацию уже установленного.

| Поле | Зачем именно оно |
|---|---|
| `id` | provenance |
| `sourceId` | структурная привязка ветвей (§13.4) — не авторитет |
| `extractionUnitKey` | **структурная идентичность слота** (§13.2); при `null` — запасной ключ из `contentHash` + нормализованный `fragment` |
| `sourceClass` | атрибуция базиса `DURABILITY_BASIS` (§17), не авторитет |
| `officiality` | перенос оговорки в атрибут звена, не пересчёт |
| `mechanismState` | перенос состояния (S5 уже нормализовал агрегат; §10) |
| `publishedAt` / `fetchedAt` | временная совместимость (§10) |
| `fragment` / `summary` | закрытая словарная классификация (§5.3) |
| `retrievedUrl` / `contentHash` | provenance для Proof Core |

Всё остальное (`does_not_prove`, `claim_key`, `snapshot_ref`, …) S6 не
читает: не нужно — значит не читается.

### 3.2 Вход

```ts
interface MechanismAssemblyInput {
  researchJobId: string;
  patternVersion: number;          // сверяется с ContractView; расхождение = сбой
  pattern: PatternContent;         // ACTIVE, привязанный к контракту job'а
  contractView: Pick<ContractView,
    "patternVersion" | "mode" | "workQueue" | "reused" | "excludedComponents">;
  componentResults: ComponentReconciliationResult[];  // все, что есть у job'а
  admittedEvidence: AssemblyEvidenceProjection[];     // §3.1, только допущенные
}
```

`now` **не передаётся**: вся временная работа уже сделана S5 и лежит в
`temporalBasis`. Введение `now` в S6 сделало бы сборку зависящей от
момента запуска и сломало бы §19 (тот же вход → тот же выход).
Отсутствие `now` — это инвариант, а не упущение.

Бюджет, очередь и контроллер во вход не входят: у сборщика не должно быть
доступа к тому, чем он не имеет права управлять (та же дисциплина, что в
S5 §11.1).

---

## 4. Pattern v1 → семантика сборки

Не каждый компонент становится узлом. Явное распределение:

| Компонент | Шаг | Роль в сборке | Почему |
|---|---|---|---|
| `SOURCE_OF_VALUE` | 1 | **узел-исток** + задаёт `valueSource` потока | откуда ценность берётся — начало пути (CHECK 3, CHECK 5) |
| `FLOW_PATH` | 2 | **ребро** исток → механизм | это описание движения, а не точка |
| `MECHANISM_SPEC` | 3 | **узел-механизм** | точка, где принимается правило распределения |
| `GOVERNANCE_BASIS` | 3 | **атрибут авторитета** узла-механизма | одобрение доказывает полномочие, а не движение ценности — экономическим узлом не является |
| `EXECUTION_EVIDENCE` | 4 | **гейт ребра** механизм → назначение | без него ребро остаётся спецификацией, а не исполнением |
| `CURRENT_STATE` | 5 | **гейт жизненного цикла потока** | атрибут, а не передача ценности (§10) |
| `DESTINATION` | 6 | **терминальный узел** + `destinationKind` | куда ценность попадает |
| `RECIPIENT` | 6 | **атрибут терминального узла** + `recipientKind` | кто держит право — не отдельная точка пути, иначе «есть получатель» превратится в «есть поток» |
| `NET_EFFECT` | 7 | **атрибут исхода потока** | исход, а не звено; ярлык звена его не создаёт (§16) |
| `DURABILITY_BASIS` | 8 | **метаданные механизма** | не экономическая передача (§17) |

Из десяти компонентов узлами становятся **три** (`SOURCE_OF_VALUE`,
`MECHANISM_SPEC`, `DESTINATION`), рёбрами — **два** (`FLOW_PATH`,
исполнение), остальные пять — атрибуты и гейты. Это и есть ответ на «не
предполагать, что каждый компонент — узел графа».

---

## 5. Модель механизма

### 5.1 Не граф (вариант C по §5 задачи)

Общий граф не нужен и вреден: он позволяет выразить структуры, которых
Pattern v1 не порождает, и тем самым создаёт места, где может вырасти
недоказанный вывод. Берётся **ограниченная Pattern'ом цепочка** —
подтверждение уже утверждённого плана §9.1 («не граф, упорядоченная
цепочка типизированных звеньев»), расширенное ограниченным ветвлением
(§13).

```ts
type MechanismNodeKind = "VALUE_SOURCE" | "MECHANISM" | "DESTINATION";

interface MechanismNode {
  kind: MechanismNodeKind;
  component: string;            // компонент, который его установил
  componentStatus: ComponentReconciliationStatus;
  qualifications: NodeQualification[];   // закрытый список, §12
  attributes: NodeAttributes;   // §5.2
  provenance: Provenance;       // §18 — непусто всегда
}

interface MechanismEdge {
  from: MechanismNodeKind;
  to: MechanismNodeKind;
  basisComponent: string;       // FLOW_PATH | EXECUTION_EVIDENCE
  basisStatus: ComponentReconciliationStatus;
  executed: boolean;            // true только при установленном EXECUTION_EVIDENCE
  qualifications: NodeQualification[];
  provenance: Provenance;
}

interface MechanismFlow {
  // §6 — СТРУКТУРНАЯ идентичность: производная от родословной и
  // provenance, никогда от семантических атрибутов.
  flowId: string;               // §6.3, детерминированный хеш
  lineage: LineageStep[];       // §6.2 — упорядоченная родословная
  sharedPrefixId: string | null;// §13.3 — общий установленный префикс
  branchPointStep: number | null;// шаг Pattern, где поток отделился

  lifecycle: "CURRENT" | "HISTORICAL" | "NOT_ESTABLISHED";  // §10
  nodes: MechanismNode[];       // в порядке шагов Pattern
  edges: MechanismEdge[];
  shape: "COMPLETE_PATH" | "PARTIAL_PATH";   // §22 — форма, не вердикт

  attributes: FlowAttributes;   // §6.4 — семантика, любое поле может быть UNKNOWN
  netEffect: NetEffectAttachment | null;      // §16
  durability: DurabilityAttachment | null;    // §17
  gaps: MechanismGap[];         // §11 — закрытый словарь
}

interface MechanismAssemblyResult {
  researchJobId: string;
  patternVersion: number;
  flows: MechanismFlow[];       // детерминированный порядок, §19
  unassignedGaps: MechanismGap[];  // разрывы, не принадлежащие ни одному потоку
}
```

Верхнеуровневого статуса нет (D-103). Ни одного поля с именем,
содержащим `verdict`, `sufficient`, `proven`, `confidence`, `claim`, в
схеме не существует, и это проверяется тестом.

**Верхнеуровневого статуса у `MechanismAssemblyResult` нет** — §22.

### 5.2 Атрибуты узлов — закрытые словари

```ts
type ValueSource =            // CHECK 3, словарь atlas-core, дословно
  | "USER_PAYMENT" | "PROTOCOL_ISSUANCE" | "TREASURY" | "EXTERNAL_INCENTIVE"
  | "FEES" | "COLLATERAL_RETURN" | "BURN_LINKED_ISSUANCE" | "MARKET_PURCHASE"
  | "UNKNOWN";

type DestinationKind =        // план §9.1, дословно
  | "BURN" | "BUYBACK_HOLD" | "TREASURY" | "DISTRIBUTION" | "LP"
  | "NONE" | "UNKNOWN";

type RecipientKind =          // план §9.1, дословно
  | "PASSIVE_HOLDER" | "STAKER" | "NODE_OPERATOR" | "TREASURY" | "LP"
  | "EXTERNAL" | "NONE" | "UNKNOWN";

type FlowDirection = "INBOUND" | "OUTBOUND" | "RETURN" | "UNKNOWN";  // §14

// §6.4 — АТРИБУТЫ потока, не его идентичность (D-101). Любое поле может
// быть UNKNOWN, и UNKNOWN никогда не влияет на flowId.
interface FlowAttributes {
  valueSource: ValueSource;
  direction: FlowDirection;
  recipientKind: RecipientKind;
  destinationKind: DestinationKind;
  tokenState: string | null;
  assetSymbol: string | null;   // §6.5 — только если структурно доступен
}
```

Ни один словарь не изобретается в S6: `ValueSource` взят из CHECK 3
`atlas-core`, `DestinationKind`/`RecipientKind` — из уже утверждённого
плана §9.1. Новая только `FlowDirection` (§14), и она минимальна.
Каждый словарь содержит `UNKNOWN`, и `UNKNOWN` — рабочее значение, а не
дефект: он порождает разрыв (§11) и никогда не догадку.

### 5.3 Инверсия, на которой держится вся безопасность среза

> **Существование звена определяет статус S5. Классификация только
> помечает уже существующее звено. Классификация не создаёт звеньев
> никогда.**

Практически:

1. Узел `DESTINATION` появляется **только** если компонент `DESTINATION`
   имеет статус `SUPPORTED` или `PARTIALLY_SUPPORTED`. Слово «burn» в
   тексте `MECHANISM_SPEC` узла не создаёт.
2. `destinationKind = BURN` возможен **только** на уже существующем узле
   `DESTINATION` и **только** если закрытый детектор нашёл соответствующий
   термин в Evidence, которое S5 допустил **для этого компонента**.
3. Ничего не найдено или найдено неоднозначно → `UNKNOWN`.
   **`UNKNOWN` — это разрыв (§11), а не мягкое значение по умолчанию:**
   он порождает `gap`, а не догадку.
4. Классификатор — та же дисциплина, что у детектора состояния токена S5
   (D-096): закрытый кодом владеемый список, проверка границ токенов,
   консервативный отказ. Словаря тикеров, онтологии и вывода синонимов
   нет. Текст читается как **данные**, никогда как инструкция (§25).

Именно эта инверсия делает мутации 3, 4, 5 (§27) невозможными не по
договорённости, а по конструкции.

---

## 6. Идентичность потока — структура и provenance, не семантика

### 6.0 Почему семантический кортеж отвергнут (D-101, нормативно)

Первая редакция предлагала ключ
`valueSource × direction × recipientKind × tokenState`. **Это отвергнуто
владельцем, и справедливо.** Три независимых дефекта:

1. **Кортеж не различает то, что обязан различать.** Два экономически
   разных механизма могут совпасть по всем четырём полям: «выручка
   протокола → OUTBOUND → казначейство → то же состояние токена»
   описывает более одного механизма. Совпадение ярлыков склеило бы их.
2. **`UNKNOWN` становится склейкой.** При скудной семантике (а она
   скудна — §0.1) большинство полей приходят `UNKNOWN`, и ключ
   `(UNKNOWN, UNKNOWN, UNKNOWN, null)` схлопнул бы **несвязанные** потоки
   в один. Наихудшее поведение ровно там, где данных меньше всего.
3. **Идентичность оказалась бы производной от классификации**, то есть от
   самого ненадёжного слоя среза. Ошибка детектора меняла бы не ярлык, а
   то, сколько потоков существует.

**Нормативная замена: идентичность выводится из родословной и
provenance; семантика остаётся атрибутом.**

### 6.1 Инверсия идентичности

> **Provenance разделяет. Классификация не объединяет никогда.**

Это тот же приём, что D-100 применяет к существованию: семантика может
только *уточнять* уже установленную структуру и не может её *создавать*.
Здесь — классификация может только **разделить** provenance-группу, но
никогда не **слить** две разные.

Следствие: ошибка или пустота классификатора приводит к **лишнему
разделению**, а не к ложному объединению. Направление отказа
консервативно по построению.

### 6.2 Родословная

```ts
interface LineageStep {
  step: number;                 // позиция компонента в Pattern
  component: string;
  componentResultKey: string;   // `${step}:${component}` — ключ строки S5
  evidenceIds: string[];        // допущенные id этого узла, отсортированы
}
```

Родословная — упорядоченный по шагам Pattern список позиций, которые
поток фактически прошёл, с фактическим provenance каждой из них.
Родословная **не содержит ни одного семантического поля**.

### 6.3 Вывод `flowId`

```
flowId = sha256( canonical(
  patternVersion,
  [ for each LineageStep in Pattern-step order:
      step, component, sorted(evidenceIds) ],
  branchSlotPath          // §13.2 — только структурные номера слотов
))
```

Свойства, каждое из которых требовалось владельцем:

| Требование | Как обеспечено |
|---|---|
| 1. Разные provenance-подкреплённые ветви не сливаются при равной семантике | семантика в вычисление не входит вовсе; разные наборы `evidenceIds` → разные хеши |
| 2. Совпадение `assetSymbol` не сливает потоки | символ в вычисление не входит |
| 3. Разный `assetSymbol` сам по себе не задаёт разное экономическое назначение | символ в вычисление не входит — он не может ни слить, ни разделить |
| 4. `UNKNOWN` не схлопывает несвязанные потоки | `UNKNOWN` в вычисление не входит вовсе; идентичность слота структурна независимо от того, известна классификация или нет (§13.2a) |
| 5. Повтор и смена порядка не меняют идентичность | `evidenceIds` сортируются лексикографически, шаги — по номеру Pattern; порядок БД и входа не участвует |
| 6. Пересчёт из тех же замороженных входов даёт те же id | функция чистая от `(Pattern, результаты S5, допущенные id)`; ни `now`, ни ordinal, ни порядка вставки в ней нет |

**Запрещённые входы идентичности:** порядок строк БД, произвольный
рантайм-ordinal, автоинкремент, время, любое изобретённое семантическое
значение **и любой выход классификатора** — включая `branchSlotPath`,
который выводится только из структурных ключей (§13.2). Каждый —
отдельная мутация (§27).

Проверяемое следствие: замена классификации `BUYBACK → UNKNOWN` при
неизменных структурных и provenance входах **обязана оставить `flowId`
прежним**; изменение структурной provenance ветви **обязано быть
способно** дать другой `flowId`.

### 6.4 Семантика — атрибуты, а не идентичность

`valueSource`, `direction`, `recipientKind`, `destinationKind`,
`tokenState`, `assetSymbol` живут в `FlowAttributes` (§5.2). Любое поле
может быть `UNKNOWN`/`null`, и это **не мешает потоку существовать и
быть идентифицированным**: у него есть родословная и provenance, а
значит и `flowId`.

`FLOW_IDENTITY_UNRESOLVED` теперь означает не «нет ключа», а
**«экономическое назначение потока не классифицировано»**: структурно
поток идентифицирован, семантически — нет. Это разрыв (§11), а не
препятствие к сборке.

### 6.5 `Same Asset ≠ Same Economic Flow` — как это теперь кодируется

Урок сохранён и закодирован точнее:

- **Идентичность актива никогда не является единственной идентичностью
  потока** — `assetSymbol` в `flowId` не участвует, поэтому совпадение
  тикера физически не может слить потоки (мутация 1).
- **Исключать актив из атрибутов не требуется.** `assetSymbol` остаётся
  атрибутом — но заполняется **только если структурно доступен**. В
  замороженных входах структурного поля актива нет, поэтому в v1 он
  почти всегда `null`.
- **Разбирать тикер из свободного текста ради заполнения идентичности
  запрещено** (мутация 27). Система обязана оставаться корректной при
  отсутствующей идентичности актива — и остаётся, потому что идентичность
  строится на provenance.

Уроки FIL / BTC / RENDER / CRV обеспечиваются не ключом, а
разделением по provenance плюс §13.2: разные установленные ветви
происходят из разных допущенных единиц Evidence и потому structurally
различны независимо от того, как их classified.

---

## 7. `value_source` — где он живёт

Freeze S5 (§9 п.4) говорит: «заполнение `evidence.value_source` — работа
S6 и не повод переоткрывать S4». Это указывает **чья** это работа, но не
утверждает, что значение обязано быть записано в строку Evidence. Разница
принципиальна, поэтому решается явно.

### 7.1 Что показал репозиторий

- Колонка `evidence.value_source` существует (миграция 0009), nullable,
  **не пишется никем**.
- Комментарий схемы: «fees / issuance / treasury / collateral_return —
  структурно, не в прозе (CHECK 3)».
- План §6 перечислял `value_source` в одном ряду с `mechanism_state` как
  поле **нормализации при извлечении**. `mechanism_state` S4 пишет,
  `value_source` — нет.

То есть исходный замысел был **extraction-time**, и S4 его не
реализовал. Заполнить его сейчас «задним числом» означало бы вычислить
классификацию в S6 и записать её в строку наблюдения.

### 7.2 Почему S6 не должен писать в Evidence

1. **D-077 запрещает схлопывание понятий.** `EVIDENCE` — что было
   наблюдено; `FACT` — что из этого следует. `value_source` в S6 — это
   вывод классификатора, а не наблюдение. Запись вывода в таблицу
   наблюдений превращает `EVIDENCE` в `FACT` — ровно то схлопывание,
   которое запрещено.
2. **Evidence — неизменяемый provenance** (D-076, freeze S4 §6). Строка
   фиксирует, что ARI видел тогда. Обратная дозапись делает её
   изменяемой и ломает воспроизводимость Proof.
3. **S5 уже создал прецедент** и он принят: производный результат живёт в
   отдельной проекции (`research_component_results`), истина остаётся в
   `evidence`. S6 обязан следовать той же дисциплине.
4. **Дозапись небезопасна при повторе.** Одна и та же строка Evidence
   может участвовать в разных job'ах; классификация же зависит от
   компонента и потока. Одно поле на строку не выражает это без потери.

### 7.3 Рекомендация — D-098

`valueSource` — **атрибут уровня сборки** в производной проекции S6.
`evidence.value_source` остаётся `NULL` и **зарезервированной** под
возможный будущий extraction-time контракт (`evidence_contract_version = 3`),
который был бы отдельным решением владельца и отдельным срезом, а не
побочным эффектом S6. S6 в `evidence` не пишет ничего.

`SOURCE ≠ EVIDENCE ≠ FACT ≠ MEMORY ≠ PROOF` остаётся целым.

---

## 8. Перенос состояния токена

`tokenStateMentions` от S5 — **вход для суждения, а не готовый вывод**
(D-096). S6 распоряжается им так:

1. **Перенос без обобщения.** `RECIPIENT`, установленный из «veCRV
   holders», даёт `tokenState = "vecrv"` на терминальном узле. Превращение
   его в «держатели CRV» запрещено и роняет мутацию 2. Обобщение
   квалифицированного состояния до ликвидного — не упрощение, а подмена
   экономического права.
2. **Отсутствие вывода в обратную сторону.** `DESTINATION` = токен в
   казначействе не даёт «сожжён»: `destinationKind` останется `TREASURY`,
   а `BURN` появится только из собственного установленного основания.
3. **Несовместимые состояния соседних компонентов не сливаются.** Если
   `RECIPIENT` несёт `vecrv`, а `DESTINATION` — незаквалифицированный
   `crv`, S6 **не выбирает** между ними: узлы сохраняют свои состояния,
   поток получает `gap` типа `TOKEN_STATE_MISMATCH`.
4. **`TOKEN_STATE_PATH_MISMATCH` отдельным результатом не вводится** —
   это `MechanismGap` типа `TOKEN_STATE_MISMATCH` (§11). Отдельный
   верхнеуровневый результат был бы вердиктом в маскировке (§22).
5. **Сравнение — точное нормализованное равенство строк**, ровно как в
   S5. Семантическая эквивалентность состояний в S6 не выводится ни в
   какую сторону.

---

## 9. Actor × Action

CHECK 1 требует не допускать «нода получает награду ⇒ пассивный держатель
получает доход».

**Решение: в v1 актор представлен `recipientKind` из закрытого словаря и
привязан к терминальному узлу, свободного именования акторов нет.**

Обоснование: свободное имя актора извлекается только из прозы, то есть
требует модели; закрытый словарь выражает ровно то различие, ради
которого CHECK 1 существует, и не требует семантики.

Три правила, которые это обеспечивают:

1. `recipientKind = PASSIVE_HOLDER` ставится **только** при
   положительном установлении из компонента `RECIPIENT`. Из отсутствия
   информации он не выводится никогда — иначе `System Utility ≠ Passive
   Holder Capture` было бы обойдено молчанием.
2. `recipientKind` относится к **этому потоку**, а не к токену вообще.
   Второй поток с `NODE_OPERATOR` не даёт первому ничего.
3. `RECIPIENT` не установлен → `recipientKind = UNKNOWN` и
   `gap: RECIPIENT_UNRESOLVED`. Узел получателя без основания не
   создаётся.

`action` как свободная строка (поле из плана §9.1) в v1 **не
заполняется**: её нечем заполнить детерминированно. Роль действия несут
`basisComponent` ребра и `direction` потока.

---

## 10. Состояние механизма и временная совместимость

S5 уже вынес суждение на уровне компонента; S6 его не повторяет. Но
собранный поток обязан не выдавать историческое за текущее.

### 10.1 Правило жизненного цикла потока — детерминированное

`lifecycle` вычисляется из статусов и состояний компонентов потока:

```
CURRENT — если ВСЕ условия:
  · CURRENT_STATE ∈ {SUPPORTED, PARTIALLY_SUPPORTED}
  · currentState ∈ {LIVE, IMPLEMENTING}
  · ни один компонент потока не несёт currentState ∈ {DEPRECATED, REMOVED}
    с temporalBasis строго новее, чем temporalBasis CURRENT_STATE

HISTORICAL — если поток имеет установленное исполнение, но:
  · currentState ∈ {DEPRECATED, REMOVED, PAUSED}, либо
  · более новый компонент несёт DEPRECATED/REMOVED

NOT_ESTABLISHED — иначе (в том числе CURRENT_STATE
  INSUFFICIENT_EVIDENCE или CONTRADICTED)
```

`IMPLEMENTING` в `CURRENT` допускается **только** с квалификацией
`STATE_NOT_FULLY_LIVE` на потоке — S5 уже пометил это кодом, S6 переносит.

### 10.2 Пример из задачи

Исполнение 2025 + механизм объявлен `DEPRECATED` в 2026 → **не** текущий
живой путь. Поток собирается с `lifecycle = HISTORICAL`; текущий поток из
тех же компонентов не порождается. Мутация 9 обязана падать.

`NOT_ESTABLISHED` — не сбой и не вердикт: это форма, у которой нет
установленного жизненного цикла.

---

## 11. Разрывы

**S6 не изобретает отсутствующее звено никогда.** Разрыв представляется
явно и машиночитаемо.

### 11.1 Закрытый словарь типов

```ts
type MechanismGapKind =
  | "MISSING_COMPONENT"        // S5: INSUFFICIENT_EVIDENCE
  | "PARTIAL_COMPONENT"        // S5: PARTIALLY_SUPPORTED, оговорка перенесена
  | "CONTRADICTED_COMPONENT"   // S5: CONTRADICTED — победитель не выбирается
  | "TOKEN_STATE_MISMATCH"     // §8.3
  | "FLOW_IDENTITY_UNRESOLVED" // §6.4 — экономическое назначение не классифицировано
  | "TEMPORAL_STATE_MISMATCH"  // §10
  | "RECIPIENT_UNRESOLVED"     // §9.3
  | "DESTINATION_UNRESOLVED"   // DESTINATION не установлен либо kind = UNKNOWN
  | "NET_EFFECT_UNRESOLVED"    // §16
  // Два типа, добавленные вместе с уточнением ветвления D-099:
  | "BRANCH_ATTRIBUTION_UNRESOLVED"  // §13.4
  | "FLOW_ENUMERATION_INCOMPLETE";   // §13.5 — вместо молчаливого усечения
```

Одиннадцать типов, каждый привязан к конкретному правилу выше.
Свободного текста в разрыве нет — только `kind`, `component`,
`afterStep`, `provenance`. Это и мешает «разрыву» стать мусорным ведром.

Два последних типа введены не из симметрии: без
`BRANCH_ATTRIBUTION_UNRESOLVED` ветвление §13.4 пришлось бы разрешать
привязкой «ко всем ветвям», без `FLOW_ENUMERATION_INCOMPLETE` требование
«никогда не усекать молча» было бы невыполнимо при комбинаторном росте.

### 11.2 Пример из задачи

```
Revenue ✅ → Allocation ✅ → Execution ✅
→ [GAP: DESTINATION_UNRESOLVED] → [GAP: NET_EFFECT_UNRESOLVED]
```

Ровно так и собирается: три звена с provenance плюс два типизированных
разрыва. Ничего за разрывом не достраивается.

### 11.3 Отдельного канала warnings нет

Всё, что было бы «предупреждением сборки», уже является разрывом или
квалификацией (§12). Второй параллельный канал сообщений создал бы место
для мягких формулировок, не обязанных быть закрытым словарём. Ответ на
частичный вопрос §2 задачи: **нет, отдельные warnings не вводятся.**

---

## 12. Потребление статусов S5

Правила для каждого статуса — точные и без исключений:

| Статус S5 | Что даёт сборке | Чего не даёт никогда |
|---|---|---|
| `SUPPORTED` | полноценный узел/ребро/атрибут | — |
| `PARTIALLY_SUPPORTED` | тот же элемент, но **обязательно** с `NodeQualification`, перенесённой из `reasonCodes` S5, и с сопутствующим `gap: PARTIAL_COMPONENT` | безоговорочного элемента; элемент без квалификации структурно невозможен |
| `CONTRADICTED` | `gap: CONTRADICTED_COMPONENT` с обоими наборами `evidenceIds` | узла или ребра; выбора победителя — ни по классу, ни по свежести, ни по количеству |
| `INSUFFICIENT_EVIDENCE` | `gap: MISSING_COMPONENT` | ничего иного; выведенного звена |

```ts
type NodeQualification =      // ровно коды S5, не новый словарь
  | "INSUFFICIENT_AUTHORITY" | "INDIRECT_ONLY"
  | "STATE_NOT_FULLY_LIVE" | "TOKEN_STATE_UNQUALIFIED";
```

**Ответ на вопрос §12 задачи:** `PARTIALLY_SUPPORTED` включается как
**условный элемент**, а не как разрыв и не как полноценное звено — он
одновременно даёт элемент с квалификацией и разрыв рядом с ним. Так S7
получает и форму пути, и честную границу, не восстанавливая её сам.

`S6` не может превратить `CONTRADICTED` в `SUPPORTED` и не может достроить
`INSUFFICIENT_EVIDENCE` — оба запрета имеют мутационные зубы (11, 12).

---

## 13. Ветвление и потоки с общим префиксом (D-099, нормативно)

### 13.1 Ветвление не ограничено терминалом

Первая редакция допускала расхождение только на терминальном узле. **Это
исправлено владельцем:** реальные механизмы расходятся раньше — в первую
очередь на стадии распределения.

```
Revenue → Allocation → 50% Buyback   → Destination A
                     → 30% Treasury  → Destination B
                     → 20% Operations→ Destination C
```

**Нормативное представление:** S6 представляет один или несколько
ограниченных Pattern-упорядоченных потоков; несколько потоков **могут
разделять установленный префикс**. Расхождение допускается на любой
позиции Pattern, где оно структурно установлено.

```
Поток A: Revenue → Allocation → Buyback    → Destination A
Поток B: Revenue → Allocation → Treasury   → Destination B
Поток C: Revenue → Allocation → Operations → Destination C
```

Каждый поток — полная, самостоятельно трассируемая родословная. Общая
часть выражена **общим префиксом**, а не разделяемой ссылкой на узел
графа. Произвольного графа не появляется: расхождение только вперёд по
шагам Pattern, слияний ветвей нет, циклов нет, рёбер «назад» нет.

### 13.2 Слоты ветвей — структура и provenance, классификация не участвует

> **Идентичность слота структурна и provenance-первична.** Два кандидата
> занимают один слот **только** тогда, когда детерминированная
> структурная родословная показывает, что это один и тот же собранный
> экономический элемент.

Ранняя редакция группировала кандидатов по совпадению классификации
(«одинаковый известный ярлык → один слот»). **Это отвергнуто как
нарушение D-101** и заменено правилом ниже. Дефект был реальным:

```
Кандидат A: выручка протокола → BUYBACK → казначейство A
Кандидат B: выручка протокола → BUYBACK → казначейство B
```

Оба классифицируются как `BUYBACK`, но при разной provenance это **разные
экономические ветви**, и склеивать их по совпадению ярлыка недопустимо.
Совпадение классификации не является доказательством идентичности потока.

**Ключ слота — только структурные входы:**

```ts
slotKey = (
  step,                 // позиция Pattern
  component,
  parentBranchPath,     // родословная родительской ветви
  structuralUnitKey     // extractionUnitKey, либо при null —
)                       // contentHash + дайджест нормализованного fragment
```

Правило разбиения `supportingEvidenceIds` одного компонента:

1. каждой допущенной единице Evidence вычисляется `slotKey` **из полей
   §3.1, ни одно из которых не является классификацией**;
2. единицы с **идентичным `slotKey`** образуют **один слот** с
   агрегированным provenance — это и есть законная группировка
   «несколько Evidence подтверждают один структурный элемент»;
3. единицы с различающимся `slotKey` — **разные слоты**, независимо от
   того, совпали их классификации или нет, известны они или `UNKNOWN`;
4. два и более слота ⇒ установленное расхождение на этой позиции; один
   слот ⇒ расхождения нет.

**Что запрещено объединять слоты — закрытый список:** совпадение
`valueSource`, `direction`, `recipientKind`, `tokenState`, классификации
механизма, классификации экономического исхода, идентичности актива, а
также совпадение значений `UNKNOWN`. Ни одно из них не является
структурным входом и в `slotKey` не входит.

Нумерация слотов детерминирована и тоже структурна: сортировка по
`structuralUnitKey`, затем по отсортированному набору `evidenceIds`.
Классификация в сортировке не участвует, поэтому её изменение не может
изменить ни номер слота, ни `branchSlotPath`, ни `flowId`.

**Если S6 не может детерминированно установить, что два кандидата
принадлежат одному слоту, — он держит их раздельно** и, где это касается
привязки нижележащих элементов или обрыва перечисления, выставляет
`BRANCH_ATTRIBUTION_UNRESOLVED` либо `FLOW_ENUMERATION_INCOMPLETE`.
Догадок нет.

### 13.2a `UNKNOWN` — значение атрибута, а не механизм идентичности

Правило «каждый `UNKNOWN` автоматически получает собственный слот»
**удалено**: оно работало как семантическое правило идентичности, то есть
ровно то, что D-101 запрещает, — просто в обратную сторону.

Корректный принцип один и тот же для обоих случаев:

> Родословная и provenance определяют идентичность слота **независимо от
> того, известна классификация или нет.**

| Ситуация | Исход |
|---|---|
| два кандидата `UNKNOWN`, разная provenance | **разные** слоты |
| два кандидата `BUYBACK`, разная provenance | **разные** слоты |
| два кандидата `UNKNOWN`, идентичный `slotKey` | один слот, provenance агрегирован |
| несколько Evidence одного структурного кандидата | один слот с несколькими provenance id |

`UNKNOWN` не даёт ни склейки, ни разделения — он вообще не участвует в
идентичности.

### 13.2b Известное ограничение v1 — направление ошибки

В замороженных входах **структурного идентификатора ветви не
существует** (§0.1). Поэтому, когда два независимых документа описывают
одну и ту же ветвь, их `structuralUnitKey` различны, и S6 v1 представит
их как **разные потоки**.

Это принятое ограничение, а не дефект реализации: направление ошибки —
**избыточное разделение, никогда ложное слияние**, что и предписано
владельцем («не можешь установить — держи раздельно»). Устранение
потребовало бы структурного идентификатора ветви **на этапе извлечения**
— то есть будущего контракта S4 (зарезервированный путь D-098), а не
классификатора в S6. Классификатором эту задачу решать запрещено.

**Долей S6 не восстанавливает.** `50/30/20` не читается, не суммируется и
не проверяется на 100%: числовых утверждений срез не делает вовсе.

### 13.3 Общий префикс

```
sharedPrefixId = sha256( canonical(
  patternVersion,
  [ LineageStep... ]  — до позиции расхождения включительно
))
```

Потоки A, B и C несут один `sharedPrefixId` и разные `flowId`.
`branchPointStep` — шаг, на котором они разошлись. Это даёт S7 понять,
что общая часть установлена один раз, без введения графа.

### 13.4 Привязка звеньев ниже точки расхождения

Открытый структурный вопрос, который обязан быть решён консервативно.
Если `DESTINATION` разошёлся на две ветви, а `NET_EFFECT` пришёл одним
результатом S5 — к какой ветви он относится?

**Правило:** нижележащий компонент привязывается к ветви, только если его
допущенное Evidence разделяет provenance с этой ветвью — совпадает по
`sourceId` либо по единице извлечения. Иначе он **не привязывается ни к
одной** и порождает разрыв `BRANCH_ATTRIBUTION_UNRESOLVED` на каждой
затронутой ветви.

Привязать «ко всем ветвям» запрещено: это позволило бы ветви `buyback`
унаследовать чужой чистый эффект — ровно то смешение, которое запрещает
§16 (мутация 28).

### 13.5 Числового ограничения нет; молчаливого усечения нет

Числовой лимит «до двух цепочек» из плана §9.3 **снят** (D-099).
Ограничением служит структура, а не счётчик.

Установленный поток обязан быть **либо представлен, либо объявлен**
машиночитаемым структурным ограничением. Молчаливое усечение запрещено
безусловно. На случай комбинаторного роста (несколько точек расхождения
перемножаются) вводится разрыв `FLOW_ENUMERATION_INCOMPLETE`: он
объявляет, что перечисление ветвей оборвано, и называет позицию — но
никогда не убирает поток тихо (мутация 29).

---

## 14. Возврат принципала ≠ награда

Урок FIL. Различие делается **направлением потока**, а не прозой:

```
INBOUND  — ценность приходит к протоколу/механизму (оплата, комиссия)
OUTBOUND — ценность уходит от протокола к получателю (награда, распределение)
RETURN   — возврат ранее внесённого (залог, принципал)
```

`direction = RETURN` вместе с `valueSource = COLLATERAL_RETURN`
структурно не может быть прочитан как награда: `RETURN` не порождает
терминального узла с экономическим приобретением, и `recipientKind`
для него не становится `PASSIVE_HOLDER`.

Аналогично `PROTOCOL_ISSUANCE` (`OUTBOUND`, эмиссия) и `USER_PAYMENT` /
`FEES` (`INBOUND`, оплата клиентом) — разные источники ценности, и
происходят они из разных допущенных единиц Evidence, а значит образуют
**разные слоты и разные потоки** (§13.2). Различает их provenance;
`valueSource` только называет уже установленное различие.

Идентичность источника сохраняется вдоль всего пути: `valueSource` —
атрибут потока, а не отдельного звена, поэтому «размыться» по дороге не
может. При этом даже `valueSource = UNKNOWN` не позволяет сложить два
потока: идентичность к семантике не сводится (D-101).

---

## 15. Оплата пользователем против эмиссии протокола

BTC: комиссия, уплаченная пользователем, против субсидии, выпущенной
протоколом. RENDER: сжигание при оплате клиентом против эмиссионной
награды ноде.

Отдельной таксономии для этого **не вводится** — различие уже полностью
выражено существующим словарём CHECK 3 (`USER_PAYMENT` / `FEES` против
`PROTOCOL_ISSUANCE` / `BURN_LINKED_ISSUANCE`) как атрибут потока.
Структурно же потоки различны потому, что опираются на разное
допущенное Evidence: разные известные классификации дают разные слоты
(§13.2), а разные слоты — разные `flowId` (§6.3). Мутации 7 и 8 падают
на том же механизме, что и мутация 1: не на отдельном правиле, а на
конструкции идентичности.

`valueSource` определяется **только** из компонента `SOURCE_OF_VALUE`
(§4). Вывод источника из тикера актива, из имени механизма или из
получателя запрещён (мутация 18).

---

## 16. Net Effect

`NET_EFFECT` потребляется от S5 и **не пересчитывается**.

```ts
interface NetEffectAttachment {
  componentStatus: ComponentReconciliationStatus;
  qualifications: NodeQualification[];
  provenance: Provenance;
}
```

Правила:

1. `NetEffectAttachment` появляется **только** при `NET_EFFECT` в статусе
   `SUPPORTED` / `PARTIALLY_SUPPORTED`. Иначе — `gap:
   NET_EFFECT_UNRESOLVED`.
2. **Ярлык механизма исходом не является.** `destinationKind = BURN` не
   создаёт и не подменяет `NetEffectAttachment`; `Gross Burn ≠ Net Supply
   Effect`. Ребра «сокращение предложения» без установленного
   `NET_EFFECT` не существует (мутации 3, 4).
3. `BUYBACK_HOLD ≠ BURN`, `TREASURY ≠ DISTRIBUTION` — разные значения
   закрытого словаря, между собой не переходящие (мутация 5).
4. Своего вердикта о чистом эффекте S6 не выносит: он переносит статус и
   квалификации S5 и присоединяет их к потоку.

---

## 17. Durability

`DURABILITY_BASIS` — не экономическая передача, поэтому звеном не
становится (§4).

```ts
interface DurabilityAttachment {
  componentStatus: ComponentReconciliationStatus;
  basisClasses: EvidenceSourceClass[];   // фактические классы допущенного Evidence
  qualifications: NodeQualification[];
  provenance: Provenance;
}
```

**Спекулятивная таксономия не вводится.** Значения вида
«governance-controlled / contractual / discretionary / time-bounded /
emission-dependent» в репозитории ничем не обоснованы и потребовали бы
семантической классификации прозы. Вместо них переносится то, что уже
установлено: статус компонента и **фактические классы источников**
допущенного Evidence (`GOVERNANCE` → основание закреплено управлением;
`OFFICIAL_DOCS` → документировано). Это фактическое наблюдение, а не
интерпретация.

---

## 18. Provenance

```ts
interface Provenance {
  componentResults: { step: number; component: string }[];  // ключ S5
  evidenceIds: string[];                                    // детерминированный порядок
}
```

Инварианты:

1. **Каждый узел, ребро и атрибут обязан иметь непустой `provenance`.**
   Единственное исключение — `MechanismGap`, который по определению
   ссылается на отсутствие; у него `componentResults` непуст (сам
   результат S5 существует), а `evidenceIds` может быть пуст.
2. Узел или ребро с пустым `evidenceIds` — **нарушение инварианта и
   системный сбой** (§21), а не тихая пустота (сценарий R, мутация 14).
3. `evidenceIds` — только идентификаторы. **Текст Evidence в сборку не
   копируется** ни в каком виде: проза принадлежит Proof (S7) и берётся
   там из `evidence` по этим же id.
4. Цепочка трассируемости полна и односторонняя:
   `элемент сборки → ComponentReconciliationResult → evidence.id →
   retrieved_url + content_hash`.

---

## 18a. Детерминированный алгоритм по шагам

```
 1. Проверить контракт: patternVersion контракта = Pattern = результатов S5.
    Расхождение → системный сбой (§21 п.2). componentRequirementsFor на
    каждый компонент — отсутствие записи → PatternConfigurationError.
 2. Индексировать результаты S5 по (step, component). Дубль ключа →
    системный сбой (§21 п.3).
 3. Загрузить допущенное Evidence: только id из supportingEvidenceIds и
    contradictingEvidenceIds, только поля §3.1. Висячая ссылка →
    системный сбой (§21 п.5).
 4. По каждому компоненту разбить допущенные единицы на слоты (§13.2)
    ПО СТРУКТУРНОМУ slotKey: (step, component, parentBranchPath,
    structuralUnitKey). Классификация в разбиении не участвует ни в
    каком виде. Идентичный slotKey → один слот с агрегированным
    provenance; различный → разные слоты. Слоты отсортировать и
    пронумеровать структурно.
 5. Построить множество родословных: пройти позиции Pattern по возрастанию
    шага; на позиции с ≥2 слотами размножить текущие родословные по
    слотам, зафиксировав branchPointStep и продлив branchSlotPath.
    Комбинаторный предел достигнут → gap FLOW_ENUMERATION_INCOMPLETE,
    никогда молчаливое усечение (§13.5).
 6. Для каждой родословной вычислить flowId и sharedPrefixId (§6.3, §13.3).
 7. Материализовать узлы, рёбра и атрибуты по §4 и §12: статус S5
    определяет существование, PARTIALLY_SUPPORTED обязан нести
    квалификацию и сопутствующий разрыв.
 8. Привязать нижележащие компоненты к ветвям по совпадению provenance
    (§13.4); нет совпадения → BRANCH_ATTRIBUTION_UNRESOLVED, никогда
    «ко всем ветвям».
 9. Заполнить FlowAttributes закрытыми детекторами по допущенному тексту
    (§5.3); нераспознанное → UNKNOWN. Идентичность на этом шаге не
    трогается ни при каком исходе.
10. Вычислить lifecycle потока (§10.1) из currentState и temporalBasis.
11. Присоединить netEffect (§16) и durability (§17), если их компоненты
    установлены; иначе — соответствующие разрывы.
12. Собрать разрывы по §11; выставить shape = COMPLETE_PATH тогда и
    только тогда, когда gaps потока пуст.
13. Проверить инвариант provenance: узел или ребро без evidenceIds →
    системный сбой (§21 п.4).
14. Отсортировать всё по §19 и сделать upsert проекции (§20).
```

Шаг 4 стоит **до** шага 6 и шага 9 намеренно, и это не стилистика:
разбиение на слоты — часть структуры, заполнение атрибутов — нет. Шаг 9
не имеет доступа к результатам шагов 4–6 на запись и не может их
изменить. Проверяется сценариями AC, AQ и мутациями 35, 38, 41.

---

## 19. Детерминизм

Тот же Pattern, тот же Boundary Contract, те же результаты S5 и то же
допущенное Evidence → **тот же `MechanismAssemblyResult`**, независимо от
порядка строк в БД, порядка входного массива и порядка вставки.

Полный порядок сортировки:

| Коллекция | Ключ сортировки (полный) |
|---|---|
| `flows` | `branchPointStep` (`NULLS FIRST`) → `branchSlotPath` → `flowId` |
| `lineage` | шаг Pattern → имя компонента |
| `nodes` | шаг Pattern → имя компонента |
| `edges` | шаг `from` → шаг `to` → `basisComponent` |
| `gaps` | `afterStep` → `kind` → `component` |
| `evidenceIds` | лексикографически по `id` |
| `componentResults` | `step` → `component` |
| слоты ветвей (§13.2) | `structuralUnitKey` → отсортированный набор `evidenceIds` (классификация не участвует) |

Порядок `flows` **не зависит от семантики** — иначе смена классификации
меняла бы порядок при неизменной структуре. `flowId` замыкает порядок
полностью: два потока с одинаковыми `branchPointStep` и
`branchSlotPath` невозможны по построению, но ключ оставлен ради
полноты.

Последний ключ в каждой строке гарантирует полный порядок при полном
совпадении предыдущих. Модельной недетерминированности в срезе нет —
модели нет вовсе. `now` во вход не входит (§3.2), поэтому результат не
зависит и от момента запуска.

---

## 20. Персистенция

**Рекомендация: персистировать как производную проекцию** (вариант B).

Обоснование: S7 (Proof Core) обязан построить слои 1–4 и 6–7 из
собранного механизма, владелец обязан иметь возможность проверить «какое
Evidence дало это звено» без повторного прогона, а восстановление после
падения между S6 и S7 не должно требовать пересборки.

```
research_mechanism_assembly
  (research_job_id, pattern_version) UNIQUE   — одна сборка на job и версию Pattern
  flows           jsonb
  unassigned_gaps jsonb
  created_at / updated_at
```

Дисциплина — та же, что принята для S5 и уже проверена:

1. Истина остаётся в `research_component_results` и `evidence`; строка
   сборки — **производная проекция**.
2. Удаление всех строк и повторный прогон обязаны дать **семантически тот
   же** результат из тех же входов.
3. Ключ `(job, patternVersion)` + upsert → повтор не плодит строк,
   конкурентный повтор безопасен.
4. Лишних таблиц не заводится: узлы, рёбра и разрывы живут внутри
   `flows` как jsonb, потому что реляционных запросов по ним ни у кого
   нет — единственный потребитель читает сборку целиком.

---

## 21. Сбой против структурной неполноты

**Структурная неполнота — не сбой.** Отсутствующий `DESTINATION`,
противоречивый `CURRENT_STATE`, частичный `RECIPIENT` дают **валидный**
`MechanismAssemblyResult` с разрывами. Это прямое продолжение D-084:
недостаточность — законный успешный исход.

**Системный сбой — закрытый список:**

1. невалидный Pattern или `PatternConfigurationError` (компонент без
   `componentRequirements`);
2. расхождение `patternVersion` между контрактом, сборкой и результатами
   S5;
3. невозможный инвариант S5 (дубль по `(job, step, component)`; статус вне
   четырёх; `PARTIALLY_SUPPORTED` без кода основания);
4. узел или ребро без provenance (§18 п.2);
5. `supportingEvidenceIds` ссылается на строку `evidence`, которой нет;
6. ошибка БД.

Всё, что не в этом списке, обязано становиться разрывом, а не
исключением.

---

## 22. Верхнеуровневый статус результата

**Рекомендация: верхнеуровневого статуса нет.**

`ASSEMBLED` / `ASSEMBLED_WITH_GAPS` / `UNASSEMBLABLE` были бы
механически выводимы из `gaps.length` и `flows.length` — то есть не
несли бы новой информации, — но при этом создали бы единственное место в
результате, где может незаметно вырасти суждение о достаточности. Именно
такие поля со временем начинают читать как вердикт.

Форма пути нужна, но она принадлежит **потоку**, а не результату:
`MechanismFlow.shape ∈ { COMPLETE_PATH, PARTIAL_PATH }`, где
`COMPLETE_PATH` означает ровно одно: **у этого потока нет разрывов**.
Это структурная форма, а не достаточность: полный по форме поток может
оказаться недостаточным для утверждения пользователя, и решает это S7.

Ответ на частичный вопрос §2 задачи: `COMPLETE_PATH` / `PARTIAL_PATH`
допускаются **как форма на уровне потока**; верхнеуровневый статус
результата — нет.

---

## 23. Граница S6 и достаточности

| | Вопрос | Слой |
|---|---|---|
| S6 | «Какой механизм восстанавливается из установленных результатов компонентов?» | этот срез |
| S7 | «Достаточен ли этот механизм для утверждения пользователя?» | следующий |

Пример из задачи разбирается буквально: S6 собирает
`Revenue → Buyback → Treasury` с разрывом `NET_EFFECT_UNRESOLVED`.
Вывод «утверждение „buyback сокращает предложение“ не доказано» — **не
делается в S6**, потому что он про утверждение пользователя, а не про
механизм. S6 предоставляет форму, S7 сопоставляет её с вопросом.

Практическое следствие: в `MechanismAssemblyResult` нет ни одного поля,
имя которого содержит `verdict`, `sufficient`, `proven`, `confidence`
или `claim`. Это проверяется тестом (мутация 22).

---

## 24. Модель в S6

**Рекомендация: S6 полностью model-free.** Каждая операция среза
структурна: сравнение enum'ов, сопоставление статусов, сравнение
нормализованных строк, закрытый лексический детектор, сортировка.

**Где семантика действительно понадобилась бы** — названо честно, чтобы
это не выглядело обойдённым:

| Операция | Почему детерминизма не хватает | Решение v1 |
|---|---|---|
| свободное имя актора и действия | извлекается только из прозы | **не заполняется**; роль несут `recipientKind` и `direction` (§9) |
| распознавание перефразированного механизма в двух источниках | требует сравнения смыслов | это Critic-слой, отложенный планом §10.4 |
| классификация `valueSource` в источнике, не покрытом закрытым словарём | требует обобщения | `UNKNOWN` → разрыв, не догадка (§5.3) |

Ни одна из трёх не блокирует S6: во всех случаях честный ответ —
консервативный разрыв, а не вывод модели. Раз модели нет — нет и
бюджета, нет и поверхности инъекции.

Если владелец впоследствии решит, что свободное именование акторов
необходимо, это будет **отдельное решение и отдельный срез** с профилем
стоимости (D-090) и живым гейтом (S10), а не тихое добавление вызова в
S6.

---

## 25. Безопасность

1. **Текст Evidence — недоверенные инертные данные.** S6 не исполняет
   найденные в нём инструкции; модели в срезе нет, поэтому поверхности
   prompt-инъекции нет вовсе.
2. **Операции над текстом ограничены**: только сопоставление с закрытым
   кодом владеемым словарём по границам токенов. Ни регулярных
   выражений, построенных из содержимого страницы, ни интерпретации,
   ни исполнения.
3. **Сети нет.** Ни поиска, ни загрузки, ни внешних вызовов.
4. **Структурные поля приоритетнее текста**: где значение доступно из
   `componentResults` или Pattern, текст не читается вовсе.
5. **Записи нет нигде, кроме собственной проекции**: `evidence`,
   `research_component_results`, память Фазы 5 остаются нетронутыми.

---

## 26. Приёмочная матрица

| # | Сценарий | Ожидание |
|---|---|---|
| A | полная линейная цепочка `SOURCE → FLOW → MECHANISM → EXECUTION → RECIPIENT → DESTINATION → NET_EFFECT` | один поток, `shape = COMPLETE_PATH`, `gaps` пуст, provenance у каждого элемента |
| B | `DESTINATION` отсутствует | `gap: DESTINATION_UNRESOLVED`; узла назначения нет; burn не появляется ниоткуда |
| C | buyback + назначение `TREASURY` | `destinationKind = TREASURY`; `BURN` отсутствует; `NET_EFFECT` не выведен |
| D | `RECIPIENT` установлен из «veCRV holders» | `tokenState = "vecrv"` сохранён; обобщения до `CRV` нет |
| E | один актив, два разных `valueSource` | два `MechanismFlow` с разными `flowId`; слияния нет |
| F | BTC: комиссия пользователя и эмиссионная субсидия | `USER_PAYMENT` и `PROTOCOL_ISSUANCE` — разные потоки |
| G | RENDER: сжигание при оплате и эмиссионная награда ноде | разные потоки; `recipientKind` второго — `NODE_OPERATOR` |
| H | FIL: возврат залога | `direction = RETURN`, `valueSource = COLLATERAL_RETURN`; наградой не классифицируется; `PASSIVE_HOLDER` не появляется |
| I | исполнение 2025 `LIVE` + `DEPRECATED` 2026 | `lifecycle = HISTORICAL`; текущий живой путь не создаётся |
| J | компонент `PARTIALLY_SUPPORTED` | элемент присутствует с `NodeQualification` **и** `gap: PARTIAL_COMPONENT` |
| K | компонент `CONTRADICTED` | `gap: CONTRADICTED_COMPONENT` с обоими наборами id; победителя нет; узла нет |
| L | компонент `INSUFFICIENT_EVIDENCE` | `gap: MISSING_COMPONENT` |
| M | ветвление **на стадии распределения** на три ветви | три потока с общим `sharedPrefixId`, разными `flowId`, `branchPointStep` = шаг распределения; долей не восстанавливается |
| N | тот же актив, другой получатель и действие | потоки не сливаются |
| O | несовместимые состояния токена у соседних компонентов | `gap: TOKEN_STATE_MISMATCH`; состояния сохранены раздельно |
| P | обратный порядок входных массивов | результат побайтово идентичен |
| Q | дубль результата S5 по `(job, step, component)` | системный сбой (§21 п.3), не тихий выбор строки |
| R | узел без provenance | системный сбой (§21 п.4) |
| S | ярлык «burn» в `MECHANISM_SPEC` без установленного `NET_EFFECT` | ребра сокращения предложения нет; `gap: NET_EFFECT_UNRESOLVED` |
| T | возврат принципала | наградой не становится (см. H); `OUTBOUND`-звена приобретения нет |
| U | путь структурно неполон | валидный результат с разрывами, статус успеха |
| V | `valueSource` не распознан закрытым словарём | `UNKNOWN` + `gap: FLOW_IDENTITY_UNRESOLVED`; слияния с другим потоком нет |
| W | `patternVersion` расходится с контрактом | системный сбой (§21 п.2) |
| X | компонент без `componentRequirements` в Pattern | `PatternConfigurationError` — сбой конфигурации, не вывод |
| Y | повторный прогон сборки | идентичный результат, строк не прибавилось |
| Z | `RECIPIENT` не установлен | `recipientKind = UNKNOWN` + `gap: RECIPIENT_UNRESOLVED`; `PASSIVE_HOLDER` не выводится из отсутствия |

Сценарии, добавленные вместе с D-099 и переработанным D-101:

| # | Сценарий | Ожидание |
|---|---|---|
| AA | два потока со **всеми одинаковыми** семантическими атрибутами, но разным provenance | два разных `flowId`; слияния нет — семантика в идентичность не входит |
| AB | все семантические атрибуты обоих потоков `UNKNOWN` | два разных `flowId`; несвязанные потоки не схлопнулись |
| AC | классификация одной единицы Evidence изменена, provenance та же | `flowId` **не изменился**; изменились только `attributes` |
| AD | ветвление на распределении, затем `NET_EFFECT` без общего provenance с ветвями | `gap: BRANCH_ATTRIBUTION_UNRESOLVED` на каждой ветви; ни одна не наследует чужой чистый эффект |
| AE | ветвление на распределении, `NET_EFFECT` разделяет `sourceId` с одной ветвью | привязан только к ней |
| AF | четыре и более установленных потока | все представлены; числового усечения нет |
| AG | перечисление ветвей оборвано структурным пределом | `gap: FLOW_ENUMERATION_INCOMPLETE` с позицией; молчаливого усечения нет |
| AH | `assetSymbol` недоступен структурно | `null`; сборка корректна; из свободного текста тикер не разбирается |
| AI | два потока с одинаковым `assetSymbol` | не сливаются (символ в `flowId` не участвует) |
| AJ | те же замороженные входы, повторный пересчёт после удаления проекции | те же `flowId` и `sharedPrefixId` побайтово |
| AK | несколько Evidence с **идентичным `slotKey`** | **один** слот с агрегированным provenance, не несколько потоков |
| AL | единицы Evidence одного компонента, обе `UNKNOWN`, разная provenance | два слота → два потока; `UNKNOWN` идентичностью не управляет |

Сценарии, добавленные финальным уточнением D-101 (структурное разбиение
слотов):

| # | Сценарий | Ожидание |
|---|---|---|
| AM | две структурно различные ветви, **обе классифицированы `BUYBACK`** | два различных слота и два различных потока; совпадение ярлыка не склеивает |
| AN | две структурно различные ветви с **одинаковыми** `valueSource` + `direction` + `recipientKind` + `tokenState` | два различных потока; семантический кортеж идентичностью не является |
| AO | две ветви, обе `UNKNOWN`, разная родословная provenance | два различных потока |
| AP | несколько `evidenceIds` поддерживают **одну** структурную ветвь | один слот, provenance агрегирован; лишних потоков нет |
| AQ | изменена только классификация: `BUYBACK → UNKNOWN`, структура и provenance неизменны | **`flowId` не изменился**; изменились только `attributes` |
| AR | изменена структурная provenance ветви | `flowId` изменился / появилась отдельная ветвь |
| AS | дефект классификатора: одно и то же значение возвращается для **всех** кандидатов | потоки **не схлопнулись**; их число определяется структурой, а не классификатором |
| AT | дефект классификатора: `UNKNOWN` возвращается для всех кандидатов | то же — число потоков не изменилось |
| AU | два кандидата, принадлежность к одному слоту детерминированно не устанавливается | держатся раздельно; при необходимости — `BRANCH_ATTRIBUTION_UNRESOLVED`; догадки нет |

## 27. Мутационные зубы (обязаны ронять тесты)

1. один актив сливает все потоки · 2. `veCRV` становится `CRV` ·
3. buyback автоматически становится burn · 4. ярлык burn автоматически
становится `NET_EFFECT` · 5. назначение `TREASURY` становится
распределением держателям · 6. возврат залога становится наградой ·
7. комиссия BTC и субсидия сливаются · 8. сжигание и эмиссионная награда
RENDER сливаются · 9. исторический переход становится текущим путём ·
10. `PARTIALLY_SUPPORTED` трактуется как полный · 11. `CONTRADICTED`
получает произвольного победителя · 12. звено для `INSUFFICIENT_EVIDENCE`
изобретено · 13. разрыв пропущен · 14. provenance потерян ·
15. порядок БД или входа меняет сборку · 16. ветвление схлопнуто в один
путь · 17. несовпадение состояния токена молча слито ·
18. `valueSource` выведен из тикера актива · 19. S6 пересчитывает статус
или авторитет S5 · 20. S6 пишет или повышает память · 21. в S6 появился
вызов модели или сети · 22. S6 выдаёт вердикт или суждение о
достаточности · 23. S6 пишет в `evidence` (в том числе `value_source`) ·
24. классификация создаёт звено, которого не установил S5 ·
25. `recipientKind = PASSIVE_HOLDER` выведен из отсутствия данных ·
26. `now` попал во вход и результат стал зависеть от момента запуска.

Зубы, добавленные вместе с D-099 и переработанным D-101:

27. тикер разобран из свободного текста ради заполнения идентичности ·
28. нижележащий компонент привязан «ко всем ветвям» вместо разрыва
`BRANCH_ATTRIBUTION_UNRESOLVED` · 29. перечисление потоков усечено молча,
без `FLOW_ENUMERATION_INCOMPLETE` · 30. **семантический атрибут попал в
вычисление `flowId`** · 31. `UNKNOWN`-единицы схлопнуты в один общий слот ·
32. в `flowId` попал порядок вставки, автоинкремент, ordinal или время ·
33. ветвление разрешено только на терминале (расхождение на распределении
потеряно) · 34. потоки с общим префиксом слиты в один узел графа ·
35. изменение классификации изменило `flowId` при неизменной provenance ·
36. доли ветвления (50/30/20) восстановлены или просуммированы ·
37. слияние двух ветвей вперёд по шагам (появилось ребро «назад» или цикл).

Зубы, добавленные финальным уточнением D-101:

38. **слоты объединены по совпадению классификации** · 39. **слоты
объединены по совпадению семантического кортежа**
(`valueSource`/`direction`/`recipientKind`/`tokenState`) · 40. **все
кандидаты `UNKNOWN` слиты в один слот** · 41. **выход классификатора
попал в `flowId`** (в том числе через нумерацию или сортировку слотов) ·
42. **структурная provenance проигнорирована при разбиении на слоты** ·
43. слоты объединены по совпадению идентичности актива · 44. недоказанная
принадлежность к одному слоту разрешена догадкой вместо раздельного
представления или разрыва.

---

## 28. Утверждённые решения

Приняты владельцем 2026-08-22 на коммите стратегии `b5f36cf`. D-099
принято с уточнением, D-101 переработано до блокировки под тем же
номером (второе решение на исправление не заводится). Канонические
формулировки — в `docs/DECISIONS.md`.

| ID | Решение | Статус |
|---|---|---|
| **D-098** | **`value_source` — атрибут уровня сборки, а не дозапись в Evidence.** S6 не пишет в `evidence` ни одной колонки: `value_source` — вывод классификатора, а не наблюдение, и его запись в таблицу наблюдений схлопнула бы `EVIDENCE` и `FACT` (D-077) и сделала бы неизменяемый provenance изменяемым (D-076). Колонка остаётся `NULL` и зарезервированной под возможный будущий extraction-time контракт — отдельное решение владельца, а не побочный эффект S6. Классификация сборки не становится задним числом сырым Evidence | **LOCKED** |
| **D-099** | **Ограниченные Pattern-упорядоченные потоки с общим префиксом, не граф.** Расхождение допускается **на любой установленной позиции Pattern**, включая распределение, а не только на терминале; несколько потоков могут разделять установленный префикс (`sharedPrefixId`), и общая часть выражается общим префиксом, а не разделяемым узлом графа. Расхождение только вперёд по шагам, слияний ветвей, циклов и рёбер назад нет. Ветвь существует только при собственном установленном основании; долей S6 не восстанавливает и не суммирует. Числовой лимит «до двух цепочек» плана §9.3 **снят**: ограничение структурное, а не счётчик. Установленный поток обязан быть либо представлен, либо объявлен машиночитаемым структурным ограничением (`FLOW_ENUMERATION_INCOMPLETE`); **молчаливое усечение запрещено безусловно** | **LOCKED** (уточнено) |
| **D-100** | **Существование определяет S5, классификация только помечает.** Узел, ребро и атрибут появляются исключительно из статуса компонента S5 (`SUPPORTED` либо `PARTIALLY_SUPPORTED`); закрытые словари лишь размечают уже допущенный элемент, детектором той же дисциплины, что D-096. Текст «burn» не создаёт звена burn, `buyback ≠ burn`, `treasury ≠ распределение держателям`, `возврат залога ≠ награда`. Нераспознанное — `UNKNOWN` и структурный разрыв, а не семантическая догадка. Классификация не создаёт существование никогда | **LOCKED** |
| **D-101** | **Идентичность потока — структурная и provenance-первичная, семантика остаётся атрибутом.** Семантический кортеж `valueSource × direction × recipientKind × tokenState` идентичностью **не является**: два экономически разных механизма могут совпасть по всем четырём, а `UNKNOWN`-значения схлопнули бы несвязанные потоки. `flowId` — детерминированный канонический хеш от `patternVersion`, упорядоченной по шагам Pattern родословной `(step, component, отсортированные допущенные evidenceIds)` и структурного `branchSlotPath`. Гарантии: разные provenance-подкреплённые ветви не сливаются при равной семантике; совпадение `assetSymbol` не сливает потоки; различие `assetSymbol` само по себе не задаёт разное экономическое назначение; `UNKNOWN` не схлопывает несвязанные потоки — он в вычисление не входит вовсе, а идентичность слота структурна независимо от того, известна классификация или нет; повтор и смена порядка идентичность не меняют; пересчёт из тех же замороженных входов даёт те же id. **Запрещены как входы идентичности:** порядок строк БД, рантайм-ordinal, автоинкремент, время и любое изобретённое семантическое значение. Инверсия: **provenance разделяет, классификация не объединяет никогда** — ошибка классификатора даёт лишнее разделение, а не ложное слияние. `valueSource`, `direction`, `recipientKind`, `destinationKind`, `tokenState` и `assetSymbol` — атрибуты потока и могут быть `UNKNOWN`. Идентичность актива никогда не является единственной идентичностью потока; при отсутствии структурного поля актива `assetSymbol` остаётся `null`, и разбирать тикер из свободного текста ради полноты идентичности запрещено — система обязана оставаться корректной при отсутствующей идентичности актива. **Разбиение кандидатов на слоты ветвей подчиняется тому же правилу и является его частью:** идентичность слота структурна и provenance-первична; два кандидата занимают один слот только тогда, когда детерминированная структурная родословная показывает, что это один и тот же собранный экономический элемент. Совпадение классификации, семантического кортежа, состояния токена, идентичности актива или совпадение значений `UNKNOWN` основанием для объединения слотов не является никогда — `slotKey` строится только из структурных входов `(step, component, parentBranchPath, structuralUnitKey)`, где `structuralUnitKey` — `extractionUnitKey` либо, при его отсутствии, `contentHash` с дайджестом нормализованного фрагмента. `UNKNOWN` — значение атрибута, а не механизм идентичности: правило «каждый `UNKNOWN` получает собственный слот» удалено как семантическое правило идентичности, и родословная определяет слот независимо от того, известна классификация или нет. Несколько Evidence одного структурного кандидата дают один слот с агрегированным provenance. Не установлено детерминированно, что кандидаты принадлежат одному слоту, — они держатся раздельно либо порождают `BRANCH_ATTRIBUTION_UNRESOLVED` / `FLOW_ENUMERATION_INCOMPLETE`; догадка запрещена. Замена классификации при неизменных структурных входах обязана оставить `flowId` прежним; изменение структурной provenance ветви обязано быть способно дать другой `flowId`. Принятое ограничение v1: структурного идентификатора ветви в замороженных входах нет, поэтому два независимых документа об одной ветви дают разные потоки — направление ошибки избыточное разделение, никогда ложное слияние; устранение требует контракта извлечения, а не классификатора | **LOCKED** (переработано до блокировки) |
| **D-102** | **Разрывы — закрытый словарь из одиннадцати типов; структурная неполнота есть успех.** Отсутствующий `DESTINATION`, противоречивый `CURRENT_STATE`, частичный `RECIPIENT`, неразрешённое назначение потока и несовпадение состояния токена дают детерминированные разрывы и квалификации, а не системный сбой. Свободного текста в разрыве нет; отдельного канала предупреждений сборки не вводится. Системный сбой — закрытый список §21. Продолжает D-084 | **LOCKED** |
| **D-103** | **Верхнеуровневого статуса у `MechanismAssemblyResult` нет.** `SUPPORTED_MECHANISM`, `PROVEN`, `FAILED_CLAIM` и эквивалентные замаскированные вердикты S7 не вводятся. Структурная полнота принадлежит потоку: `COMPLETE_PATH` означает ровно «у этого представленного потока нет структурных разрывов сборки» и **не означает** «утверждение пользователя доказано». Поля с именами `verdict`/`sufficient`/`proven`/`confidence`/`claim` в схеме отсутствуют и проверяются тестом. Результат персистируется как производная проекция `research_mechanism_assembly` с ключом `(research_job_id, pattern_version)`; истина остаётся в Evidence, результатах S5 и Pattern/Boundary Contract; удаление и пересчёт из тех же замороженных входов дают семантически идентичную сборку | **LOCKED** |

`S6 — model-free и network-free` отдельного номера не получает: это
прямое следствие D-070 и D-100 и живёт в §24 как контракт реализации.

---

## 29. Границы фаз

S6 **не меняет**: семантику памяти Фазы 5; получение, provenance и
авторитет Evidence (S4); результаты и статусы S5; область Boundary
Contract; `research_enabled = false`; живой гейт S10.

S4 остаётся замороженным (`d7e5b8a`, D-091); S5 остаётся замороженным
(`a657db3`, D-097). Ни один пункт этой стратегии не требует изменения ни
одного из них — включая §7, где отказ от дозаписи `value_source` прямо
выбран ради того, чтобы замороженный provenance остался неизменяемым.

**S7 не начат.**

---

**PHASE 6 S6 STRATEGY APPROVED — READY FOR DEVELOPMENT**
