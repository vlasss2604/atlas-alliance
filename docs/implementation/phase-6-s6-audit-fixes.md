# Фаза 6, S6 — fix-пакет по глубокому аудиту

> Закрывает находки `phase-6-s6-audit.md` (аудит @ `7d72b53`). Каждая
> находка получила диспозицию; каждое исправление несёт регрессионный
> тест, который **падал** на 7d72b53 (тесты — исполняемая форма проб
> аудита, `tests/phase6-s6-audit-fixes.test.ts`, 18 тестов).
>
> Изменены ровно два файла реализации: `mechanism-assembler.ts`,
> `mechanism-assembly-store.ts`. S4/S5 runtime не тронуты (D-091,
> D-097); Evidence по-прежнему не пишется (D-098); словари и схема БД
> не менялись — миграция не требуется.
>
> Регрессия после фиксов: vitest **590 passed / 4 skipped** (было
> 572 + 18 новых), все 51 исходных S6-теста зелёные без единой правки,
> tsc / eslint / drizzle-drift чисто.

## Диспозиции

| Находка | Диспозиция | Исправление |
|---|---|---|
| **HIGH-1** декартово размножение ветвей | **FIXED** | Атрибуция §13.4 решается против всей картины развилки, не по-ветвьево: для каждого ряда считается, сколько ветвей проходит его `sourceId` по **пост-развилочному** provenance. Прикрепляется только ряд, однозначно указывающий на одну ветвь (`passCount == 1`); ряд, разделяемый несколькими ветвями или ни одной, даёт `BRANCH_ATTRIBUTION_UNRESOLVED` и не прикрепляется никуда. Кейс аудита PR1 теперь: 2 потока с разрывом, 0 фабрикаций. Суб-развилка ниже развилки остаётся законной, когда её provenance уникален для одной ветви (отдельный тест) |
| **HIGH-2** кап молча уродует поток | **FIXED** | При капе затронутая родословная получает **позиционированный** `FLOW_ENUMERATION_INCOMPLETE` (component + afterStep + непустой `componentResults`) → `COMPLETE_PATH` для неё структурно невозможен; result-уровневый gap тоже несёт позицию первой точки капа. Сценарии AG и AF получили прямые тесты |
| **MEDIUM-1** привязка через источник префикса | **FIXED** | `postForkSourceIds` заменил прежний накопитель: источники общего префикса больше не считаются «provenance этой ветви» — разделение с префиксом равно разделению со всеми ветвями и ничего не дискриминирует (PR8 теперь: gap на каждой ветви) |
| **MEDIUM-2** token-state инверсия | **FIXED + документированное ограничение** | Сравнение по-компонентно и identity-aware: список mentions одного компонента — описание ОДНОГО состояния (квалификатор + fused-идентичность не конфликтуют); несовпадение — только когда обе стороны несут состояние и их представления не пересекаются. «staked»/«vested» защищены от ложного прочтения как идентичность (зеркало замороженного словаря S5 с явной пометкой синхронизации). Кейс «qualified против незаквалифицированного соседа» (§8 п.3 плана) **недетектируем из замороженных данных S5** (пустые mentions неотличимы от «токен не упомянут») — зафиксировано в коде как ограничение v1 в духе §13.2b, решение — будущий контракт извлечения, не догадка S6 |
| **MEDIUM-3** `executed: true` захардкожен | **FIXED** | `executed = установлен ли EXECUTION_EVIDENCE на этой родословной` для каждого ребра (§5.1 буквально); оба направления покрыты тестами |
| **MEDIUM-4** `TEMPORAL_STATE_MISMATCH` мёртв | **FIXED** | Gap эмитится ровно по предикату newer-conflict из `computeLifecycle` (CURRENT_STATE установлен и заявляет LIVE/IMPLEMENTING, а другой компонент родословной несёт DEPRECATED/REMOVED на строго более новой основе) — gap и деградация lifecycle не могут разойтись. Чистый исторический переход (CURRENT_STATE сам DEPRECATED) gap не порождает — тест |
| **MEDIUM-5** дубли разрывов | **FIXED** | Walk — единственный источник gap'ов отсутствия; absence-эмиссии `buildFlow` гейтированы «нет ли уже gap по этому компоненту» (защитный бэкстоп, не вторая копия). CONTRADICTED даёт ровно один `CONTRADICTED_COMPONENT` (§12); тест инварианта уникальности `(kind, component, afterStep)` |
| **LOW-1** зубы слот-сортировки; мёртвые поля slotKey | **FIXED (зубы) + честная пометка (ключ)** | Новый дискриминирующий тест: смена классификации одного слота в **многослотовой** развилке обязана оставить множество и порядок `flowId` неизменными — мутация «сортировка слотов классификатором» теперь падает (проверено реальной мутацией: 1 fail). Мёртвые входы slotKey (`step`, `component`, `parentBranchPath` — константы в пределах вызова) задокументированы в коде с сильным инвариантом (изоляция на уровне вызова + пред-фильтрация кандидатов); поля сохранены как defense-in-depth формы D-101, **без** заявки мутационных зубов. Правка текста D-101 — решение владельца, не реализации |
| **LOW-2** версия Pattern без плана; stale-строки | **FIXED** | Job без строки `research_plans` теперь отказ (нет замороженного `patternVersion` для кросс-чека — сборка «под тем, что сейчас ACTIVE» запрещена); при upsert стираются строки сборки других версий этого job'а (производная проекция — удаление всегда безопасно). DB-тест на отказ |
| **LOW-3** классификаторы: отрицания/инъекции | **ДОКУМЕНТИРОВАНО** | Явный комментарий у словарей: лексическое сопоставление без грамматики — принятая цена §5.3/§24; искажения касаются только атрибутов, структура/идентичность недостижимы (D-100/D-101, проверено мутациями). Расширение грамматики — решение владельца по закрытому словарю |
| **NOTE-2** висячие contradicting-ids | **FIXED** | `contradictingEvidenceIds` валидируются так же, как supporting (§21 п.5); тест |
| NOTE-1 (лишние поля проекции §3.1), NOTE-3 (`NONE` недостижим), NOTE-4 (формулировка sharedPrefixId), NOTE-5 (матрица покрытия), NOTE-6 (playwright-артефакт) | **ACCEPTED / без изменений кода** | Поля — из утверждённого списка §3.1; `NONE` — зарезервированное значение словаря плана; «включительно» ↔ «исключительно» — кандидат на правку текста стратегии владельцем |

## Что сознательно НЕ сделано

- Не менялись словари классификаторов, словарь gap'ов, схема БД и
  `flowId`-контракт — всё в пределах уже утверждённых D-098…D-103.
- Не трогались замороженные S4/S5 и текст стратегии: два места, где
  контракт стоит уточнить владельцу (формулировка slotKey в D-101;
  «включительно» в §13.3; недетектируемость §8 п.3 из данных S5), --
  названы здесь и в аудите, но не «исправлены» тихо в тексте LOCKED-
  решений.

## Проверка

- 18 новых тестов = пробы аудита (все падали на 7d72b53).
- 51 исходный S6-тест: зелёные без правок — поведение вне находок
  не изменилось.
- Реальная мутация «сортировка слотов по классификатору» теперь
  ловится (1 fail); «attach ко всем ветвям», «lifecycle без
  newer-conflict», «семантика в flowId», «слоты по классификации» —
  ловились и ловятся.
- Полный vitest 590/594, tsc, eslint, drizzle-drift — чисто.
